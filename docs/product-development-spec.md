# EigenTruth Product And Development Charter

EigenTruth is a representation-observability and factuality-control toolkit for LLM systems. It is not a standalone truth oracle, a safety boundary, or a replacement for external evidence. The product direction is to combine internal model diagnostics, calibrated risk scoring, tool-based verification, and optional world-model correction while preserving the language model's creative strengths.

EigenTruth 的产品定位是：面向 LLM 系统的表征观测与事实性控制工具集。它不是单一的“反幻觉模型”，也不是生产安全边界。产品方向是把模型内部诊断、风险校准、工具验证和可选世界模型校正组合起来，在保留 LLM 创造性的同时降低事实错误。

## Current State

Implemented today:

- `TruthManifold`: online hidden-state mean/covariance, ridge-regularized precision, save/load support.
- `TruthProbe`: PyTorch forward-hook monitor for selected Transformer layers, Mahalanobis-style drift, HSE tracking, and optional activation steering.
- `EigenTruthWrapper`: warmup, generation passthrough, diagnostics, and probe lifecycle management for Hugging Face-style PyTorch models.
- `eigentruth.eval`: conformal p-values/thresholds plus CPU-testable metrics such as AUROC, Euclidean dispersion, selective accuracy, coverage, confidence intervals, and high-confidence error audits.
- `eigentruth.calibration` / `eigentruth.eval.conformal`: JSON-serializable calibration artifacts, split-conformal calibrators, adaptive feature-inflated conformal calibrators, layer/score sweep reports, rank-calibrated multi-score fusion artifacts, conformal abstention/comparison reports with conservative selective-correctness lower bounds, and abstention release gates for promotion checks.
- `eigentruth.core.TruthSubspace`: low-rank factual subspace scoring, benchmark residual signal, and optional true-minus-false projection.
- `eigentruth.core.internal_eigenscore` / `lexical_semantic_entropy` / `embedding_semantic_entropy` / `semantic_energy_score` / `lexical_semantic_energy`: INSIDE/EigenScore-style spectral diversity, dependency-free sampled semantic-entropy proxies, and confidence-weighted semantic-energy disagreement, including adaptive sampling budget support in benchmarks.
- `eigentruth.control.RiskController` / `ControlPolicyConfig` / `ParticipationGateConfig`: maps calibrated diagnostic thresholds, optional claim verification results, and optional conformal abstention participation gates to configurable product actions.
- `eigentruth.control.PreGenerationRiskPolicy` / `select_pre_generation_profile`: dependency-free pre-generation runtime-profile routing from prompt features and caller metadata before model or verifier work.
- `eigentruth.control.DefaultCorrectionPolicy` / `PlanAwareCorrectionPolicy` / `ActionRequest`: compiles decisions into executable JSON-ready payloads for accept/retrieve/rewrite/steer/execute-tool/abstain/clarify flows; the plan-aware wrapper can enrich or append retrieval actions from `ClaimVerificationPlan` retrieval queries without changing the wrapped policy by default.
- `eigentruth.control.ActionExecutorRegistry` / `DryRunActionExecutor` / `TimeoutActionExecutor` / `ActionResult`: routes action requests to registered executors with side-effect-free fallback execution and best-effort timeout wrapping for local control-loop traces.
- `eigentruth.control.ActionExecutionPolicy` / `PolicyGuardedActionExecutor`: validates side-effecting action requests, replays idempotent results when a ledger is configured, and records audit metadata such as request ids, idempotency keys, timeout bounds, and timeout-enforcement status.
- `eigentruth.control.InMemoryActionExecutionLedger` / `JsonActionExecutionLedger` / `SQLiteActionExecutionLedger`: store successful idempotent action results for request-local, JSON-file, or SQLite-backed replay without repeating side effects.
- `eigentruth.control.ProductTrace`: JSON-ready traces for diagnostics, claims, claim verification plans, verification results, decisions, action execution summaries, verifier-route summaries, request runtime summaries, and metadata.
- `eigentruth.control.ProductFeedbackRecord` / `ProductFeedbackStore`: dependency-free JSONL feedback records for post-hoc request or claim outcomes, linked by request id or trace fingerprint for product-quality audit reports.
- `eigentruth.control.run_verification_loop` / `StagedVerificationPolicy` / `EvidenceBundle`: dependency-free verify -> decide -> execute -> reverify loop that records a `ClaimVerificationPlan`, passes it through action-planning context for optional plan-aware policies, can gate expensive verifier routes behind diagnostic risk or sensitive claim metadata, then feed retrieval action results back into verifier context.
- `eigentruth.verify.ClaimVerificationPlanner` / `ClaimVerificationPlan` / `VerificationPlanCostEstimate`: dependency-free claim verification planning layer that preserves extracted claim metadata while emitting verifier scope, route hints, retrieval queries, calculator checks, structured-state checks, world-model checks, inferred claim dependencies, and relative route/tool cost estimates for later control/runtime integration.
- `eigentruth.verify.ClaimDependency` / `ClaimCoherenceReport` / `apply_claim_coherence`: optional claim dependency-graph coherence pass that downgrades supported child claims when required parent claims are missing or unsupported; `run_verification_loop(..., enforce_claim_coherence=True)` records coherence reports in product traces.
- `eigentruth.verify`: dependency-free pluggable claim extraction, claim verification planning, rule-based claim metadata, in-memory verifier tools, lexical groundedness checks, and sampled-response self-consistency verification for first-pass claim workflows.
- `eigentruth.adapters.RetrievalActionExecutor` / `InMemoryRetriever`: dependency-free retrieval executor shell for unsupported-claim evidence gathering.
- `eigentruth.adapters.ToolOutputStateSource` / `ToolOutputMapping`: maps local tool/action execution outputs into structured verifier state for post-tool checks.
- `eigentruth.adapters.InMemoryWorldModelAdapter`: deterministic world-model adapter for tests and domain-rule prototypes.
- `eigentruth.adapters.StateTransitionVerifier`: dependency-free action-conditioned postcondition verifier that predicts next state through a world-model adapter, then reuses structured state checks.
- `eigentruth.registry.ArtifactRegistry`: local JSON registry for calibration reports, calibration artifacts, traces, reports, action results, and saved concept metadata.
- Benchmark scripts for TruthfulQA-style evaluation, TruthSubspace residual scoring, layer/score sweeps, conformal calibration, and selective reporting.
- Local development baseline: `make check-fast` runs lint, tests, and dependency consistency; `make check` adds deterministic profile-gate smoke checks; `make release-check` also builds the package.

Recent cleanup and platform changes:

- Core dependency is intentionally light: `torch` only.
- Hugging Face, examples, and benchmark datasets are optional extras.
- CI covers Python 3.10-3.13 with minimum/latest dependency lanes.
- Build metadata uses SPDX license syntax and validates with `python -m build`.

## Product Definition

EigenTruth should become a toolkit with four layers:

1. **Observe**: inspect hidden-state geometry during warmup, forward passes, and generation.
2. **Calibrate**: convert raw signals into reproducible thresholds, p-values, and abstention policies.
3. **Control**: decide whether to monitor, steer, retrieve evidence, rewrite, abstain, or ask for clarification.
4. **Verify**: validate atomic claims against tools, retrieval, structured data, or domain/world-model adapters.

The default product experience should be monitor-first. Steering and correction are opt-in and must expose diagnostics explaining why they fired.

## Non-Goals

- Do not claim that EigenTruth proves an answer is true.
- Do not claim that activation steering eliminates hallucinations.
- Do not hide uncertainty behind confident rewrites.
- Do not make Hugging Face, datasets, retrieval systems, or world models mandatory core dependencies.
- Do not optimize only for a single benchmark if it weakens general observability.

## User Workflows

### 1. Researcher Workflow

A researcher should be able to:

1. Build a manifold from factual warmup examples.
2. Sweep candidate layers and scoring methods.
3. Calibrate thresholds on held-out data.
4. Compare raw diagnostics, conformal risk, and external benchmark labels.
5. Save an experiment artifact with model revision, layer, seed, thresholds, and commit SHA.

### 2. Application Developer Workflow

An application developer should be able to:

1. Attach EigenTruth to an existing PyTorch/HF generation stack.
2. Run in monitor-only mode with no activation changes.
3. Receive structured risk diagnostics per generation.
4. Route high-risk answers through retrieval, verification, rewrite, or abstention.
5. Keep domain-specific tools optional and replaceable.

### 3. Product Workflow

A product system should be able to:

1. Route the request to a latency, balanced, or audit runtime profile from cheap pre-generation signals.
2. Generate a draft answer creatively under the selected runtime budget.
3. Extract atomic claims from the draft.
4. Score internal drift and semantic instability.
5. Verify high-risk claims with external tools or world models.
6. Produce a final answer with confidence, evidence, and correction trace.

## Proposed Runtime Architecture

```text
user request
  |
  v
pre-generation risk router
  |
  +--> latency / balanced / audit runtime profile
  |
  v
LLM draft generation <---- optional retrieval/context injection
  |
  +--> EigenTruth internal diagnostics
  |
  v
atomic claim extraction
  |
  +--> retrieval / database / calculator / code tools
  +--> world-model or domain-state verifier
  |
  v
risk controller
  |
  +--> accept
  +--> rewrite with evidence
  +--> steer and regenerate
  +--> abstain or ask clarification
  |
  v
final answer + diagnostics + evidence trace
```

## World Model Positioning

World-model correction should be an adapter layer, not a hard dependency and not part of the core manifold math.

Recommended interface:

```python
class WorldModelAdapter:
    def verify(self, claim, context): ...
    def predict(self, state, action): ...
    def explain(self, claim): ...
```

Use the world model in three places:

1. **Before generation**: constrain the prompt with domain state, physical limits, business rules, or time-sensitive facts.
2. **After draft generation**: verify atomic claims and correct unsupported statements.
3. **During high-risk generation**: if internal diagnostics spike, pause free generation and route through verification.

This keeps LLM creativity in wording, synthesis, analogy, and exploration, while requiring evidence for factual claims, numbers, citations, physical constraints, and domain-state updates.

## Automatic Parameter Tuning

EigenTruth should treat layer choice, threshold choice, and steering strength as calibrated artifacts, not magic constants.

Planned artifact:

```text
CalibrationArtifact
- model_id
- model_revision
- target_layer
- score_names
- thresholds
- conformal_alpha
- steering_policy
- warmup_dataset_metadata
- calibration_dataset_metadata
- created_at
- eigentruth_version
- commit_sha
```

The calibrator should support:

- layer sweeps
- score sweeps (`maha`, `truth_proj`, future subspace scores)
- conformal thresholds
- steering-lambda sweeps
- coverage and power reports
- CPU-friendly smoke runs before large-model replication

## Module Boundaries

Current modules should remain narrow:

- `eigentruth.core`: tensor math, manifolds, distances, projections. No model loading, no datasets, no network.
- `eigentruth.intervention`: hooks and steering logic for PyTorch modules.
- `eigentruth.models`: user-facing wrappers around model instances.
- `eigentruth.eval`: metrics, conformal thresholds, selective reports, and benchmark helpers.
- `eigentruth.calibration`: calibration artifacts and parameter sweeps.
- `eigentruth.control`: pre-generation routing, risk controller, policy configuration, correction policy, action executor registry, verification loops, traces, and post-hoc feedback records.
- `eigentruth.verify`: claim extraction, verifier protocols, in-memory verification, and lexical groundedness.
- `eigentruth.registry`: local artifact metadata records.
- `eigentruth.adapters`: optional integration shells, including retrieval and world models.
- `examples/`: qualitative demos only; no benchmark claims.
- `benchmarks/`: reproducible evaluations with structured output.

Integration modules should stay interface-first: plain dataclasses, enums, and protocols with no heavyweight dependencies. Concrete retrieval, database, rewrite, and world-model implementations should land behind these contracts as evidence and tests justify them.

## Development Principles

1. **Evidence before claims**: every product claim must be tied to a test, benchmark, or documented limitation.
2. **Monitor before steer**: new interventions should first ship as diagnostics or dry-run policies.
3. **Small core, optional integrations**: keep mandatory dependencies minimal and push integrations into extras.
4. **CPU-first validation**: every new scientific idea needs a small CPU-testable mechanism check.
5. **Calibrate per setting**: thresholds must be model/layer/domain/dataset specific unless evidence shows transfer.
6. **Structured outputs**: diagnostics and benchmarks should produce machine-readable artifacts.
7. **Negative results are assets**: failed hypotheses should be documented rather than silently removed.
8. **No hidden global state**: hooks, probes, and adapters must have explicit lifecycle management.
9. **Safe default behavior**: default mode should not modify model activations.
10. **Composable APIs**: components should be usable independently in existing LLM systems.

## Definition Of Done

For code changes:

- Public behavior is covered by focused tests.
- `make check` passes locally.
- `make release-check` passes for dependency, package, or public API changes.
- New dependencies are optional unless they are required by core math.
- Documentation is updated when behavior, interfaces, dependencies, or limitations change.

For experiment changes:

- The experiment states question, method, accept criterion, deliverable, and cost.
- Dataset provenance, model revision, seed, layer, thresholds, and commit SHA are recorded.
- Results are saved as structured JSON when practical.
- Claims distinguish qualitative observations from benchmark evidence.

For product features:

- The feature has a monitor-only or dry-run mode.
- The feature exposes diagnostics explaining its decision.
- Failure modes are documented.
- High-risk corrections can abstain or ask for clarification instead of fabricating certainty.

## Near-Term Product Roadmap

### Completed 0.2-0.3 Foundation

- Score dump -> layer/score sweep -> conformal calibration artifact -> risk decision -> action request/result -> product trace.
- Conformal abstention report -> runtime `participate/abstain` decision helper plus `eval_conformal.py --save-abstention-report`, `--save-abstention-comparison`, and `--save-abstention-release-gate` sidecars for risk-coverage-controlled answer participation, multi-signal selection, and fail-closed promotion checks; `eval_abstention_stability.py` replays participation-gate candidates across seeded calibration splits without reloading models; `ParticipationGateConfig` can now consume a single report, candidate, or comparison report and let `RiskController` abstain/clarify accepted answers when the selected uncertainty score is outside the retained region.
- Configurable risk policy hooks for refuted, unsupported, error, and compound diagnostic/verification cases.
- Action executor registry with dry-run fallback and a dependency-free in-memory retrieval executor shell.
- Claim extraction metadata for numbers, citations, negation, and time-sensitive claims.
- Benchmark reports now include selective accuracy, coverage, and confidence intervals.

### Completed 0.4 Verification Loop Shell

- Dependency-free verify -> decide -> execute -> reverify helper with final `ProductTrace` output and optional staged verifier gating for low-risk, non-sensitive claims.
- `EvidenceBundle` conversion from retrieval `ActionResult` payloads into claim-scoped verifier evidence context.
- Demo and tests for unsupported -> retrieve -> supported, no-hit retrieve, and refuted-claim hard stop paths.
- `eval_verifier_ensemble.py` benchmark shell for comparing calibrated internal diagnostics against retrieval/verifier suppression and refutation policies, including structured QA, static state, and action-conditioned state-transition routes.
- `build_truthfulqa_corpus.py` for creating a local TruthfulQA correct-answer evidence corpus.
- `build_evidence_fixture.py` for building non-oracle verifier fixtures from statement-bearing score dumps and local JSON/JSONL/text corpora, with explicit `label_usage` / `input_provenance` metadata and an `--omit-label-metadata` mode so adapter-audit fixtures can keep labels only in the score dump used for evaluation.
- `backfill_truthfulqa_statements.py` for adding statement metadata to older TruthfulQA score dumps without rerunning models.
- Qwen l80 / SmolLM2 l80 oracle verifier-ensemble upper-bound report: label-derived perfect evidence drives verified false alarm to 0.000 and detection to 1.000 at alpha 0.100.
- Verifier ensemble reports include label-conditioned `verification_quality`, so evidence fixture quality can be measured separately from downstream control-policy metrics.
- Qwen l80 / SmolLM2 l80 local-corpus verifier baseline: conservative correct-answer retrieval drives verified false alarm to 0.008 at alpha 0.100, with true-supported rate 0.908 and false-supported rate 0.042.
- `QuestionAnswerVerifier` structured QA/domain-state adapter and Qwen l80 / SmolLM2 l80 structured QA baseline: exact question-answer facts drive verified false alarm to 0.000 and detection to 1.000 at alpha 0.100 on covered TruthfulQA questions.
- Staged structured QA gate artifact: at alpha 0.100, Qwen l80 / SmolLM2 l80 skip 79.3% / 82.9% of structured QA verifier calls, aggregate staged verified false alarm is 0.009, aggregate staged verified detection is 0.275, and `compare_verifier_routes.py` promotes `structured_qa` under explicit staged skip/quality gates. The promoted staged route is registered as `benchmark_manifest:truthfulqa-l80-structured-qa-staged-route:0.4`, and `compare_route_baselines.py` promotes it as the current local verifier-route baseline.
- Frontier l80 verifier-stability report: `eval_verifier_stability.py` replays staged structured QA on the current Qwen/SmolLM2 l80 JSONL frontier score dumps across seeds `0..9`; `report:truthfulqa-frontier-qwen-smollm2-l80-verifier-stability:0.1` shows verified false alarm/detection means of `0.006` / `0.305` for Qwen and `0.010` / `0.244` for SmolLM2, beating internal-only detection in 10/10 seeds for both runs while routing only 115/556 and 95/556 records respectively to `structured_qa`. The verified manifest is `benchmark_manifest:truthfulqa-frontier-qwen-smollm2-l80-verifier-stability:0.1`.
- `CalculatorVerifier` deterministic tool adapter for structured arithmetic claims and simple symbolic equations, using a restricted local arithmetic evaluator with no new mandatory dependencies.
- `StructuredStateVerifier` / `StateCheck` structured state and business-rule adapter for database, policy, and domain-state checks. It can be routed through `state_check` claim metadata or context and returns supported, refuted, insufficient-evidence, or error results with explicit decision rules.
- `SQLiteStateSource` / `SQLiteStateQuery` load read-only SQLite query results into nested verifier state, giving the structured-state path a real database integration without new mandatory dependencies.
- `ToolOutputStateSource` / `ToolOutputMapping` map local action or tool execution outputs into nested verifier state, giving post-tool checks a structured path without requiring a new tool runtime.
- `StateTransitionVerifier` / `StateTransitionCheck` verify claims about action consequences by predicting next state through a world-model adapter and checking structured postconditions; `InMemoryWorldModelAdapter` supports nested and dotted-path `set`/`increment`/`decrement` actions for deterministic domain-rule tests.
- `sqlite_state_control_demo.py` seeds an order/inventory/account SQLite fixture and emits a final `ProductTrace` where database state drives a dry-run abstain action despite low internal diagnostics.
- `state_transition_control_demo.py` emits a final `ProductTrace` where a world-model predicted postcondition refutes a claim about action consequences despite low internal diagnostics.
- `production_tool_loop_demo.py` emits one product-style trace that combines SQLite pre-tool checks, guarded side-effecting local `execute_tool`, optional JSON/SQLite idempotency replay, mapped `ActionResult.output`, post-tool structured verification, action execution summary, action audit metadata, and runtime route summary metadata.
- `TimeoutActionExecutor` can return a traceable `timed_out` action result using stdlib thread-pool timeouts; it is a best-effort local boundary, not a hard cancellation mechanism for already-running side-effecting adapters.
- `CompositeVerifier`, `RoutedVerifier`, and `calibrated_control_demo.py --enable-calculator` show a tool-first product trace path: claim metadata, context, or text patterns can route calculator-supported/refuted claims before lexical verifier fallback. `SentenceClaimExtractor` now adds structured `calculation.expression` / `calculation.expected` metadata for simple symbolic equations and limited word-operator arithmetic such as `2 plus 2 is 5`, so calculator routing does not depend only on text patterns.
- `StagedVerificationPolicy` can be passed to `run_verification_loop` to skip expensive verifier routes for low-risk, non-sensitive claims while still triggering verification for calibrated diagnostic risk or claim metadata such as numbers, citations, time sensitivity, or explicit `requires_verification` flags. Product loops default to `fail_closed_on_skip=true`: skipped factual verification returns `clarify/unknown` rather than silently accepting unverified claims; local latency experiments can explicitly set `stage_fail_closed_on_skip=false` to preserve the older skip-and-accept behavior.
- `ClaimDependency` / `apply_claim_coherence` adds an optional dependency-graph coherence check over verified claims, so staged verification can fail closed when a supported conclusion depends on a missing or unsupported premise; coherence reports are trace events and metadata, and the default loop behavior is unchanged unless explicitly enabled or dependency edges are supplied.
- `SelfConsistencyVerifier` can be evaluated as a `self_consistency` verifier-ensemble route when fixtures provide sampled responses, giving claim-level support/refutation rates before retrieval fallback.
- `build_selfcheck_fixture.py` converts dumped INSIDE sampled continuations or external sampled-generation files into verifier-ensemble fixtures for reproducible self-consistency route evaluation.
- `RoutedVerifier` records route match reasons such as metadata keys, context keys, text patterns, feature flags, or fallback selection; `ProductTrace.verification_route_summary()` aggregates selected, matched, and skipped verifier routes for runtime trace review.
- `RuntimeProfile` now exposes shared `latency`, `balanced`, and `audit` defaults for release gates and product control-plane staging; `PreGenerationRiskPolicy` / `select_pre_generation_profile()` choose a profile before generation from cheap prompt/metadata risk markers; `calibrated_control_demo.py --pre-generation-profile auto` records that assessment in `ProductTrace.metadata` and can apply it when no explicit runtime profile is supplied; `RuntimeProfileSelectorPolicy` makes the post-diagnostic auto selector configurable by diagnostic risk/action, sensitive claim features, metadata keys, and optional verification-plan cost thresholds; `select_runtime_profile()` chooses among profiles before verifier work, and `calibrated_control_demo.py --runtime-profile auto --runtime-profile-selector-policy` writes the selected profile, selection reason, selector policy, and staging behavior into trace and registry metadata.
- `RuntimeTrace` / `RuntimePhaseTiming` records dependency-free request phase timings in `ProductTrace.runtime_trace`, so product runs can compare diagnostic, verification, action execution, retrieval evidence, and re-verification costs before changing performance defaults.
- `ProductRuntimeBudgetPolicy` / `evaluate_product_runtime_budget` turns `ProductTrace.runtime_trace`, cache metadata, verifier route metadata, and staged-verification skip events into optional fail-closed product gates over total request time, named phase timings, phase p95/p99 timings, route duration, mean attempted routes, retrieval use rate, aggregate/named cache hit rates, minimum verification skip rate, and maximum verified claim count; `calibrated_control_demo.py --max-runtime-total-seconds/--max-runtime-phase-seconds/--max-runtime-phase-p95-seconds/--max-runtime-phase-p99-seconds/--max-mean-attempted-route-count/--max-retrieval-use-rate/--min-cache-hit-rate/--min-verification-skip-rate/--max-verified-claim-count` writes the evaluated result into trace and registry metadata.
- `run_product_runtime_baseline.py` now adds `optimization.hotspots`, `optimization.recommendations`, and `optimization.policy_hints` to product runtime baselines, while runtime metrics expose claim verification-plan coverage plus route/tool-payload counts and ProductTrace summaries retain verification-plan cost estimates. Saved traces can identify dominant phases/routes, low cache hit rates, excessive retrieval or verifier fanout, missing staged verification, missing verification plans, audit-heavy profile usage, and candidate budget thresholds without rerunning models or verifiers. With `--save-recommended-policy`, those candidate thresholds are also materialized as a reusable `ProductRuntimeBudgetPolicy` artifact for later gate runs.
- `eval_verifier_ensemble.py` now supports `--state-source` JSON state and SQLite query specs plus fixture-level `state_check` and `state_transition` routes, and reports `route_summary`, `route_quality`, route-level cost/tail-latency metrics, optional `--staged-verification` skip-rate/cost gating metrics, and per-alpha `route_control_impact` for structured QA, state transition, structured state, lexical groundedness, retrieval-backed groundedness, and staged verifier skips; `--verified-records-jsonl` writes per-claim verifier outputs to a compact sidecar for audit/debug without embedding them in the main report, and `--compact-json` keeps large automated report artifacts smaller without changing payload semantics.
- `refresh_verifier_route_artifacts.py` regenerates current-schema verifier route reports from saved score dumps, claims, and local verifier corpora so old artifacts can be upgraded for promotion gates without rerunning model forward passes; its summary exposes route latency/cost/cache metrics for promotion audit, supports compact verifier/route/promotion/workflow JSON output, and regression coverage now includes structured QA, structured-state, and state-transition promotion gates.
- `build_domain_state_fixture.py` generates deterministic order-fulfillment score/claim/state fixtures plus optional SQLite database and SQLite state-source spec so structured-state verifier behavior can be benchmarked without relying on label-derived TruthfulQA oracle evidence.
- `build_transition_fixture.py` generates deterministic order-reservation score/claim/state fixtures so world-model predicted postconditions can be benchmarked without a real simulator or external dependency.
- `compare_verifier_routes.py` aggregates route-level verifier quality, runtime cost, tail latency, cache summary, staged-verification skip/quality metrics, and control impact across saved verifier-ensemble reports, producing a cost-aware route leaderboard, Pareto frontier candidates, route-specific promotion decisions, by-route weighted metrics, and optional fail-closed route quality gates without loading models. Aggregate cost means use only observation counts paired with finite source totals; missing or non-finite source metrics are surfaced as `invalid_metric_counts` and block promotion instead of being silently averaged away; configured staged gates fail closed when staged evidence is missing or verifier skipping degrades verified false alarm/detection beyond threshold.
- `run_adapter_promotion_workflow.py` composes route comparison, route-specific `promotion_decision=promote`, and optional registry-backed performance baseline gates into one fail-closed adapter promotion report, with compact JSON output and optional registry-ready artifact manifests available for automation artifacts.
- `run_adapter_promotion_registry_workflow.py` closes the route-baseline automation loop: it runs adapter promotion, writes the artifact manifest, recursively verifies it, and registers the promoted verifier-route baseline in `ArtifactRegistry` unless the route decision is blocked. Registry metadata includes staged-verification skip/quality metrics when the source route comparison was built with staged gates.
- `compare_route_baselines.py` compares registered verifier-route promotion manifests without rerunning model or verifier work, recursively verifies each manifest, reloads saved route-comparison reports, applies optional quality/cost/tail/retrieval/runtime/cache-reuse gates, can require non-oracle retrieval evidence with label-free claim metadata and `input_provenance`, fails closed on invalid source metrics or enabled budget metrics that are missing/non-finite, and recommends one deployable route baseline.
- `run_adapter_family_matrix.py` builds deterministic structured QA, structured-state, state-transition, optional retrieval-groundedness, and optional retrieval-structured-QA fixtures, runs each through refresh/promotion gates, and aggregates the route-family quality/cost/tail/cache metrics into one local no-model comparison matrix.
- `run_local_retrieval_route_workflow.py` turns a statement-bearing score dump and local JSON/JSONL/text corpora into a registered retrieval-groundedness route baseline, including the generated claims fixture, verifier report, route comparison, promotion report, source corpus, score dump, optional persistent SQLite FTS candidate retrieval metadata, optional fingerprint-keyed claims and verifier-trace cache metadata, lightweight runtime profile metadata, and optional runtime/cache budget gates in the manifest for recursive provenance and cost checks.
- `run_adapter_readiness_workflow.py` composes adapter-family quality promotion with same-machine cache-profile performance promotion into one final fail-closed adapter readiness decision, can explicitly include retrieval-groundedness and retrieval-structured-QA adapter families, writes `runtime-recommendation.json` from the promoted performance matrix plus optional INSIDE sampling profile / trigger-budget sweep evidence, and includes it in the top-level artifact manifest for recursive verification and registry promotion.
- `run_adapter_readiness_registry_workflow.py` runs the readiness workflow, requires readiness promotion by default, recursively verifies the top-level manifest, and records it in `ArtifactRegistry` as the reusable local promotion baseline.
- `CachedVerifier`, `CachedRetriever`, and `CachedStateSource` provide request-scoped caching plus hit/miss stats for repeated verifier, retrieval, and state-source calls; `eval_verifier_ensemble.py` includes these `cache_stats` in each run report.
- `eval_truthfulqa.py --profile` now emits a structured performance summary with bottleneck phase, top phases, grouped time shares, and warmup/eval throughput fields for reproducible optimization comparisons.
- `ScoreDump` / `ScoreDumpIdentity` / `load_score_dump` / `load_score_dump_columns` / `load_score_dump_columns_with_extras` / `load_score_dump_statement_scores` / `load_score_dump_layer_scores` / `score_dump_file_metadata` / `score_dump_identity` / `score_dump_cache_summary` provide one validated post-hoc score-dump entry point for conformal calibration, abstention/comparison, score-ensemble, verifier-ensemble, fixture-builder, calibration-transfer, and sweep-calibration reports, including record counts, score names, sweep-layer metadata, file size, SHA-256 provenance, stable model/dataset/layer/score-schema/scoring-config identity, and run-local cache hit/miss observability so repeated analyses can verify exact input reuse without model reruns. `LayerScoreSweepCalibrator.calibrate_from_score_dump()` and `LayerScoreSweepCalibrator.calibrate_from_layer_scores()` consume validated or preloaded dumps directly and support bounded `max_workers` CPU parallelism for large layer/score post-processing sweeps, while `RankScoreFusionArtifact` / `RankScoreFusionCalibrator` make simple direction-aware rank fusion reusable as a deployable conformal artifact for follow-up experiments. Report builders share a run-local score-dump cache to avoid duplicate hashing of repeated score paths and duplicate scans of the same selected JSONL view, `ScoreDumpJsonlManifest` / `ScoreDumpRecord` / `iter_score_dump_jsonl_records()` provide an optional manifest-backed JSONL format for large post-hoc score artifacts, selected loaders materialize only requested primary, adaptive-extra, abstention/comparison, statement-bearing primary, or layer/score columns plus labels, cached selected views invalidate when the manifest or records file changes, JSONL metadata fingerprints both the manifest and records file and emits a canonical identity cache key, `score_dump_file_metadata()` builds JSONL summaries from manifest label counts when available and otherwise falls back to a cached label-only record scan, selected JSONL scans prime the run-local records hash cache for later full-provenance metadata, and `eval_conformal.py` reuses a preloaded JSONL layer/score view when writing sweep reports or best calibration artifacts. `eval_truthfulqa.py --dump-scores-format jsonl` writes that format directly with per-record extras in the records sidecar. `eval_conformal.py --artifact-manifest` now fingerprints the input score dump, structured conformal report, abstention report, abstention comparison report, sweep report, and saved calibration artifacts for registry/release verification.
- `run_calibrated_observability_workflow.py` now wraps score-dump creation or reuse plus conformal report, sweep report, best calibration artifact, nested manifest verification, top-level artifact manifest, optional registry record, and an `evidence_bundle` summary into one reproducible calibrated-observability closure; `--runtime-preset quick|calibrate|full` fixes common local smoke, dump-reuse, and real-run parameter profiles while preserving explicit CLI overrides. Its default sweep signals now include the ICR-inspired `resid_update_norm` residual-dynamics proxy.
- Abstention release-gate sidecars from `eval_conformal.py --save-abstention-release-gate` add a fail-closed promotion check on top of abstention report/comparison evidence, requiring both a minimum conservative conditional-correctness lower bound and a maximum empirical abstention rate before promoting a participation gate into product control.
- `run_truthfulqa_frontier_workflow.py` is the multi-model/multi-scale research orchestrator for Qwen/SmolLM2 l20/l80-style runs: it executes or reuses calibrated-observability cells, then produces a cross-cell `eval_score_ensemble.py` rank-fusion report, top-level artifact manifest, and optional registry record. It now accepts a per-cell `--cache-dir` root that forwards statement encoding, layer stats, warmup checkpoint, and eval-reps cache paths through the calibrated-observability workflow, so l80 and multi-seed studies can reuse expensive model-forward evidence instead of rebuilding it. The current registered l80 frontier artifact is `report:truthfulqa-frontier-qwen-smollm2-l80:0.1`: Qwen peaks at `truth_proj` layer `-10` with AUROC `0.764` and detection `0.242` at alpha `0.100`; SmolLM2 peaks at `truth_proj` layer `-16` with AUROC `0.782` and detection `0.265`. The paired cache-only replay is `report:truthfulqa-frontier-qwen-smollm2-l80-cache-only:0.1`: it reproduces the same score records and best sweep choices while reducing frontier wall-clock from `1140.245s` to `24.028s`, with Qwen cell replay `784.040s -> 3.496s` and SmolLM2 cell replay `336.829s -> 2.797s`. `eval_frontier_stability.py` adds the post-hoc multi-seed check without reloading models; the registered report `report:truthfulqa-frontier-qwen-smollm2-l80-stability:0.1` and verified manifest `benchmark_manifest:truthfulqa-frontier-qwen-smollm2-l80-stability:0.1` replay seeds `0..9`, fingerprint both score manifests and JSONL record sidecars, show `truth_proj` is best in 10/10 seeds for both Qwen l80 and SmolLM2 l80 while beating the best `mean_rank` ensemble in 10/10 seeds, and now report shared score-dump cache reuse with 18/22 JSONL selected-view lookups served from cache. Mean single-minus-ensemble detection margin is `0.034` for Qwen and `0.053` for SmolLM2. `eval_verifier_stability.py` applies the same post-hoc stability check to staged structured QA verifier routing on the current frontier score dumps; `report:truthfulqa-frontier-qwen-smollm2-l80-verifier-stability:0.1` keeps verified false alarm within budget in 10/10 seeds and improves detection in 10/10 seeds for both models with only 115 and 95 structured-QA calls per run. Its default run-local verifier trace cache avoids rerunning seed-invariant verified records after the first seed, with 9/10 trace-cache hits per model in the current report. The accompanying rank-fusion, internal-stability, and verifier-stability reports show naive internal score ensembles still trail single `truth_proj`, while staged external evidence is the current path that improves detection under the calibrated false-alarm budget.
- `compare_frontier_release_evidence.py` combines the l80 staged verifier-stability report with the l80 abstention-stability report into one fail-closed frontier release verdict. The current evidence promotes the verifier track but blocks the abstention track because participation-gate conservative conditional-correctness lower bounds remain below the `0.8` release threshold, so the overall frontier release-evidence decision is blocked until the participation signal improves.
- `eval_truthfulqa.py --auto-batch-size` can halve and retry warmup/forced-answer batch size after retriable memory errors, recording requested/effective batch size and reduction events in JSON/profile output.
- `eval_truthfulqa.py --max-batch-tokens` caps padded tokens per warmup/eval forward while preserving `--batch-size` as the row-count cap; triplet, matrix, and readiness workflows pass it through for same-machine performance gates. `run_cache_profile_matrix.py --max-batch-token-budgets` can compare several token budgets in one triplet matrix and ranks those cells by uncached forced-answer forward time.
- `eval_truthfulqa.py --covariance-mode {full,diag,low_rank}` controls the `maha_last` TruthManifold covariance approximation. `full` is the backward-compatible default, `diag` keeps only diagonal Welford scatter statistics for low-memory local sweeps, and `low_rank` scores with a ridge plus top-K approximation. Layer-stats caches validate covariance mode/rank, and calibrated thresholds should be regenerated after changing modes.
- `run_cache_profile_matrix.py --max-workers` and `run_adapter_readiness_workflow.py --performance-max-workers` can execute independent performance matrix cells concurrently. The default remains serial; shared-cache refresh cells are still run behind a conservative barrier before dependent warm-start/cache-only cells are parallelized. Matrix/readiness reports record end-to-end wall-clock seconds so worker-count comparisons are auditable.
- `run_cache_worker_sweep.py` wraps the cache-profile matrix across several worker counts, isolates shared-cache roots per worker count, and recommends the fastest promoted worker count by end-to-end wall-clock time.
- `eval_truthfulqa.py --prefix-kv-cache` is an experimental shared-prefix forced-answer path for eval scoring: repeated question prefixes are run once and answer continuations reuse the prefix KV cache while preserving answer-window hidden states and NLL semantics. It is off by default; use cache-profile matrices with `--prefix-kv-cache-modes off,on`, inspect `prefix_kv_comparisons` / `forced_answer_forward_seconds` for forward-speed claims, and promote only through profile/readiness gates.
- `eval_truthfulqa.py --statement-encoding-cache` persists tokenizer outputs and answer-span lengths as validated JSON so repeated benchmark/cache rebuilds avoid redundant tokenizer setup without changing scoring semantics.
- `AdaptiveScoreTransform` / `AdaptiveConformalCalibrator` adds a dependency-free adaptive conformal scoring layer for ACSE-style experiments: callers can inflate native diagnostics with semantic entropy, cluster, prompt-risk, or verifier-route features, then save the adjusted higher-is-anomalous score as a normal calibration artifact without changing score-dump schemas. `eval_conformal.py --adaptive-feature ... --save-adaptive-calibration ...` now wires this into score-dump workflows by loading adaptive features from selected primary scores, JSON extras, JSONL manifest extras, or JSONL per-record extras and emitting an `adaptive_conformal_report` next to the base conformal report.
- Sharded `--eval-reps-cache` readers reuse recently touched shards through a default 2-shard read-side LRU cache and use a manifest index to seek directly to the first touched shard for range reads, reducing repeated `torch.load` calls and avoidable manifest scans during small-batch cache-only or cached scoring runs; `--eval-reps-shard-read-cache-size 1` restores the previous single-shard memory profile, and triplet/matrix/worker-sweep/performance-baseline profile runners pass this setting through for cached/cache-only runs. `run_cache_profile_matrix.py --eval-reps-shard-read-cache-sizes 1,2,4` promotes read-cache capacity into a gated matrix dimension with shared eval-reps cache artifacts and cell-level runtime recommendations. JSON output includes `cache_stats.eval_reps_reader` counters for read requests, records read, shard read requests, cross-shard reads, shard loads, shard cache hits, and shard manifest scans when an eval-reps reader is used, and profile summary adds `cache_efficiency.eval_reps_reader` hit-rate/read-shape/manifest-scan metrics.
- Current small CPU SmolLM2 l8 performance evidence registers `performance_baseline:smollm2-l8-read-cache-worker-sweep-score-fusion-performance-baseline:0.2`: read-cache size `2` remains the promoted cache-only setting, worker count `2` lowers same-machine matrix wall-clock from `184.467s` to `141.385s`, the selected cell keeps `truth_proj` AUROC `0.830`, and the runtime recommendation carries conformal-gated `score_fusion_mean_rank` evidence from the l80 score-ensemble report (`AUROC=0.679`, false alarm `0.090`, detection `0.196`). The same evidence promotes `benchmark_manifest:smollm2-l8-read-cache-worker-sweep-score-fusion-staged-qa-release-candidate:0.2` through the staged structured-QA release gate, and the release manifest/registry metadata now expose score-fusion status, signal, AUROC, conformal-gate result, and source score-ensemble report. Treat this as local CPU deployment evidence, not a cross-hardware default.
- `compare_profiles.py` compares profile JSON payloads across baseline/cache/cache-only runs, reporting speedup, phase deltas, grouped time deltas, throughput ratios, and cache-efficiency deltas such as eval-reps shard hit rate / cross-shard read rate without loading models. Optional regression gates can fail CI/local checks when total time, phase time, or throughput ratios exceed configured limits, while cache-efficiency fields remain explanatory report metadata and are propagated into cache-profile matrix cell summaries.
- `run_cache_profile_triplet.py` is the optional local performance harness: it runs uncached, cached, and cache-only `eval_truthfulqa.py` profiles on the same machine and writes a gated `compare_profiles.py` report. The default path uses the offline fixture, while `--real-truthfulqa` generates representative model/data profile artifacts with configurable model, dtype, layer, limit, and warmup size.
- `run_inside_sampling_profile.py` compares fixed INSIDE sampling, adaptive entropy-stability sampling, and adaptive self-check-bound sampling on the same machine, writing per-run result/profile JSON plus a sample-efficiency comparison report so generated-sample savings and `inside_generation` time ratios can be promoted as reproducible artifacts. Optional shared statement/layer/eval caches let comparable sampling runs avoid repeated tokenization, warmup, and forced-answer forward work while still running real INSIDE generation for sampled records.
- `run_inside_trigger_budget_sweep.py` wraps that profile runner across several `--inside-trigger-*` budgets, writing one sweep report with generated samples, `inside_generation`, ratios to an optional full-sample reference, inside-score AUROCs, a cost-first recommendation, and a quality-balanced recommendation that chooses the cheapest budget within a small AUROC tolerance of the best INSIDE quality signal. `--shared-cache-dir` applies the same cache reuse across all budget children. For nested top-fraction budgets with one run, `--derive-from-max-budget` executes only the largest budget and derives smaller rows from the score dump while preserving batch-local top-fraction semantics.
- `run_cache_profile_matrix.py` expands the triplet harness across layer, batch-size, hidden-state-capture, and TruthManifold covariance-mode combinations, writing a single same-machine matrix report with command logs, gate summaries, cache-only timing, AUROC quality signals, wall-clock execution metadata, and a fail-closed matrix-level performance promotion decision. Shared-cache runs reuse statement encodings and eval hidden-state caches across covariance modes while keeping layer-stats caches separated by covariance mode/rank. Covariance sweeps should use the default `triplet` matrix mode; `rescore` is reserved for cache-only variants that share the same layer-stats scoring semantics. In `rescore` mode, later cache-only cells in the same shared-cache group are gated against the first cell's uncached baseline so post-processing/cache-only variants can be promoted without repeating model forward passes.
- Current small SmolLM2 l8 covariance evidence (`layer=-12`, `batch_size=4`, `max_batch_tokens=512`, `limit=8`, `manifold_questions=4`) shows a real tradeoff: `diag` is fastest for cache-only replay (`0.156s` versus `0.211s` for `full`) but lowers `maha_last` AUROC from `0.614` to `0.500`; `low_rank` rank 8 keeps `maha_last` close to `full` (`0.610`) with cache-only replay `0.197s`. Runtime recommendations now emit `covariance_tradeoff`, and readiness/release gates can enforce `--max-covariance-maha-last-auroc-drop` so speed-only covariance choices do not silently replace `maha_last`-quality-preserving choices.
- `profile_gate_smoke.py`, `cache_profile_smoke.py`, `score_fusion_profile_smoke.py`, `inside_sampling_profile_smoke.py`, `cache_worker_sweep_smoke.py`, `registry_baseline_smoke.py`, `performance_baseline_smoke.py`, `product_trace_replay_smoke.py`, `release_candidate_registry_smoke.py`, and `make perf-check` provide deterministic no-model smoke checks for profile regression, cache/cache-only gate machinery, score-fusion runtime caching, INSIDE sampling sample-efficiency gates, worker-count sweep decisions, registry-backed baselines, performance-baseline handoff bundles, ProductTrace replay handoff plus bounded-telemetry rejection, and release-candidate registry promote/block behavior; real performance claims still require representative `eval_truthfulqa.py --profile-json` artifacts.
- `recommend_runtime_config.py` turns promoted cache-profile matrix, optional worker-sweep reports, optional INSIDE sampling profile reports, optional trigger-budget sweep reports, and optional score-ensemble reports into one deployable runtime recommendation with layer, batch size, capture mode, covariance mode/rank, token budget, prefix-KV mode, worker count, sampling configuration, selected trigger budget, gated best AUROC quality signal, covariance tradeoff details, cache-tuning advice, and equivalent benchmark flags. It emits cache-tuning recommendations when eval-reps shard hit rate is low, cross-shard reads are high, or each cache read returns too few records; when `eval_reps_shard_read_cache_sizes` has already compared several capacities, it records the swept sizes and selected read-cache size instead of emitting contradictory read-cache heuristic advice. Score-fusion evidence only enters `quality_signals` when the selected ensemble alpha passed its conformal false-alarm gate; blocked or ambiguous fusion evidence is retained for audit without changing the runtime recommendation. It supports explicit trigger-budget selection policies: `quality_balanced` preserves the default release posture, `cost_first` minimizes sampled INSIDE work for latency-constrained deployments, and `quality_first` chooses the highest measured INSIDE quality metric. It preserves `--derive-from-max-budget` in generated sweep flags, performs no model work, and should be treated as the final handoff from same-machine performance evidence to local deployment defaults.
- `run_performance_baseline_workflow.py` wraps the runtime-recommendation handoff into a fingerprinted, registry-ready performance baseline bundle. It can run or reuse cache-profile matrix evidence, optional worker-count sweeps, optional INSIDE sampling / trigger-budget reports, and optional score-ensemble reports, then writes `performance-baseline-workflow.json`, `runtime-recommendation.json`, an artifact manifest, a top-level `performance_evidence_bundle` summary for recommendation cost ratios / evidence status / artifact readiness / score-dump cache evidence / score-fusion status, and an optional `performance_baseline:*:*` registry record. When reusing a matrix report, the top-level config, performance evidence runtime block, and manifest metadata inherit the matrix report's effective runtime settings for reproducibility. With `--verify-manifest`, it writes a separate recursive manifest verification report and records `manifest_verification:<name>-verification:<version>` next to the performance baseline. Dry-run outputs remain `needs_evidence` so command inspection cannot be mistaken for promoted performance evidence.
- `run_product_runtime_baseline.py` aggregates saved `ProductTrace` JSON payloads into a request-runtime baseline, including phase timing distributions, verifier-route cost summaries, retrieval-use rates, cache hit rates, staged-verification saved/verified claim counts, verification-plan coverage/route/tool-payload counts, optional `ProductRuntimeBudgetPolicy` / `ProductPromotionContract` gate results, an artifact manifest, and an optional `product_runtime_baseline:*:*` registry record. It supports `--save-recommended-policy` to materialize the optimization hint as a directly reusable `ProductRuntimeBudgetPolicy` artifact, `--trace-records-jsonl` to stream per-trace metric/budget records into a sidecar instead of embedding them in the main report, `--trace-records-cache-json` to reuse compact metric/budget records across repeated budget sweeps when source trace fingerprints and the resolved policy payload match, supports `--compact-json` for minified automation artifacts, performs no model or verifier work, and is intended to validate the product control path separately from model/cache performance recommendations.
- `run_product_feedback_report.py` joins saved full `ProductTrace` payloads with `ProductFeedbackRecord` JSONL labels, preferring trace fingerprint matches and falling back to unique request ids. It reports matched/unmatched feedback, accepted-but-wrong, retrieved-failure, retrieved-but-still-unsupported, and abstain-false-positive metrics with optional fail-closed product gates, manifest provenance, and registry metadata. It performs no model, verifier, retrieval, or database work.
- `recommend_control_policy_from_feedback.py` consumes one or more product feedback reports and emits a deterministic candidate `ControlPolicyConfig` plus optional runtime control-default artifact. Accepted-but-wrong feedback increases sensitive-claim verification, retrieval failures can move unsupported claims toward clarification or abstention, and abstain false positives can de-escalate compound unsupported decisions only when safety feedback is not also elevated.
- `audit_feedback_policy_replay.py` audits that candidate policy against historical feedback labels without pretending to rerun the full controller. It reports counterfactual safety coverage, residual safety issues, unknown claim-metadata gaps, overblock relief, manifest provenance, and registry metadata so feedback-driven policy changes can be gated before rollout.
- `run_feedback_policy_workflow.py` chains the same three feedback-policy steps into one reproducible artifact bundle: feedback join report, candidate policy/default artifacts, inline candidate `ControlPolicyConfig` and control-default config, replay audit, top-level manifest, and optional local registry record. Its top-level status is `recommend`, `observed`, `needs_evidence`, or `blocked`; registered `report:<name>:<version>` records can be consumed by release-candidate gates; it performs no model, verifier, retrieval, database, or network work.
- `ProductTrace.to_bounded_dict()` and `calibrated_control_demo.py --bounded-trace` emit smaller online telemetry payloads by keeping diagnostics, routing decisions, summaries, selected artifact/contract metadata, and truncation counts while bounding long claim, verifier, event, and action-result lists. Bounded trace generation reuses one prepared normalized trace payload for summaries and bounded fields, avoiding repeated verifier/action/event normalization on large traces. Bounded traces are for product telemetry; corpus, replay, and runtime-baseline tools reject them by default, so full `ProductTrace.to_dict()` payloads remain the reproduction and runtime-baseline input.
- `compare_product_runtime_baselines.py` compares a fresh ProductTrace runtime baseline against a file or registered `product_runtime_baseline:*:*` record, writing a manifest-backed drift report with fail-closed gates for request latency, route duration, attempted-route count, retrieval-use drift, cache-hit-rate drop, verifier-skip-rate drop, and minimum current trace count. It can also consume a saved `ProductRuntimeBudgetPolicy` by file path or `product_runtime_budget_policy:*:*` registry key and apply it to the current aggregate baseline summary using p95/aggregate metrics, so recommended runtime policies can become the next run's promotion gate. It records optional `product_runtime_drift_report:*:*` registry metadata and gives the product loop a local continuous-assurance check before selector or release-candidate defaults are trusted on shifted traffic.
- `build_product_trace_corpus.py` turns raw saved ProductTrace JSON/JSONL into replay-ready corpus artifacts: it validates risk decisions and optional runtime traces, redacts text-like claim/evidence fields by default, adds stable `metadata.runtime_replay_key` values, writes standardized trace files, emits a manifest-backed `runtime-pair-index.json` for paired-runtime replay, can reuse per-source validation/redaction results with `--source-cache-json`, and can register the corpus. The current registered SmolLM2 corpus is `report:smollm2-product-trace-corpus:0.1`, with 12 accepted deterministic profile-sweep traces, no rejects, full runtime coverage, and all claim/evidence text redacted before replay.
- `run_product_trace_replay_workflow.py` wraps the raw-trace handoff into one reproducible workflow: build a redacted trace corpus plus runtime-pair index, run a ProductTrace runtime baseline, run selector replay over the standardized traces using that index, optionally run the current runtime baseline through `compare_product_runtime_baselines.py` as a product-runtime drift/policy gate, recursively manifest all child reports, record phase timing/cache summaries for local performance tuning, lift the runtime baseline `optimization` status/recommendations/policy hints into the top-level report and registry metadata, optionally materialize the baseline's recommended runtime budget policy, and register a single workflow report. With `--verify-manifest`, it writes a separate recursive verification report and records `manifest_verification:<name>-verification:<version>` beside the workflow report; with `--fingerprint-cache`, repeated local manifest checks reuse unchanged artifact fingerprints across runs; with `--corpus-source-cache-json`, repeated raw-trace handoffs reuse unchanged source validation/redaction results before rebuilding corpus outputs; with `--corpus-cache-json`, repeated raw-trace handoffs reuse the already standardized corpus when source fingerprints, output fingerprints, and corpus config still match; with `--runtime-trace-records-cache-json`, repeated runtime budget sweeps reuse compact trace-record metrics; with `--save-runtime-recommended-policy`, the workflow saves a reusable `ProductRuntimeBudgetPolicy` candidate from the observed baseline; with `--runtime-drift-baseline` and optional `--runtime-drift-budget-policy`, the workflow writes and registers a child `product_runtime_drift_report:*:*` gate before release-candidate evaluation; with `--selector-trace-inputs-json`, repeated selector replay sweeps reuse a minimal trace-input cache keyed by source trace fingerprints. The current registered workflow is `report:smollm2-product-trace-replay-workflow:0.1`; it promotes the default selector from 12 redacted traces with full paired-runtime coverage while keeping the runtime baseline as observed evidence unless an explicit runtime budget or promotion contract is supplied, and its verification record is `manifest_verification:smollm2-product-trace-replay-workflow-verification:0.1`.
- `run_product_runtime_profile_sweep.py` runs deterministic calibrated-control demo scenarios across `latency`, `balanced`, `audit`, and request-level `auto` selection modes, writes trace artifacts, builds one ProductTrace runtime baseline per mode, records actual selected profiles for `auto`, ranks non-blocked modes by request runtime and route cost, and can register the sweep report. It supports `--max-workers` mode-level concurrency while keeping trace order deterministic inside each mode; use the default single-worker mode for promotion timing evidence and multi-worker mode for faster smoke/coverage scans. It also supports `--compact-json` across generated traces, baselines, reports, and manifests without changing payload schemas. `--runtime-profile-selector-policy` configures request-time auto routing, `--policy` keeps applying trace-level `ProductRuntimeBudgetPolicy` gates through each per-mode baseline, and `--slo-policy` adds aggregate profile SLO gates for p95 request latency, attempted-route count, retrieval-use rate, verifier skip/claim cost, and auto selector distribution. It is the lightweight local path for deciding which staged-verification mode should become the default product posture before collecting real traffic traces. Skipped verifier stages with no selected route now count as zero route cost for route-budget checks, while missing runtime traces still fail closed when runtime phase/total thresholds are configured. The current registered sweep is `report:smollm2-product-runtime-profile-sweep:0.1`, which promotes the three static profiles plus `auto` against the strict product runtime budget and the local profile SLO policy, then recommends `auto` on the deterministic scenario set.
- `run_runtime_profile_selector_tuning.py` compares multiple `RuntimeProfileSelectorPolicy` candidates by materializing each policy, running `run_product_runtime_profile_sweep.py --profiles auto` under the same SLO gate, ranking non-blocked candidates by request runtime and verifier cost, writing a top-level artifact manifest, and optionally registering the tuning report. The current registered selector tuner is `report:smollm2-runtime-profile-selector-tuning:0.1`; it promotes the default selector and blocks latency-biased and audit-biased alternatives because they fail the auto selector distribution SLO.
- `run_runtime_profile_selector_replay.py` replays multiple `RuntimeProfileSelectorPolicy` candidates over saved `ProductTrace` JSON without rerunning demos, verifiers, retrieval, or model work. It uses each trace's `risk_decision` and claim metadata to estimate selected-profile distribution, switches from the original runtime profile, configurable profile-cost units, paired observed runtime when the same logical request is available under multiple profiles, selected-vs-original runtime deltas, optional `--runtime-pair-index` reuse from a trace corpus, optional `--trace-inputs-json` reuse of minimal trace replay inputs, optional replay gates, an artifact manifest, and an optional `report:*:*` registry record. This is the cheaper path for validating selector changes against captured product traces once demo sweeps have established the baseline policy shape.
- `compare_readiness_baselines.py` compares registered adapter-readiness manifests without rerunning model work: it recursively verifies each manifest, regenerates runtime recommendations from saved performance matrices and optional INSIDE sampling profile / trigger-budget sweep artifacts when possible, applies optional quality/runtime/covariance-quality/sampling-cost gates, uses uncached total time as a conservative forward-cost fallback for legacy reports, and recommends the passing baseline with the best AUROC quality signal. Sampling gates use profile `*_to_baseline` ratios when present and trigger-sweep `*_to_reference` ratios when baseline ratios are absent; reports record the ratio source. Trigger-budget sweep selection can be explicitly overridden with `quality_balanced`, `cost_first`, or `quality_first`; otherwise it follows the recorded readiness/runtime policy and falls back to the quality-balanced release posture.
- `compare_release_candidates.py` combines registered readiness, verifier-route, optional performance-baseline records, optional product-trace-replay workflow reports or `report:*:*` registry keys, optional feedback-policy workflow reports or `report:*:*` registry keys, optional selector-replay reports, optional product-runtime-drift reports, and optional adapter-family matrix evidence into the final local release gate: both underlying baseline comparisons must promote, including any readiness covariance-quality gates, sampling-cost gates, and route runtime/cache budget gates; a supplied `performance_baseline:*:*` key must verify, match the selected runtime recommendation, require `performance_evidence_bundle.release_ready=true` when that bundle exists, and can optionally gate covariance `maha_last` quality drop, score-dump cache evidence / selected JSONL view hit rate, plus performance trend against an explicit prior `performance_baseline:*:*` key; supplied product-trace-replay workflow, selector replay, and product runtime drift reports must verify and promote; supplied feedback-policy workflow reports must verify, have `workflow=feedback_policy_workflow`, have `status=recommend` or `status=observed`, carry a valid promotion decision, include a parseable `candidate_control_policy_config` when recommending a candidate policy, and can be gated on matched feedback count, safety coverage, and unknown safety issue rate; and a supplied adapter-family matrix must promote with all required routes present and promoted. When `--product-trace-replay-workflow-key` or `--product-trace-replay-workflow` is supplied, the release gate resolves its selector-replay and runtime-drift child report paths unless explicit child paths override them. The report emits one release candidate with runtime flags, verifier route, quality evidence, runtime cost, route cost, selected trigger-budget policy, trigger-budget metadata, ratio-source fields, performance-baseline provenance, performance evidence bundle cost/readiness/cache/trend metadata, covariance tradeoff gate metadata, product-trace-replay provenance, feedback-policy candidate config/control-default provenance and safety metrics, selector replay runtime-delta evidence, product runtime drift evidence, and adapter-family coverage metadata. Optional `latency`, `balanced`, and `audit` runtime profiles fill unset performance/cost gate defaults while leaving explicit quality thresholds such as AUROC to the caller.
- `run_release_candidate_registry_workflow.py` registers a final local release candidate by writing the release-candidate report, building a manifest over that report plus the selected readiness, route, optional performance manifests, optional product-trace-replay manifests, optional feedback-policy manifests, optional selector-replay manifests, optional product-runtime-drift manifests, and optional adapter-family matrix report, recursively verifying the whole chain, and recording the verified candidate in `ArtifactRegistry` with recommended runtime, selected runtime profile, selected trigger-budget policy, covariance quality-gate metadata, INSIDE sampling / trigger-budget cost evidence when available, route quality, tail latency, attempted-route, retrieval-use metadata, performance-baseline record metadata, performance evidence bundle readiness/cost/cache/trend/score-fusion metadata, product-trace-replay metadata, feedback-policy workflow status/report/record/candidate-policy/safety metrics, selector replay runtime-delta metadata, product runtime drift metadata, and required adapter-family route metadata. `--fingerprint-cache` persists the recursive manifest fingerprint cache across repeated local release checks so unchanged source artifacts are not re-hashed, `--manifest-fingerprint-workers` enables bounded parallel artifact fingerprinting for large local manifests, `artifact_cache.artifact_fingerprint_cache` reports requests, hits, misses, entries, and hit rate, and the workflow report records compare / comparison-write / manifest-build / promotion / workflow-report-write phase timings for release-gate overhead analysis.
- `run_manifest_fingerprint_worker_sweep.py` replays saved artifact-manifest verification over one or more manifests under multiple bounded worker counts, using the same optional seed fingerprint cache for each sample, then records timing/cache summaries and recommends the fastest passing worker count for future `--manifest-fingerprint-workers` release checks. This gives local teams an empirical worker setting without rerunning model work, verifier routes, or release-candidate comparison.
- `verify_artifact_manifest.py --max-workers` exposes the same bounded parallel artifact fingerprinting for standalone manifest checks; use it with `--fingerprint-cache` when large local artifacts are repeatedly verified outside the release-candidate workflow.
- Directory artifact fingerprinting now reuses the cache-signature file-list scan for full content hashing on cache misses, preserving deterministic directory digests while avoiding duplicate directory enumeration in release and manifest verification gates.
- `run_release_gate_overhead_baseline.py` reads one or more saved release-candidate registry workflow reports and writes a dependency-free overhead baseline over total time, phase-total time, per-phase timing, artifact fingerprint cache reuse, artifact JSON cache reuse, and the slowest observed phase. It can gate maximum total/phase time, minimum weighted fingerprint-cache hit rate, minimum last-run fingerprint-cache hit rate for warm-cache validation, and minimum report count, then optionally records the result as a `report:*:*` registry artifact.
- `export_product_promotion_contract.py` converts a promoted release-candidate report or registry-workflow payload into a compact, manifest-backed `ProductPromotionContract` artifact and can record it as `product_promotion_contract:*:*` in the local release registry.
- `ProductPromotionContract` consumes a promoted release-candidate report, registry-workflow payload, or exported contract artifact and produces a dependency-free product handoff containing runtime defaults, verifier-route metadata, covariance quality-gate provenance, performance evidence bundle status/cost/cache metadata, product-trace-replay workflow provenance, feedback-policy workflow provenance and candidate policy/default paths, validated feedback-derived `ControlPolicyConfig`, selector replay runtime-delta evidence, product runtime drift evidence, and a `ProductRuntimeBudgetPolicy`; `load_product_promotion_contract()` resolves explicit or ordered default compact contract paths once, while `ProductRuntimeEvidenceBundle` / `load_product_runtime_evidence_bundle()` lazily attach optional artifact-manifest verification and local registry provenance for runtime trace metadata. `calibrated_control_demo.py --promotion-contract` can pass that feedback-derived policy into `RiskController`, use the artifact directly, and still allow explicit CLI budget overrides.
- Local release-candidate artifacts: `benchmark_manifest:tiny-local-staged-qa-release-candidate:0.4` combines the tiny-gpt2 offline readiness/runtime baseline with the TruthfulQA l80 staged structured QA route baseline, and `benchmark_manifest:tiny-local-inside-staged-qa-release-candidate:0.5` adds a registered INSIDE sampling profile gate. `benchmark_manifest:smollm2-l20-inside-staged-qa-release-candidate:0.6` is the first non-tiny local release candidate: SmolLM2-135M l20 readiness promotes `truth_proj` AUROC `0.682`, uncached forced-answer cost `38.786s`, cache-only replay cost `0.339s`, and the same staged structured QA route baseline. Its full-sample INSIDE profile selects `adaptive_selfcheck` but only reduces generated samples to `0.937` of fixed while `inside_generation` remains `1.001` of fixed. `benchmark_manifest:smollm2-l20-inside-triggered-staged-qa-release-candidate:0.7` promotes the same readiness/route chain with `truth_proj` top-25% triggered INSIDE: 39/154 statements sampled, 115 skipped, fixed `inside_generation` reduced from `467.563s` to `118.513s` (`0.253x`), and the promoted `adaptive_selfcheck` triggered run using 110 generated samples. `benchmark_manifest:smollm2-l20-inside-trigger-budget-derived-staged-qa-release-candidate:0.8` reuses the same readiness/route evidence, folds in the derived trigger-budget sweep, and selects the quality-balanced top-40% triggered `adaptive_selfcheck` profile with sample-count ratio `0.472`, `inside_generation` ratio `0.503`, semantic-entropy AUROC `0.570`, and 218 generated samples from a single largest-budget source run. `benchmark_manifest:smollm2-l20-inside-trigger-budget-derived-staged-qa-release-candidate:0.9` adds `performance_baseline:smollm2-l20-performance-baseline:0.9` as a required verified gate and proves the performance handoff matches the selected runtime. `benchmark_manifest:smollm2-l20-inside-trigger-budget-derived-adapter-gated-staged-qa-release-candidate:1.0` adds required structured-state and state-transition adapter-family evidence. `benchmark_manifest:smollm2-l20-inside-trigger-budget-derived-retrieval-adapter-gated-staged-qa-release-candidate:1.1` additionally requires the promoted adapter-family matrix with `structured_state`, `state_transition`, and `retrieval_groundedness` routes present and promoted. `benchmark_manifest:smollm2-l20-inside-trigger-budget-derived-structured-retrieval-audit-staged-qa-release-candidate:1.2` adds `benchmark_manifest:smollm2-l80-retrieval-structured-qa-route:0.5` as a required retrieval-structured-QA audit baseline. The current registered SmolLM2 default is `benchmark_manifest:smollm2-l20-inside-trigger-budget-derived-strict-structured-retrieval-audit-staged-qa-release-candidate:1.5`, which keeps the same runtime evidence, selected staged `structured_qa` product route, performance baseline, adapter-family matrix with `retrieval_structured_qa` coverage, and required retrieval audit, then explicitly gates the product route at `max_retrieval_use_rate=0.0` and `max_mean_attempted_route_count=1.1`. Version 1.5 also requires promoted selector replay and product runtime drift evidence from `report:smollm2-product-trace-replay-workflow:0.1`, with 9 compared drift metrics and 0 blocked metrics. Its compact deployable handoff is `product_promotion_contract:smollm2-product-promotion-contract:1.5` at `artifacts/smollm2_product_promotion_contract_v1_5/product-promotion-contract.json`. The required audit route promotes with selected `238`, decision accuracy `0.992`, false-supported rate `0.000`, false-refuted rate `1.000`, verified detection `1.000`, verified false alarm `0.009`, runtime about `1.05s`, and `410` retrieval hits under a `450` hit budget. The selected runtime remains layer `-12`, batch size `8`, `outputs` capture, worker count `1`, `truth_proj` AUROC `0.682`, and the quality-balanced `top_0p4` triggered `adaptive_selfcheck` budget. The same verified sweep can still drive a cost-first release-gate override that selects the top-10% trigger budget for latency-constrained deployments. This makes performance-gated, product-trace-replay-checked, selector-replay-checked, runtime-drift-gated, strict structured-retrieval-audit, trigger-derived INSIDE the current real-model default direction.

### Next Verification Adapter Work

- Connect `StructuredStateVerifier` to additional live database, business-rule, and domain-state sources behind optional adapters; the current reproducible path uses local JSON state sources, stdlib SQLite, and mapped local tool outputs.
- Connect additional QA/database/world-model route policies to production adapters while preserving trace-visible match reasons for each selected or skipped tool.
- Generalize real side-effecting executors behind the same trace, execution-policy, timeout, idempotency-ledger, and tool-output mapping interfaces; the current production-like demo proves the path with a guarded local SQLite mutation and optional JSON/SQLite replay, while hard cancellation remains adapter-specific.
- Extend route-level metrics to future adapters and real fixtures, then compare them with `compare_verifier_routes.py` so new tools are judged by route hit rate, false support, false refutation, runtime cost, tail latency, cache efficiency, downstream conformal control impact, Pareto frontier status, route-specific promotion decision, and explicit gates before adapter work proceeds.
- Replace label-derived oracle evidence with real retrieval/database/calculator evidence and rerun verifier/retrieval ensemble reports on Qwen/SmolLM2.
- Use local corpus fixtures from `build_evidence_fixture.py` as the reproducible baseline before adding networked retrieval extras.
- Add optional retrieval/database/calculator verifier adapters behind extras, keeping core dependencies unchanged.
- Add concrete domain/world-model adapters beyond the in-memory test double.
- Add semantic-entropy sampling probes, RAG groundedness adapters, and optional SAE/ReFT integrations behind extras.
- Add benchmark/demo domain examples where facts depend on state transitions, physical constraints, or business rules.
- Compare real verifier/retrieval ensembles against the current `truth_proj` baseline under the same conformal false-alarm budget.

### Later Control-Plane Hardening

- Persist full trace/report/action-result artifacts from benchmark and demo commands into local registries by default when requested.
- Add policy graph composition only after current dataclass-based policy config becomes insufficient.
- Add steering-lambda sweep utilities after monitor-only diagnostics are stable on larger replicated runs.
- Extend registry persistence from metadata records to saved subspace/manifold tensors.

## Product Tagline

EigenTruth is a factuality control plane for LLM systems: it observes hidden-state drift, calibrates risk, routes uncertain generations through tools, and leaves creativity where creativity belongs.

EigenTruth 是 LLM 系统的事实性控制平面：观测表征漂移，校准风险，把不确定生成路由到工具验证，同时保留模型该有的创造力。
