# EigenTruth Product And Development Charter

EigenTruth is a representation-observability and factuality-control toolkit for LLM systems. It is not a standalone truth oracle, a safety boundary, or a replacement for external evidence. The product direction is to combine internal model diagnostics, calibrated risk scoring, tool-based verification, and optional world-model correction while preserving the language model's creative strengths.

EigenTruth 的产品定位是：面向 LLM 系统的表征观测与事实性控制工具集。它不是单一的“反幻觉模型”，也不是生产安全边界。产品方向是把模型内部诊断、风险校准、工具验证和可选世界模型校正组合起来，在保留 LLM 创造性的同时降低事实错误。

## Current State

The current packaged baseline is the `0.2.0` research release. The release
writeup in [`docs/release-0.2.0.md`](release-0.2.0.md) summarizes the evidence,
negative results, validation commands, and known non-claims for this baseline.

Implemented today:

- `TruthManifold` / `RepresentationManifold`: online hidden-state mean/covariance, ridge-regularized precision, save/load support, dependency-free covariance-spectrum diagnostics with Marchenko-Pastur bulk edges, spike count, effective rank, participation ratio, stable rank, and condition number, plus closed-form Gaussian 2-Wasserstein/Bures manifold distance for comparing layers, checkpoints, or runs.
- `TruthProbe` / `RepresentationProbe`: PyTorch forward-hook monitor for selected Transformer layers, Mahalanobis-style drift, optional HSE tracking, and optional activation steering.
- `EigenTruthWrapper` / `RepresentationMonitor`: warmup, generation passthrough, diagnostics, and probe lifecycle management for Hugging Face-style PyTorch models.
- `ConceptArtifact` / `MultiConceptMonitor`: versioned concept manifold artifacts plus multi-probe attachment for monitoring several concept/layer pairs at once without changing the default single-probe wrapper.
- `eigentruth.eval`: conformal p-values/thresholds, sequential alpha-spending conformal monitors for repeated requests or batch/session audits, TwoNN intrinsic-dimension profiles, first-token top-k entropy/confidence primitives, plus CPU-testable metrics such as AUROC, Spearman correlation, Euclidean dispersion, selective accuracy, coverage, confidence intervals, and high-confidence error audits.
- `eigentruth.calibration` / `eigentruth.eval.conformal`: JSON-serializable calibration artifacts, split-conformal calibrators, adaptive feature-inflated conformal calibrators, sequential conformal alpha-spending artifacts for session/batch replay, layer/score sweep reports, rank-calibrated multi-score fusion artifacts, geometry-by-uncertainty and GLU-style global-local uncertainty fusion artifacts, evidence-driven signal-selection reports, conformal abstention/comparison reports with conservative selective-correctness lower bounds, and abstention release gates for promotion checks.
- `eigentruth.eval.hidden_evidence`: HIVE-style, dependency-free hidden-evidence selection from primary and layer-sweep score dumps. Reports rank-normalize by source/layer/signal channel, preserve directionality and statement metadata, and apply budget caps before verifier or world-model conditioning experiments. `ClaimVerificationPlanner` can consume those reports to prioritize matched claims, and `ProductTrace` exposes compact hidden-evidence summaries.
- `eigentruth.core.TruthSubspace`: low-rank factual subspace scoring, benchmark residual signal, and optional true-minus-false projection.
- `eigentruth.core.AttentionSoftTargetProbeArtifact` / `soft_error_rate_targets`: torch-only pre-generation hidden-state attention probe artifacts trained from empirical sampled-answer error-rate soft targets.
- `eigentruth.core.ClaimFactualityProbeArtifact` / `ClaimFactualityScore` / `pool_claim_hidden_states`: torch-only claim-level hidden-state factuality-risk probe artifacts with token-span pooling, per-claim JSON scores, diagnostics, and save/load support. `eval_truthfulqa.py --dump-claim-factuality-probe-records`, `eval_claim_factuality_probe.py`, `run_claim_factuality_probe_workflow.py`, and `compare_claim_factuality_probe_workflows.py` now provide local record export/reuse, layer sweep, conformal calibration, artifact save/load, cheap text-redline comparison, manifest writing, optional local registry handoff, and multi-run promotion gating. This is a long-form/claim-level probe experiment chain, not release evidence until multi-run gates pass.
- `eigentruth.core.TrajectoryMonitor`: dependency-free generation-trajectory convergence diagnostics over per-token hidden states, including step-distance decay, Koopman-style rate, path efficiency, and convergence scores for quality/confidence correlation checks.
- `eigentruth.core.ResidualContributionProfile`: dependency-free ICR-inspired cross-layer residual-update profile summaries, exposed in TruthfulQA score dumps as `resid_update_profile_area`, `resid_update_profile_peak`, `resid_update_profile_late_mass`, and `resid_update_profile_concentration` for conformal calibration and fusion experiments without an extra model pass.
- `eigentruth.core.PromptAnswerPathwayMetrics`: dependency-free prompt/question-anchored vs answer-anchored hidden-state pathway summaries, exposed in TruthfulQA score dumps as `prompt_answer_distance`, `prompt_answer_cosine_gap`, `answer_anchor_distance`, `answer_path_length`, and `pathway_disagreement` for calibrated pathway-signal experiments.
- `eigentruth.core.AttentionPathwayMetrics`: optional attention-flow pathway readouts over returned model attention tensors, exposed behind `eval_truthfulqa.py --attention-pathway` as `attn_prompt_flow_loss`, `attn_answer_self_flow`, `attn_pathway_gap`, and `attn_pathway_concentration`; unsupported attention backends fail closed.
- `eigentruth.intervention.activation`: temporary model-side activation intervention hooks for forced-answer reruns, including prompt/answer/all/last/first-answer spans, zero/scale/mean operations, and source-token hidden-state patching; `eval_truthfulqa.py --activation-intervention-layer ...` records ablation intervention configs in score dumps, while `--activation-patch-layer ...` records deterministic source-token patch reruns.
- `eigentruth.intervention.pathways`: dependency-free pathway-intervention analysis helpers, including prompt/answer attention-pathway knockout over captured attention tensors and direction-aware intervention-effect reports for model-rerun score deltas. These are mechanism-experiment scaffolds, not default runtime interventions.
- `benchmarks/eval_pathway_intervention.py`: model-free comparison of row-aligned baseline and mechanism-intervention score dumps, producing direction-aware rerun evidence reports, optional artifact manifests, and optional local registry records before any pathway-causality claim is promoted.
- `benchmarks/run_pathway_intervention_workflow.py`: one-command mechanism-evidence wrapper for baseline, activation-ablation, and source-token patch TruthfulQA reruns, followed by both direction-aware intervention comparisons, a top-level evidence-bundle manifest, and optional local registry recording.
- `eigentruth.core.internal_eigenscore` / `lexical_semantic_entropy` / `embedding_semantic_entropy` / `semantic_energy_score` / `lexical_semantic_energy`: INSIDE/EigenScore-style spectral diversity, dependency-free sampled semantic-entropy proxies, and confidence-weighted semantic-energy disagreement, including adaptive sampling budget support in benchmarks.
- `eigentruth.training`: dependency-free training-side representation telemetry, including per-layer mean norm, variance trace, spectrum rank diagnostics, Gaussian 2-Wasserstein/Bures distance to an initialization baseline, and an optional HF Trainer-compatible callback adapter that does not import transformers.
- `eigentruth.control.RiskController` / `ControlPolicyConfig` / `ParticipationGateConfig` / `MultipleTestingGateConfig` / `SequentialGateConfig`: maps calibrated diagnostic thresholds, optional claim verification results, optional conformal abstention participation gates, multi-signal family-wise conformal gates, and explicit `decide_sequence(...)` sequential conformal gates to configurable product actions while keeping single `decide(...)` calls stateless.
- `eigentruth.control.PreGenerationRiskPolicy` / `SoftPreGenerationRiskConfig` / `select_pre_generation_profile`: dependency-free pre-generation runtime-profile routing from prompt features and caller metadata before model or verifier work, including a JSON-ready soft risk estimate with feature/metadata contribution traces.
- `eigentruth.control.DefaultCorrectionPolicy` / `PlanAwareCorrectionPolicy` / `ActionRequest`: compiles decisions into executable JSON-ready payloads for accept/retrieve/rewrite/steer/execute-tool/abstain/clarify flows; the plan-aware wrapper can enrich or append retrieval actions from `ClaimVerificationPlan` retrieval queries without changing the wrapped policy by default.
- `eigentruth.control.ActionExecutorRegistry` / `DryRunActionExecutor` / `TimeoutActionExecutor` / `ActionResult`: routes action requests to registered executors with side-effect-free fallback execution and best-effort timeout wrapping for local control-loop traces.
- `eigentruth.control.ActionExecutionPolicy` / `PolicyGuardedActionExecutor`: validates side-effecting action requests, replays idempotent results when a ledger is configured, and records audit metadata such as request ids, idempotency keys, timeout bounds, and timeout-enforcement status.
- `eigentruth.control.ActionReceipt` / `ActionReceiptSigner` / `ReceiptActionExecutor`: dependency-free HMAC-SHA256 action receipts for tool/action results. Receipts bind action, status, request id, output fingerprint, result fingerprint, and optional request fingerprint; `ReceiptActionExecutor` can wrap existing executors, while `ProductTrace.action_receipt_summary()`, `product_runtime_metrics()`, and bounded `summaries.action_receipts` expose receipt coverage, signed/unsigned counts, and result-fingerprint mismatches without requiring the signing secret.
- `eigentruth.control.ActionAuditPolicy` / `ActionAuditReport` / `audit_action_requests`: monitor-first action/tool-selection audit that checks planned actions against the selected risk decision and `ClaimVerificationPlan`, including missing retrieval actions, non-executable retrieval payloads, malformed `execute_tool` parameters, and claim-id mismatches; `ProductTrace` and runtime metrics expose the resulting summary without blocking execution by default.
- `eigentruth.control.InMemoryActionExecutionLedger` / `JsonActionExecutionLedger` / `SQLiteActionExecutionLedger`: store successful idempotent action results for request-local, JSON-file, or SQLite-backed replay without repeating side effects.
- `eigentruth.control.ProductTrace`: JSON-ready traces for diagnostics, claims, claim verification plans, verification-budget summaries, action-audit summaries, planned-action/result alignment summaries, claim-risk localization summaries, world-model participation/conflict/trace-gap summaries, context-sensitivity summaries, counterfactual-robustness pass/flip/false-invariance summaries, verification results, decisions, action execution summaries, verifier-route summaries, request runtime summaries, optional final answers, and metadata.
- `eigentruth.control.EvidenceGapPlan` / `plan_evidence_gaps_from_release_candidate(...)`: dependency-free release-blocker planner that converts failed release-candidate, registry-workflow, or direct `frontier_release_evidence_comparison` reports into JSON-ready evidence gaps and prioritized next actions, including multiple-testing frontier release blockers that should trigger a `run_truthfulqa_frontier_workflow.py --multiple-testing-signals ...` rerun or cell-level audit, citation/source-family batch-evidence blockers that should complete missing `run_external_citation_search_adapter_workflow.py`, `run_source_family_citation_search_workflow.py`, and `rollup_citation_search_batch_evidence.py` steps, product-runtime drift blockers for trace-level world-model/context-sensitivity/counterfactual-robustness evidence, and frontier rerun-rollup blockers that should complete the targeted rerun queue plus `rollup_frontier_*_reruns.py` artifact before feeding it back through `compare_frontier_release_evidence.py --frontier-rerun-rollup-report`. `benchmarks/plan_frontier_multiple_testing_reruns.py` can turn blocked multiple-testing cell summaries back into a per-cell rerun queue when the original frontier workflow report is available, and `benchmarks/plan_release_evidence_gaps.py --frontier-rerun-rollup-completion-json ... --frontier-rerun-rollup-queue TRACK=PATH` can turn blocked rerun-rollup decisions into explicit `rollup_frontier_*_reruns.py` commands with missing-queue diagnostics. These are planning artifacts only: they do not promote evidence, change control defaults, or export a product contract.
- `eigentruth.control.ProductPromotionEvidenceAudit` / `ProductPromotionEvidenceExport` / `audit_product_promotion_contract_evidence(...)` / `enrich_product_promotion_contract_evidence(...)`: dependency-free promotion-contract handoff auditor/exporter that checks whether a deployable contract carries the exact product-runtime drift evidence groups expected by `frontier_audit`, then can copy explicit local child-report evidence into an enriched contract. It is still an evidence-handoff artifact only: it explains and reduces missing runtime-drift evidence before replay, but does not invent verifier results or satisfy a release gate by itself.
- Product promotion contracts now preserve release-candidate recommended-runtime evidence (`recommended_runtime_seconds`, cost source, max recommended-runtime gate, and uncached forward cost) in contract metadata and ProductTrace metadata. The current frontier-audit handoff is `artifacts/smollm2_product_promotion_contract_v1_9/`: the source contract is exported from the promoted v13 release candidate (`benchmark_manifest:smollm2-l8-frontier-audit-release-candidate:0.13`) and carries the selected cache-only runtime recommendation (`0.191662s`). The enriched handoff records explicit child-report evidence, requires frontier release-evidence verdict provenance as the seventh default `frontier_audit` handoff group, and independently audits as promoted with `65/65` fields, verified manifests, and zero blocked groups.
- `eigentruth.control.FinalAnswer`: conservative JSON-ready product output that turns control-loop results into an answered/abstained/needs-retrieval/needs-clarification status with claim summaries, evidence snippets, follow-up metadata, and no hidden LLM rewrite step; `ProductTrace` can carry it for replay, feedback, bounded telemetry, and runtime-baseline audits.
- `eigentruth.control.ProductFeedbackRecord` / `ProductFeedbackStore`: dependency-free JSONL feedback records for post-hoc request or claim outcomes, linked by request id or trace fingerprint for product-quality audit reports.
- `eigentruth.control.run_verification_loop` / `StagedVerificationPolicy` / `EvidenceAcquisitionPolicy` / `EvidenceBundle`: dependency-free verify -> decide -> execute -> reverify loop that records a `ClaimVerificationPlan`, passes it through action-planning context for optional plan-aware policies, can gate expensive verifier routes behind diagnostic risk or sensitive claim metadata, can opt into uncertainty-driven second-stage verification planning for low-confidence preliminary results, can apply a budgeted answer/acquire/abstain evidence-acquisition decision before action planning, then feed retrieval action results back into verifier context. Evidence-acquisition traces explicitly record that conformal guarantees should be calibrated on the post-acquisition policy score, not inherited from a naive pre-acquisition threshold.
- `eigentruth.verify.ClaimVerificationPlanner` / `ClaimVerificationPlan` / `VerificationBudgetPolicy` / `VerificationEscalationPolicy` / `VerificationPlanCostEstimate`: dependency-free claim verification planning layer that preserves extracted claim metadata while emitting verifier scope, route hints, retrieval queries, citation checks, calculator checks, structured-state checks, world-model checks, triple-evidence audit routes for sensitive factual claims, inferred claim dependencies, and relative route/tool cost estimates for later control/runtime integration. `VerificationBudgetPolicy` adds a monitor-first adaptive verification step: it selects high-value claims and routes under claim, route, tool-payload, or estimated-cost caps, then records dropped-claim/route budget evidence for `ProductTrace` and runtime-baseline reports. `VerificationEscalationPolicy` adds the next monitor-first step: after a cheap preliminary verifier, low-confidence, insufficient-evidence, or entity-sensitive medium-confidence claims can produce a second-stage plan for stronger retrieval, triple-evidence, or world-model routes while preserving uncertainty reasons, entity candidates, and budget metadata.
- `eigentruth.verify.ClaimRiskSpan` / `ClaimRiskLocalizationReport` / `localize_claim_risk_spans`: dependency-free claim/span risk localization built from existing claim offsets, verifier statuses, route hints, entity/surface candidates, claim feature flags, and verification-budget drops; the report is JSON-ready and feeds `ProductTrace.to_bounded_dict()` plus runtime metrics without introducing learned token detectors.
- `eigentruth.verify.ClaimDependency` / `ClaimCoherenceReport` / `apply_claim_coherence`: optional claim dependency-graph coherence pass that downgrades supported child claims when required parent claims are missing or unsupported; `run_verification_loop(..., enforce_claim_coherence=True)` records coherence reports in product traces.
- `eigentruth.verify`: dependency-free pluggable claim extraction, configurable rule/regex/lookup/composite triple extraction, citation-integrity checks against caller-supplied citation catalogs, claim verification planning, rule-based claim metadata, in-memory verifier tools, lexical groundedness checks, subject-predicate-object triple evidence audits with linked cross-document slot coverage, per-slot evidence details, claim-level slot summaries, and sampled-response self-consistency verification for first-pass claim workflows.
- `eigentruth.adapters.RetrievalActionExecutor` / `InMemoryRetriever` / `SQLiteFTSRetriever` / `HTTPJSONRetriever` / `ProvenanceFilteredRetriever`: dependency-free retrieval executor shells for unsupported-claim evidence gathering from local snippets, SQLite FTS, or caller-provided HTTP JSON search services; adapter errors fail closed into structured action results for trace/replay, and provenance filters can block untrusted or low-score hits before they become verifier evidence.
- `eigentruth.adapters.ToolOutputStateSource` / `ToolOutputMapping`: maps local tool/action execution outputs into structured verifier state for post-tool checks.
- `eigentruth.adapters.InMemoryWorldModelAdapter`: deterministic world-model adapter for tests and domain-rule prototypes.
- `eigentruth.adapters.RuleBasedWorldModelAdapter` / `WorldModelRule`: auditable dependency-free domain/world-model adapter that matches explicit action fields, checks structured-state preconditions, applies deterministic state updates, and marks no-rule transitions so downstream control fails closed.
- `eigentruth.adapters.EnsembleWorldModelAdapter`: dependency-free disagreement-aware world-model ensemble wrapper for combining domain simulators, rule models, or learned world models behind the same transition verifier.
- `eigentruth.adapters.WorldModelReference` / `WorldModelView`: dependency-free serialization of the reference world-model contract and per-claim view function used by transition verification.
- `eigentruth.adapters.StateTransitionVerifier`: dependency-free action-conditioned postcondition verifier that predicts next state through a world-model adapter, then reuses structured state checks and emits `world_model_reference`, `world_model_view`, and `world_model_conflict` metadata for audit.
- `eigentruth.registry.ArtifactRegistry`: local JSON registry for calibration reports, calibration artifacts, traces, reports, action results, reusable concept artifacts, and saved concept metadata.
- Benchmark scripts for TruthfulQA-style evaluation, first-token entropy score dumps, TruthSubspace residual scoring, layer/score sweeps, conformal calibration, selective reporting, single- or multi-layer prompt-token hidden-state record export for pre-generation probe training, single- or multi-layer candidate-claim hidden-state record export for claim factuality probe training, manifold-distance sanity checks, intrinsic-dimension and covariance-spectrum layer-selection validation, synthetic training-telemetry sanity checks, pure-PyTorch tiny fine-tune telemetry checks, pseudo-label self-training collapse early-warning checks, synthetic trajectory-convergence sanity checks, forced-answer TruthfulQA trajectory replay, local pre-generation attention-probe training/evaluation/calibration/layer-sweep from hidden-state records, local claim factuality-probe training/evaluation/calibration/layer-sweep from hidden-state records, claim factuality workflow wrapping with text-redline, registry handoff, and multi-run comparison gating, fusion ablation signal selection, selected fusion artifact building, concept-registry/multi-probe smoke coverage, and callback-level training telemetry smoke coverage.
- The current source-family structured-QA product path is validated as a precise covered-fact correction slot, not a broad hallucination fix. It can produce a ProductTrace-visible correction handoff for an exact mapped fact, while the post-correction replay plus gap triage blocks new handoffs until richer property/indicator facts, provenance-audited citations, or world-model/calculator rules cover the remaining gaps. `run_source_family_structured_qa_claim_correction_workflow.py` now provides the one-command manifest-backed `claim mapping -> gap triage -> correction handoff -> optional triple audit` path for existing promoted covered-fact QA corpora, with the optional trace-level audit child failing closed when enabled. The promotion-contract evidence handoff exporter can consume either that promoted workflow report or the standalone triple-audit enrichment report directly, so exact correction evidence can enter release handoff without requiring a runtime-baseline aggregation detour. The lane execution queue now turns blocked lanes into cost-controlled adapter/rule batches without promoting them as evidence, and the lane rerun planner converts those batches into command rows that fail closed when source catalogs or prerequisite paths are missing. The first batch replay shows that reliable covered facts still need claim-intent mapping before product correction.
- Local development baseline: `make check-fast` runs lint, tests, and dependency consistency; `make check` adds deterministic profile-gate smoke checks; `make release-check` also builds the package.

Recent cleanup and platform changes:

- Core dependency is intentionally light: `torch` only.
- Hugging Face, examples, and benchmark datasets are optional extras.
- CI covers Python 3.10-3.13 with minimum/latest dependency lanes.
- Build metadata uses SPDX license syntax and validates with `python -m build`.

## Product Definition

EigenTruth should become a toolkit with four layers:

1. **Observe**: inspect hidden-state geometry during warmup, forward passes, and generation.
2. **Calibrate**: convert raw signals into reproducible thresholds, p-values, and abstention policies.
3. **Control**: decide whether to monitor, steer, retrieve evidence, rewrite, abstain, or ask for clarification.
4. **Verify**: validate atomic claims against tools, retrieval, structured data, or domain/world-model adapters.

The default product experience should be monitor-first. Steering and correction are opt-in and must expose diagnostics explaining why they fired.

## Non-Goals

- Do not claim that EigenTruth proves an answer is true.
- Do not claim that activation steering eliminates hallucinations.
- Do not hide uncertainty behind confident rewrites.
- Do not make Hugging Face, datasets, retrieval systems, or world models mandatory core dependencies.
- Do not optimize only for a single benchmark if it weakens general observability.

## User Workflows

### 1. Researcher Workflow

A researcher should be able to:

1. Build a manifold from factual warmup examples.
2. Sweep candidate layers and scoring methods.
3. Calibrate thresholds on held-out data.
4. Compare raw diagnostics, conformal risk, and external benchmark labels.
5. Save an experiment artifact with model revision, layer, seed, thresholds, and commit SHA.

### 2. Application Developer Workflow

An application developer should be able to:

1. Attach EigenTruth to an existing PyTorch/HF generation stack.
2. Run in monitor-only mode with no activation changes.
3. Receive structured risk diagnostics per generation.
4. Route high-risk answers through retrieval, verification, rewrite, or abstention.
5. Keep domain-specific tools optional and replaceable.

### 3. Product Workflow

A product system should be able to:

1. Route the request to a latency, balanced, or audit runtime profile from cheap pre-generation signals.
2. Generate a draft answer creatively under the selected runtime budget.
3. Extract atomic claims from the draft.
4. Score internal drift and semantic instability.
5. Decide whether to answer, acquire more evidence, or abstain under an explicit evidence budget.
6. Verify high-risk claims with external tools or world models.
7. Produce a final answer with confidence, evidence, and correction trace.

## Proposed Runtime Architecture

```text
user request
  |
  v
pre-generation risk router
  |
  +--> latency / balanced / audit runtime profile
  |
  v
LLM draft generation <---- optional retrieval/context injection
  |
  +--> EigenTruth internal diagnostics
  |
  v
atomic claim extraction
  |
  v
budgeted evidence acquisition decision
  |
  +--> retrieval / database / calculator / code tools
  +--> world-model or domain-state verifier
  |
  v
risk controller
  |
  +--> accept
  +--> rewrite with evidence
  +--> steer and regenerate
  +--> abstain or ask clarification
  |
  v
final answer + diagnostics + evidence trace
```

## World Model Positioning

World-model correction should be an adapter layer, not a hard dependency and not part of the core manifold math.

Recommended interface:

```python
class WorldModelAdapter:
    def verify(self, claim, context): ...
    def predict(self, state, action): ...
    def explain(self, claim): ...
```

Use the world model in three places:

1. **Before generation**: constrain the prompt with domain state, physical limits, business rules, or time-sensitive facts.
2. **After draft generation**: verify atomic claims and correct unsupported statements.
3. **During high-risk generation**: if internal diagnostics spike, pause free generation and route through verification.

This keeps LLM creativity in wording, synthesis, analogy, and exploration, while requiring evidence for factual claims, numbers, citations, physical constraints, and domain-state updates.

## Automatic Parameter Tuning

EigenTruth should treat layer choice, threshold choice, and steering strength as calibrated artifacts, not magic constants.

Planned artifact:

```text
CalibrationArtifact
- model_id
- model_revision
- target_layer
- score_names
- thresholds
- conformal_alpha
- steering_policy
- warmup_dataset_metadata
- calibration_dataset_metadata
- created_at
- eigentruth_version
- commit_sha
```

The calibrator should support:

- layer sweeps
- score sweeps (`maha`, `truth_proj`, future subspace scores)
- conformal thresholds
- steering-lambda sweeps
- coverage and power reports
- CPU-friendly smoke runs before large-model replication

## Module Boundaries

Current modules should remain narrow:

- `eigentruth.core`: tensor math, manifolds, distances, projections. No model loading, no datasets, no network.
- `eigentruth.intervention`: hooks and steering logic for PyTorch modules.
- `eigentruth.models`: user-facing wrappers around model instances.
- `eigentruth.eval`: metrics, conformal thresholds, selective reports, and benchmark helpers.
- `eigentruth.calibration`: calibration artifacts and parameter sweeps.
- `eigentruth.control`: pre-generation routing, risk controller, policy configuration, correction policy, action executor registry, verification loops, traces, and post-hoc feedback records.
- `eigentruth.verify`: claim extraction, verifier protocols, in-memory verification, and lexical groundedness.
- `eigentruth.registry`: local artifact metadata records.
- `eigentruth.adapters`: optional integration shells, including retrieval and world models.
- `examples/`: qualitative demos only; no benchmark claims.
- `benchmarks/`: reproducible evaluations with structured output.

Integration modules should stay interface-first: plain dataclasses, enums, and protocols with no heavyweight dependencies. Concrete retrieval, database, rewrite, and world-model implementations should land behind these contracts as evidence and tests justify them.

## Development Principles

1. **Evidence before claims**: every product claim must be tied to a test, benchmark, or documented limitation.
2. **Monitor before steer**: new interventions should first ship as diagnostics or dry-run policies.
3. **Small core, optional integrations**: keep mandatory dependencies minimal and push integrations into extras.
4. **CPU-first validation**: every new scientific idea needs a small CPU-testable mechanism check.
5. **Calibrate per setting**: thresholds must be model/layer/domain/dataset specific unless evidence shows transfer.
6. **Structured outputs**: diagnostics and benchmarks should produce machine-readable artifacts.
7. **Negative results are assets**: failed hypotheses should be documented rather than silently removed.
8. **No hidden global state**: hooks, probes, and adapters must have explicit lifecycle management.
9. **Safe default behavior**: default mode should not modify model activations.
10. **Composable APIs**: components should be usable independently in existing LLM systems.

## Definition Of Done

For code changes:

- Public behavior is covered by focused tests.
- `make check` passes locally.
- `make release-check` passes for dependency, package, or public API changes.
- New dependencies are optional unless they are required by core math.
- Documentation is updated when behavior, interfaces, dependencies, or limitations change.

For experiment changes:

- The experiment states question, method, accept criterion, deliverable, and cost.
- Dataset provenance, model revision, seed, layer, thresholds, and commit SHA are recorded.
- Results are saved as structured JSON when practical.
- Claims distinguish qualitative observations from benchmark evidence.

For product features:

- The feature has a monitor-only or dry-run mode.
- The feature exposes diagnostics explaining its decision.
- Failure modes are documented.
- High-risk corrections can abstain or ask for clarification instead of fabricating certainty.

## Near-Term Product Roadmap

### Completed 0.2-0.3 Foundation

- Score dump -> layer/score sweep -> conformal calibration artifact -> risk decision -> action request/result -> product trace.
- Conformal abstention report -> runtime `participate/abstain` decision helper plus `eval_conformal.py --save-abstention-report`, `--save-abstention-comparison`, and `--save-abstention-release-gate` sidecars for risk-coverage-controlled answer participation, multi-signal selection, and fail-closed promotion checks; `eval_abstention_stability.py` replays participation-gate candidates across seeded calibration splits without reloading models; `ParticipationGateConfig` can now consume a single report, candidate, or comparison report and let `RiskController` abstain/clarify accepted answers when the selected uncertainty score is outside the retained region. `ControlPolicyConfig.participation_gate_supported_override` is an opt-in verification-aware escape hatch: only all-supported verification results above the configured confidence floor can preserve an otherwise accepted answer when the internal participation gate would abstain.
- Configurable risk policy hooks for refuted, unsupported, error, and compound diagnostic/verification cases.
- Action executor registry with dry-run fallback and dependency-free local/SQLite/HTTP JSON retrieval executor shells plus provenance filtering before verifier evidence handoff.
- Claim extraction metadata for numbers, citations, negation, and time-sensitive claims.
- Benchmark reports now include selective accuracy, coverage, and confidence intervals.
- Versioned concept artifacts plus multi-concept probe monitoring for bring-your-own concept experiments.

### Completed 0.4 Verification Loop Shell

- Dependency-free verify -> decide -> execute -> reverify helper with final `ProductTrace` output, optional staged verifier gating for low-risk non-sensitive claims, and optional budgeted answer/acquire/abstain evidence acquisition before action planning.
- `EvidenceBundle` conversion from retrieval `ActionResult` payloads into claim-scoped verifier evidence context.
- Demo and tests for unsupported -> retrieve -> supported, no-hit retrieve, and refuted-claim hard stop paths.
- `eval_verifier_ensemble.py` benchmark shell for comparing calibrated internal diagnostics against retrieval/verifier suppression and refutation policies, including structured QA, static state, and action-conditioned state-transition routes.
- `build_truthfulqa_corpus.py` for creating a local TruthfulQA correct-answer evidence corpus.
- `build_evidence_fixture.py` for building non-oracle verifier fixtures from statement-bearing score dumps and local JSON/JSONL/text corpora, with explicit `label_usage` / `input_provenance` metadata, an `--omit-label-metadata` mode so adapter-audit fixtures can keep labels only in the score dump used for evaluation, and provenance filter flags for source/metadata/score/per-source gates before retrieval hits become verifier evidence.
- `backfill_truthfulqa_statements.py` for adding statement metadata to older TruthfulQA score dumps without rerunning models.
- Qwen l80 / SmolLM2 l80 oracle verifier-ensemble upper-bound report: label-derived perfect evidence drives verified false alarm to 0.000 and detection to 1.000 at alpha 0.100.
- Verifier ensemble reports include label-conditioned `verification_quality`, so evidence fixture quality can be measured separately from downstream control-policy metrics.
- Qwen l80 / SmolLM2 l80 local-corpus verifier baseline: conservative correct-answer retrieval drives verified false alarm to 0.008 at alpha 0.100, with true-supported rate 0.908 and false-supported rate 0.042.
- Qwen l80 / SmolLM2 l80 answer-echo retrieval stress control: retrieving from the audited answers themselves covers 556/556 records but drives false-supported rate to 0.980 and false-refuted rate to 0.000; verified detection collapses to 0.013 / 0.010 at alpha 0.100. This artifact is now executable as a fail-closed route/release gate via `compare_route_baselines.py --require-retrieval-stress-control` and `compare_release_candidates.py --required-route-require-retrieval-stress-control`, so retrieval grounding claims must show real evidence quality while also proving answer-derived retrieval fails this negative control.
- `QuestionAnswerVerifier` structured QA/domain-state adapter and Qwen l80 / SmolLM2 l80 structured QA baseline: exact question-answer facts drive verified false alarm to 0.000 and detection to 1.000 at alpha 0.100 on covered TruthfulQA questions.
- Staged structured QA gate artifact: at alpha 0.100, Qwen l80 / SmolLM2 l80 skip 79.3% / 82.9% of structured QA verifier calls, aggregate staged verified false alarm is 0.009, aggregate staged verified detection is 0.275, and `compare_verifier_routes.py` promotes `structured_qa` under explicit staged skip/quality gates. The promoted staged route is registered as `benchmark_manifest:truthfulqa-l80-structured-qa-staged-route:0.4`, and `compare_route_baselines.py` promotes it as the current local verifier-route baseline.
- Frontier l80 verifier-stability report: `eval_verifier_stability.py` replays staged structured QA on the current Qwen/SmolLM2 l80 JSONL frontier score dumps across seeds `0..9`; `report:truthfulqa-frontier-qwen-smollm2-l80-verifier-stability:0.1` shows verified false alarm/detection means of `0.006` / `0.305` for Qwen and `0.010` / `0.244` for SmolLM2, beating internal-only detection in 10/10 seeds for both runs while routing only 115/556 and 95/556 records respectively to `structured_qa`. The verified manifest is `benchmark_manifest:truthfulqa-frontier-qwen-smollm2-l80-verifier-stability:0.1`.
- `CalculatorVerifier` deterministic tool adapter for structured arithmetic claims and simple symbolic equations, using a restricted local arithmetic evaluator with no new mandatory dependencies.
- `StructuredStateVerifier` / `StateCheck` structured state and business-rule adapter for database, policy, and domain-state checks. It can be routed through `state_check` claim metadata or context and returns supported, refuted, insufficient-evidence, or error results with explicit decision rules.
- `SQLiteStateSource` / `SQLiteStateQuery` load read-only SQLite query results into nested verifier state, giving the structured-state path a real database integration without new mandatory dependencies.
- `ToolOutputStateSource` / `ToolOutputMapping` map local action or tool execution outputs into nested verifier state, giving post-tool checks a structured path without requiring a new tool runtime.
- `EnsembleWorldModelAdapter` combines multiple world-model adapters, reports agreement/disagreement metadata, degrades consensus confidence by agreement rate, and marks low-agreement predictions so `StateTransitionVerifier` returns insufficient evidence instead of trusting one uncertain transition.
- `StateTransitionVerifier` / `StateTransitionCheck` verify claims about action consequences by predicting next state through a world-model adapter and checking structured postconditions; `min_prediction_confidence` can fail closed on low-confidence predictions, `RuleBasedWorldModelAdapter` fails closed when no transition rule matches, and `InMemoryWorldModelAdapter` supports nested and dotted-path `set`/`increment`/`decrement` actions for deterministic domain-rule tests. Transition verifier outputs now make the world-model contract explicit through `WorldModelReference` and `WorldModelView`, and refuted postconditions include a compact `world_model_conflict` summary with expected/actual values and state fingerprints.
- `sqlite_state_control_demo.py` seeds an order/inventory/account SQLite fixture and emits a final `ProductTrace` where database state drives a dry-run abstain action despite low internal diagnostics.
- `state_transition_control_demo.py` emits a final `ProductTrace` where a world-model predicted postcondition refutes a claim about action consequences despite low internal diagnostics.
- `production_tool_loop_demo.py` emits one product-style trace that combines SQLite pre-tool checks, guarded side-effecting local `execute_tool`, optional JSON/SQLite idempotency replay, mapped `ActionResult.output`, post-tool structured verification, action execution summary, action audit metadata, and runtime route summary metadata.
- `TimeoutActionExecutor` can return a traceable `timed_out` action result using stdlib thread-pool timeouts; it is a best-effort local boundary, not a hard cancellation mechanism for already-running side-effecting adapters.
- `CompositeVerifier`, `RoutedVerifier`, and `calibrated_control_demo.py --enable-calculator` show a tool-first product trace path: claim metadata, context, or text patterns can route calculator-supported/refuted claims before lexical verifier fallback. `CitationVerifier` adds the same dependency-free route shape for citation-integrity checks against caller-supplied catalogs, catching unresolved references and DOI/arXiv/URL/author/year/title drift before a cited claim falls back to broader retrieval or groundedness. `SentenceClaimExtractor` now adds structured `calculation.expression` / `calculation.expected` metadata for simple symbolic equations and limited word-operator arithmetic such as `2 plus 2 is 5`, so calculator routing does not depend only on text patterns.
- `StagedVerificationPolicy` can be passed to `run_verification_loop` to skip expensive verifier routes for low-risk, non-sensitive claims while still triggering verification for calibrated diagnostic risk or claim metadata such as numbers, citations, time sensitivity, or explicit `requires_verification` flags. Product loops default to `fail_closed_on_skip=true`: skipped factual verification returns `clarify/unknown` rather than silently accepting unverified claims; local latency experiments can explicitly set `stage_fail_closed_on_skip=false` to preserve the older skip-and-accept behavior.
- `ClaimDependency` / `apply_claim_coherence` adds an optional dependency-graph coherence check over verified claims, so staged verification can fail closed when a supported conclusion depends on a missing or unsupported premise; coherence reports are trace events and metadata, and the default loop behavior is unchanged unless explicitly enabled or dependency edges are supplied.
- `SelfConsistencyVerifier` can be evaluated as a `self_consistency` verifier-ensemble route when fixtures provide sampled responses, giving claim-level support/refutation rates before retrieval fallback.
- `build_selfcheck_fixture.py` converts dumped INSIDE sampled continuations or external sampled-generation files into verifier-ensemble fixtures for reproducible self-consistency route evaluation. `build_selfcheck_signal_score_dump.py` uses the same alignment path to append direct self-consistency score columns for conformal calibration or score fusion without requiring a verifier sidecar. `plan_selfcheck_sample_collection.py` preflights those aligned samples before fusion, writing the missing-record list, total sample deficit, sample-quality gate projection, and rerun commands needed to turn sample-quality failures into a concrete collection plan; `run_selfcheck_signal_fusion_workflow.py` now writes the per-run plans into its workflow report and artifact manifest by default.
- `default_verifier_routes` / `default_routed_verifier` provide a reusable product route stack: deterministic calculator, optional citation, state/world-model/QA adapters, strict triple-evidence audits for sensitive factual claims, and lexical groundedness fallback. `RoutedVerifier` records route match reasons such as metadata keys, context keys, text patterns, feature flags, or fallback selection; `ProductTrace.verification_route_summary()` aggregates selected, matched, and skipped verifier routes for runtime trace review.
- `RuntimeProfile` now exposes shared `latency`, `balanced`, and `audit` defaults for release gates and product control-plane staging; `PreGenerationRiskPolicy` / `SoftPreGenerationRiskConfig` / `select_pre_generation_profile()` choose a profile before generation from cheap prompt/metadata risk markers, record a soft risk probability plus feature/metadata contributions, and can optionally route on that soft estimate when explicitly configured; `calibrated_control_demo.py --pre-generation-profile auto` records that assessment in `ProductTrace.metadata` and can apply it when no explicit runtime profile is supplied; `RuntimeProfileSelectorPolicy` makes the post-diagnostic auto selector configurable by diagnostic risk/action, sensitive claim features, metadata keys, and optional verification-plan cost thresholds; `select_runtime_profile()` chooses among profiles before verifier work, and `calibrated_control_demo.py --runtime-profile auto --runtime-profile-selector-policy` writes the selected profile, selection reason, selector policy, and staging behavior into trace and registry metadata.
- `RuntimeTrace` / `RuntimePhaseTiming` records dependency-free request phase timings in `ProductTrace.runtime_trace`, so product runs can compare diagnostic, verification, action execution, retrieval evidence, and re-verification costs before changing performance defaults.
- `ProductRuntimeBudgetPolicy` / `evaluate_product_runtime_budget` turns `ProductTrace.runtime_trace`, cache metadata, verifier route metadata, and staged-verification skip events into optional fail-closed product gates over total request time, named phase timings, phase p95/p99 timings, route duration, mean attempted routes, retrieval use rate, aggregate/named cache hit rates, minimum verification skip rate, and maximum verified claim count; `calibrated_control_demo.py --max-runtime-total-seconds/--max-runtime-phase-seconds/--max-runtime-phase-p95-seconds/--max-runtime-phase-p99-seconds/--max-mean-attempted-route-count/--max-retrieval-use-rate/--min-cache-hit-rate/--min-verification-skip-rate/--max-verified-claim-count` writes the evaluated result into trace and registry metadata.
- `run_product_runtime_baseline.py` now adds `optimization.hotspots`, `optimization.recommendations`, and `optimization.policy_hints` to product runtime baselines, while runtime metrics expose claim verification-plan coverage, route/tool-payload counts, planned-action/result alignment, verification-budget exhaustion summaries, claim-risk localization counts, triple/slot-audit coverage, world-model participation/conflict/low-agreement/trace-gap summaries, and ProductTrace summaries retain verification-plan cost estimates. Saved traces can identify dominant phases/routes, low cache hit rates, excessive retrieval or verifier fanout, missing action execution results, missing staged verification, missing verification plans, exhausted verification budgets, high-risk localized claims, extracted triples without strict slot audits, world-model reference/view trace gaps, audit-heavy profile usage, and candidate budget thresholds without rerunning models or verifiers. With `--save-recommended-policy`, those candidate thresholds are also materialized as a reusable `ProductRuntimeBudgetPolicy` artifact for later gate runs.
- `eval_verifier_ensemble.py` now supports `--state-source` JSON state and SQLite query specs plus fixture-level `state_check` and `state_transition` routes, optional state-source `world_model_rules` that switch transition prediction from legacy in-memory updates to `RuleBasedWorldModelAdapter`, optional state-source `world_model_ensemble` specs that wrap multiple rule-based members in `EnsembleWorldModelAdapter` and preserve prediction agreement metadata in verified-record sidecars even when agreement fails closed, optional `--min-world-model-confidence` fail-closed gating for action-conditioned world-model postconditions, optional `--enable-triple-evidence` strict fact-level triple audits, and reports `route_summary`, `route_quality`, route-level cost/tail-latency metrics, optional `--staged-verification` skip-rate/cost gating metrics, and per-alpha `route_control_impact` for structured QA, state transition, structured state, triple evidence, lexical groundedness, retrieval-backed groundedness, and staged verifier skips; `--verified-records-jsonl` writes per-claim verifier outputs to a compact sidecar for audit/debug without embedding them in the main report, including transition-level `world_model_reference`, `world_model_view`, prediction metadata, and conflict summaries; `--compact-json` keeps large automated report artifacts smaller without changing payload semantics.
- `refresh_verifier_route_artifacts.py` regenerates current-schema verifier route reports from saved score dumps, claims, and local verifier corpora so old artifacts can be upgraded for promotion gates without rerunning model forward passes; its summary exposes route latency/cost/cache metrics for promotion audit, supports compact verifier/route/promotion/workflow JSON output, and regression coverage now includes structured QA, structured-state, state-transition, and triple-evidence promotion gates.
- `build_domain_state_fixture.py` generates deterministic order-fulfillment score/claim/state fixtures plus optional SQLite database and SQLite state-source spec so structured-state verifier behavior can be benchmarked without relying on label-derived TruthfulQA oracle evidence.
- `build_transition_fixture.py` generates deterministic order-reservation score/claim/state fixtures so world-model predicted postconditions can be benchmarked without a real simulator or external dependency; `--rule-based-world-model` emits typed transition actions plus auditable `world_model_rules` for the rule adapter path, while `--world-model-ensemble` emits a three-member rule-based ensemble stress fixture that creates nonzero world-model disagreement/agreement-gap signals. The default `label_stress` strategy preserves the controlled false-transition stress pattern, and `--world-model-ensemble-strategy policy_replay` makes disagreement arise from a conservative high-quantity reservation policy that cuts across true and false labels.
- `compare_verifier_routes.py` aggregates route-level verifier quality, runtime cost, tail latency, cache summary, staged-verification skip/quality metrics, and control impact across saved verifier-ensemble reports, producing a cost-aware route leaderboard, Pareto frontier candidates, route-specific promotion decisions, by-route weighted metrics, and optional fail-closed route quality gates without loading models. Aggregate cost means use only observation counts paired with finite source totals; missing or non-finite source metrics are surfaced as `invalid_metric_counts` and block promotion instead of being silently averaged away; configured staged gates fail closed when staged evidence is missing or verifier skipping degrades verified false alarm/detection beyond threshold.
- `run_adapter_promotion_workflow.py` composes route comparison, route-specific `promotion_decision=promote`, and optional registry-backed performance baseline gates into one fail-closed adapter promotion report, with compact JSON output and optional registry-ready artifact manifests available for automation artifacts.
- `run_adapter_promotion_registry_workflow.py` closes the route-baseline automation loop: it runs adapter promotion, writes the artifact manifest, recursively verifies it, and registers the promoted verifier-route baseline in `ArtifactRegistry` unless the route decision is blocked. Registry metadata includes staged-verification skip/quality metrics when the source route comparison was built with staged gates.
- `compare_route_baselines.py` compares registered verifier-route promotion manifests without rerunning model or verifier work, recursively verifies each manifest, reloads saved route-comparison reports or covered-facts `route_summary` artifacts from the Wikidata structured route workflow, applies optional quality/cost/tail/retrieval/runtime/cache-reuse gates, can require covered-fact `property_metrics` count and per-property record/source/decision-quality minima, can require non-oracle retrieval evidence with label-free claim metadata and `input_provenance`, can require a recorded retrieval provenance filter with expected source prefixes, metadata tags, and score floor, can require an answer-echo retrieval stress manifest whose corpus type is `retrieval_stress_answer_echo` and whose false-supported/false-refuted rates expose self-support failure, fails closed on invalid source metrics or enabled budget metrics that are missing/non-finite, and recommends one deployable route baseline.
- `compare_external_evidence_baselines.py` is the post-hoc external-evidence release comparator: it delegates route/stress/provenance gates to `compare_route_baselines.py`, pairs saved candidate and text/length redline `eval_score_ensemble.py` reports by run name, compares the best non-text candidate signal or fusion against cheap text controls at the report `best_alpha`, and blocks on missing reports, ambiguous pairing, non-finite metrics, or insufficient detection/AUROC margins. It can also require a promoted `wikidata_structured_qa_route_workflow` covered-facts route summary with configured route names, source/true/false record counts, and per-property covered-fact quality minima, so structured KG correction evidence is explicitly scoped and not confused with broad open-domain retrieval coverage. It can write a recursive artifact manifest, verify that manifest, and register the comparison as a reusable `report:*:*` handoff. `compare_release_candidates.py --external-evidence-baseline-comparison` or `--external-evidence-baseline-comparison-key` can now require that comparator artifact before release promotion.
- `fetch_wikidata_reference_docs.py` fetches or replays small Wikidata SPARQL result sets into CC0 JSONL source documents. The default `country_capitals` preset gives the external evidence pipeline a real, licensed, domain-scoped source gate while keeping route-quality promotion separate. The `country_core_facts` preset fetches template-ready `P36` capital, `P37` official-language, and `P38` currency facts so the same source gate can expand into structured knowledge-graph correction routes. Bare Wikidata Q/P id labels are filtered by default to avoid unresolved label-service rows entering retrieval evidence.
- `build_external_retrieval_corpus.py` normalizes caller-supplied JSON/JSONL/text source files into an explicit `external_evidence_candidate` retrieval corpus, fingerprints the source files, and rejects score-label, claim-id, or score-dump row-link metadata before the corpus can enter verifier benchmarks.
- `audit_retrieval_corpus_provenance.py` adds a corpus-level preflight for retrieval evidence, classifying corpora as `external_candidate`, `controlled_dataset_baseline`, `answer_echo_stress_control`, `untyped_local_corpus`, or `unrecognized_corpus` from score-dump/corpus metadata, source-record links, claim-id links, label-use flags, and answer-copy rates before a local retrieval run is promoted as grounding evidence. Grounding audit fails closed unless the corpus has a recognized explicit external corpus type.
- `artifacts/wikidata-country-capitals-external-route-audit-qwen05-l80/` is the first real external-source route-quality audit. It verifies the manifest chain and records the correct blocked status: country-capital evidence covers 254/556 Qwen l80 statements but fails the retrieval route false-alarm gate (`0.149` > `0.05`), so it remains a source/provenance smoke gate rather than a product verifier route.
- `artifacts/wikidata-country-core-facts-external-route-audit-qwen05-l80/` measures the broader multi-predicate source. Coverage improves to 275/556 records with 1125 hits across `P36`, `P37`, and `P38`, but lexical `retrieval_groundedness` still fails false-alarm gating (`0.155` > `0.05`). Product implication: integrate these facts through structured QA or property/triple verification instead of promoting broad lexical retrieval.
- `analyze_retrieval_route_gaps.py` consumes verifier verified-record JSONL sidecars and produces route-gap reports with coverage, final statuses, selected routes, source/property counts, and bounded examples. The Wikidata route gap report shows the current failure is coverage/semantic matching (`insufficient_evidence` for all records), not provenance.
- `build_wikidata_qa_corpus.py` converts structured Wikidata fact documents into label-free, template-driven or source-inferred `QuestionAnswerVerifier` corpora, giving the control plane a low-dependency knowledge-graph correction bridge for covered properties. For `P36`, it can support correct country-capital answers and refute mismatched answers through `retrieval_structured_qa`; template JSON extends the same route to additional properties such as official language or currency, while `--auto-template-from-source` handles generic `subject/property/value` Wikidata source docs. Broader product claims still require more predicates or a richer structured verifier.
- `run_wikidata_structured_qa_route_workflow.py` turns the generated Wikidata QA corpus into a reproducible covered-facts route artifact. Its route summary now includes `property_metrics` keyed by Wikidata property id, so release gates can inspect predicate-level record counts and support/refutation quality instead of relying only on aggregate route metrics. The current `artifacts/wikidata-country-core-facts-structured-qa-route/` result promotes `structured_qa` for the `P36`/`P37`/`P38` facts it covers: `718` balanced true/false rows, `359` supported true facts, `359` refuted swapped-answer false facts, decision accuracy `1.0`, and false-supported rate `0.0`. Product implication: use the structured route as the correction path for KG-covered claims; keep lexical retrieval and broad open-domain grounding behind their separate route-quality gates.
- `build_source_family_qa_corpus.py` and `run_source_family_structured_qa_route_workflow.py` generalize that bridge beyond Wikidata-only corpora while preserving the covered-fact boundary. The current source-family corpus extracts `18` safe structured facts from seeded-news source-family results (`16` Wikidata/reference and `2` World Bank official-statistics), then the route workflow creates `36` balanced records and promotes `structured_qa` with decision accuracy `1.0` and false-supported rate `0.0`; provider slices are exposed separately (`wikidata=32`, `worldbank=4`). `audit_source_family_structured_qa_claim_mapping.py` then checks real blind spots against those exact facts and blocks with `0/89` mapped correction candidates. Product implication: source-family acquisition can feed exact structured QA correction for covered facts, but runtime corrections must first prove generated claims or blind spots map into those facts; the current SmolLM2 corpus needs claim-specific fact expansion, citation evidence, or world-model/calculator rules before a correction handoff can use it.
- `run_source_family_structured_qa_fact_collection_workflow.py` closes the next source-family candidate loop for those claim-specific gaps. It executes the `806`-row fact-collection request corpus against local source-family catalogs, returns `2334` candidate adapter results for `778/778` source-backed requests, preserves `28` world-model/calculator rule stubs, and rebuilds `70` structured QA candidate facts without treating adapter results as verifier evidence. The follow-up route audit promotes covered-fact quality on `140` balanced rows, and claim mapping recovers `1/89` correction candidate: the Tesla founder blind spot maps to Wikidata `P112` with Martin Eberhard. A post-correction replay plus `triage_source_family_structured_qa_gaps.py` keeps the remaining `88` rows blocked from handoff, with `0` new correction candidates, `1` answer-support audit row, and lane-level request counts for structured facts, citations, entity resolution, disambiguation, and world-model rules. `build_source_family_structured_qa_lane_execution_queue.py` now converts that blocked state into `87` collection targets, `752` answer-free adapter/rule requests, and `29` lane-aware batches. `run_source_family_structured_qa_lane_batch_workflow.py` now proves both execution sides: all `24` source-backed batches run to `2145` candidate results and `63` structured QA facts with a promoted `126`-row covered-fact route, but claim mapping still remains `0/88`; the `5` rule-only batches now emit `37` non-evidence world-model/calculator stubs over `34` targets. `run_world_model_rule_authoring_adapter.py` converts those stubs into a deterministic input contract and blocks as `needs_inputs`, then `build_world_model_rule_input_collection_plan.py` lowers the `37` requests into `4` typed input batches: `12` entity-role, `12` numeric, `9` mechanism, and `4` temporal snapshot tasks, adding execution-only fields such as `expected_entity`, `calculation.expression`, `calculation.expected`, and per-task `source_citation`. `fill_world_model_rule_inputs_from_correction_handoff.py` now proves the first source-backed fill: it fills the Tesla founder entity-role task from the promoted correction handoff and ProductTrace claim binding, then a filled adapter replay executes `1/37` stubs and emits one candidate `refuted` rule result while leaving `36` tasks unfilled. `promote_world_model_rule_candidates.py` promotion-gates that candidate with matching source citation and explicit rule inputs (`1` promoted, `0` blocked, `36` pending), and `build_world_model_rule_candidate_handoff.py` turns the promoted candidate into a ProductTrace/action-result handoff (`high/abstain`, dry-run action, source-citation provenance). Product implication: EigenTruth now has a full candidate-fact loop from observed blind spot to mapped structured QA correction, typed rule-input worklist, first deterministic candidate execution, first rule candidate promotion gate, and first source-backed rule-candidate ProductTrace handoff; the remaining records need broader deterministic input collection or stronger citation evidence, not another broad local-catalog replay.
- The target-specific SmolLM2 l80 blind-spot Wikidata run now promotes a second covered-facts handoff: `292` source docs over `10` properties become `584` balanced `structured_qa` rows with all true facts supported, all swapped-answer false facts refuted, and handoff record `report:truthfulqa-frontier-smollm2-l80-blind-spot-wikidata-covered-facts-handoff:0.1`. The mapping audit `report:truthfulqa-frontier-smollm2-l80-blind-spot-covered-fact-mapping:0.1` keeps the product boundary honest: only `10/89` blind spots are conservative covered-fact correction candidates, `5` have evidence values supporting the model answer, `6` show answer-entity collision risk, and `52` have no joined facts. Product implication: EigenTruth can now correct collected Wikidata-covered facts deterministically, but should not change broad blind-spot runtime defaults until generated claims are mapped into that covered-fact set or additional citation/world-model evidence covers the unresolved records.
- `StructuredFactVerifier` is the natural-language property route for the same KG-covered facts. It extracts subject-predicate-object triples from claims through the default rule-based extractor or an injected `ClaimTripleExtractor`, supports common paraphrases, aliases, and multi-object lists for known facts, and refutes object mismatches when the subject/predicate pair is known. The current `artifacts/wikidata-country-core-facts-structured-fact-route/` result promotes `structured_fact` on the same `718` balanced rows with decision accuracy `1.0` and false-supported rate `0.0`, closing the gap between QA-shaped metadata and normal generated claims for covered `P36`/`P37`/`P38` properties.
- `run_wikidata_structured_qa_route_workflow.py --fact-claim-style paraphrase_robustness` is the covered-fact robustness replay for `StructuredFactVerifier`. The current `artifacts/wikidata-country-core-facts-structured-fact-paraphrase-route/` result expands those KG facts into `2868` canonical, possessive, subject-first, currency-use, and multi-object-list true/false rows (`1434` true / `1434` false), selects `structured_fact` for every row, reaches decision accuracy `1.0`, and keeps false-supported rate `0.0`. Product implication: the route can tolerate common surface-form variation for covered facts, but still must be gated by KG coverage.
- `artifacts/wikidata-cross-corpus-triple-extraction-fixture-matrix/` is the first real cross-corpus extractor evidence artifact. It combines the country-core Wikidata source corpus with a fetched organization/product Wikidata source corpus over `P159`, `P176`, and `P571`; both generated fixture workflows promote, the combined matrix covers six predicates, and recursive manifest verification passes. Product implication: regex-with-rule fallback templates have release evidence for covered KG predicates, but broad open-domain extraction still requires broader corpora and separate negative/adversarial extractor gates.
- `artifacts/wikidata-cross-corpus-triple-extraction-adversarial-matrix/` is the first negative cross-corpus extractor robustness gate. It adds one record per structured fact for each of six controls: negated near-miss, predicate-confusion assertion, non-assertive quoted/questioned mention, ambiguous/multi-object wording, temporal-qualified wording, and metalinguistic/comparison context. After applying a shared blocked-context guard to regex and rule-based extraction paths, both country-core and organization/product workflows promote. The expanded matrix has `3590` country-core records and `80` organization/product records, keeps best F1 at `1.000`, reaches predicate-confusion F1 `1.000`, and keeps all zero-expected subgroup false-positive rates at `0.000`. Product implication: covered-template routes can reject common negative-context traps; open-domain extraction still needs broader corpora, richer surface variation, and learned/external extractor adapters.
- `artifacts/wikidata-cross-corpus-triple-extraction-adversarial-matrix-v1/` runs the same adversarial matrix through the local external-prediction adapter contract using two lookup prediction files. The matrix promotes with two external prediction files, both corpora covered, `mean_best_external_f1=1.000`, and recursive manifest verification. Product implication: the external extractor handoff format is now release-gated end to end, but this is still a contract replay rather than evidence that a real learned extractor is ready.
- `LookupTripleExtractor`, `eval_triple_extraction.py --predictions`, `run_triple_extraction_fixture_workflow.py --external-predictions NAME=PATH`, and `run_triple_extraction_fixture_matrix.py --external-predictions CORPUS:NAME=PATH` are the dependency-free adapter shell for learned or external triple extractors. External systems stay outside the core package and write local JSON/JSONL predictions; EigenTruth evaluates those predictions against the same labeled fixtures, exact F1 metrics, subgroup false-positive rates, cross-corpus matrix summaries, and artifact manifest paths before any extractor is trusted by structured-fact or triple-evidence routes. `compare_release_candidates.py` and `run_release_candidate_registry_workflow.py` can now fail closed on external prediction count, external-prediction corpus coverage, and mean best external F1 when learned/external extractor evidence is required for a release.
- `run_adapter_family_matrix.py` builds deterministic structured QA, structured-state, state-transition, optional retrieval-groundedness, optional retrieval-structured-QA, and optional strict triple-evidence fixtures, runs each through refresh/promotion gates, and aggregates the route-family quality/cost/tail/cache metrics into one local no-model comparison matrix. The triple-evidence route gates false support from missing subject-predicate-object evidence coverage rather than treating insufficient evidence as a direct refutation.
- `run_local_retrieval_route_workflow.py` turns a statement-bearing score dump and local JSON/JSONL/text corpora into a registered retrieval-groundedness route baseline, including the generated claims fixture, verifier report, route comparison, promotion report, source corpus, score dump, optional answer-echo stress manifest reference, optional persistent SQLite FTS candidate retrieval metadata, optional provenance filter gates, optional fingerprint-keyed claims and verifier-trace cache metadata, lightweight runtime profile metadata, and optional runtime/cache budget gates in the manifest for recursive provenance and cost checks.
- `run_adapter_readiness_workflow.py` composes adapter-family quality promotion with same-machine cache-profile performance promotion into one final fail-closed adapter readiness decision, can explicitly include retrieval-groundedness, retrieval-structured-QA, and strict triple-evidence adapter families, writes `runtime-recommendation.json` from the promoted performance matrix plus optional INSIDE sampling profile / trigger-budget sweep evidence, lifts `state_transition` world-model adapter/rule-count evidence into the readiness report and manifest metadata, and includes it in the top-level artifact manifest for recursive verification and registry promotion.
- `run_adapter_readiness_registry_workflow.py` runs the readiness workflow, supports the same retrieval and strict triple-evidence adapter-family inclusion flags, requires readiness promotion by default, recursively verifies the top-level manifest, and records it in `ArtifactRegistry` as the reusable local promotion baseline with adapter-family route, retrieval-route, audit-route, and state-transition world-model evidence metadata.
- `CachedVerifier`, `CachedRetriever`, and `CachedStateSource` provide request-scoped caching plus hit/miss stats for repeated verifier, retrieval, and state-source calls; `eval_verifier_ensemble.py` includes these `cache_stats` in each run report.
- `run_uncertainty_escalation_workflow.py`, `uncertainty_escalation_report()`, and `eval_uncertainty_escalation.py` provide the no-model measurement layer for opt-in low-confidence and entity-sensitive verification escalation: local claim/evidence fixtures can now be run through `run_verification_loop(..., escalation_policy=...)`, saved as loop-result JSONL, fingerprinted as an artifact manifest, optionally registered in `ArtifactRegistry`, then replayed into escalation trigger rate, entity-sensitive trigger counts, selected route/retrieval cost, decision transitions, retrieval evidence coverage, and optional label-conditioned false-accept/selective-accuracy deltas without binding the core package to a concrete production verifier or retriever.
- `eval_truthfulqa.py --profile` now emits a structured performance summary with bottleneck phase, top phases, grouped time shares, and warmup/eval throughput fields for reproducible optimization comparisons.
- `ScoreDump` / `ScoreDumpIdentity` / `load_score_dump` / `load_score_dump_columns` / `load_score_dump_columns_with_extras` / `load_score_dump_statement_scores` / `load_score_dump_layer_scores` / `score_dump_file_metadata` / `score_dump_identity` / `score_dump_cache_summary` provide one validated post-hoc score-dump entry point for conformal calibration, abstention/comparison, score-ensemble, verifier-ensemble, fixture-builder, calibration-transfer, and sweep-calibration reports, including record counts, score names, sweep-layer metadata, file size, SHA-256 provenance, stable model/dataset/layer/score-schema/scoring-config identity, and run-local cache hit/miss observability so repeated analyses can verify exact input reuse without model reruns. `LayerScoreSweepCalibrator.calibrate_from_score_dump()` and `LayerScoreSweepCalibrator.calibrate_from_layer_scores()` consume validated or preloaded dumps directly and support bounded `max_workers` CPU parallelism for large layer/score post-processing sweeps, while `RankScoreFusionArtifact` / `RankScoreFusionCalibrator` make simple direction-aware rank fusion reusable as a deployable conformal artifact and `GeometryScoreFusionArtifact` / `GeometryScoreFusionCalibrator` add a rank-calibrated geometry-by-uncertainty interaction score that `eval_score_ensemble.py` can benchmark and save from existing dumps. `build_verifier_signal_score_dump.py` converts verifier verified-record JSONL sidecars into standard score-dump columns, making structured verifier, retrieval, selfcheck, world-model agreement, explicit world-model conflict, conflict magnitude, and world-model trace-gap outcomes usable as calibrated final-correction signals; `build_selfcheck_signal_score_dump.py` converts aligned sampled responses directly into score-dump columns for isolated self-consistency signal tests; `export_inside_diagnostics_samples.py` recovers sampled response text from an existing INSIDE diagnostics cache and fingerprints the source score dump/cache/output samples; `plan_selfcheck_sample_collection.py` records aligned-sample deficits before fusion; `build_text_baseline_score_dump.py` appends dependency-free text/length redline controls so detector improvements can be compared against cheap lexical artifacts; `run_verifier_signal_fusion_workflow.py` wraps local retrieval/selfcheck fixture construction, verifier sidecar writing, verifier-signal score-dump conversion, geometry-fusion reporting, geometry artifact export, and manifest verification into one no-model non-oracle evidence workflow; `run_world_model_signal_calibration_workflow.py` specializes that loop for deterministic state-transition/world-model correction evidence, generating the transition fixture, verifier sidecar, enhanced score dump, fusion report, release gate over trace-gap/conflict-calibration evidence, nested manifest verification, and optional registry report in one command; `run_selfcheck_signal_fusion_workflow.py` wraps direct sampled-response selfcheck signal construction, sample-collection preflight artifacts, sample-quality gating, score-ensemble comparison, optional geometry-by-selfcheck artifact export, and manifest verification into one no-model workflow for aligned multi-sample replay. Report builders share a run-local score-dump cache to avoid duplicate hashing of repeated score paths and duplicate scans of the same selected JSONL view, `ScoreDumpJsonlManifest` / `ScoreDumpRecord` / `iter_score_dump_jsonl_records()` provide an optional manifest-backed JSONL format for large post-hoc score artifacts, selected loaders materialize only requested primary, adaptive-extra, abstention/comparison, statement-bearing primary, or layer/score columns plus labels, cached selected views invalidate when the manifest or records file changes, JSONL metadata fingerprints both the manifest and records file and emits a canonical identity cache key, `score_dump_file_metadata()` builds JSONL summaries from manifest label counts when available and otherwise falls back to a cached label-only record scan, selected JSONL scans prime the run-local records hash cache for later full-provenance metadata, and `eval_conformal.py` reuses a preloaded JSONL layer/score view when writing sweep reports or best calibration artifacts. `eval_truthfulqa.py --dump-scores-format jsonl` writes that format directly with per-record extras in the records sidecar. `eval_conformal.py --artifact-manifest` now fingerprints the input score dump, structured conformal report, abstention report, abstention comparison report, sweep report, and saved calibration artifacts for registry/release verification.
- `run_calibrated_observability_workflow.py` now wraps score-dump creation or reuse plus conformal report, sweep report, best calibration artifact, nested manifest verification, top-level artifact manifest, optional registry record, and an `evidence_bundle` summary into one reproducible calibrated-observability closure; `--runtime-preset quick|calibrate|full` fixes common local smoke, dump-reuse, and real-run parameter profiles while preserving explicit CLI overrides. It can consume a `compare_layer_band_selectors.py` report via `--sweep-layers-from-band-report`, derive the selected candidate layers, optionally expand them into a denser local grid with `--sweep-band-expand-radius`, optionally set the primary target layer from the report with `--sweep-band-target-layer`, fingerprint that source report, and record the chosen strategy/run/AUROC-regret provenance before running the normal calibrated sweep. It also forwards optional `--attention-pathway` / `--attn-implementation` settings into `eval_truthfulqa.py` when attention-flow pathway columns are explicitly requested, and `--multiple-testing-signals` now saves a multi-signal conformal gate report plus runtime calibration artifact into the same manifest/evidence bundle for product-gate replay. Its default calibrate/full candidate signals now include the ICR-inspired `resid_update_norm` residual-dynamics proxy plus cross-layer `resid_update_profile_*` summaries for post-hoc calibration/fusion experiments; attention-pathway signals remain opt-in because backend support varies.
- `run_pathway_intervention_workflow.py` now turns pathway mechanism checks into a reproducible local evidence bundle: it runs or plans baseline, activation ablation, and source-token patch TruthfulQA reruns, compares both interventions against the baseline with `eval_pathway_intervention.py`, writes nested manifests plus a top-level artifact manifest, and exposes `evidence_bundle.release_ready` only when both intervention gates promote and all expected artifacts exist.
- `compare_release_candidates.py` and `run_release_candidate_registry_workflow.py` can now consume that pathway-intervention workflow by file or registry key as an optional fail-closed release gate. When supplied, the workflow must be status `complete`, manifest-verified, `evidence_bundle.release_ready == true`, and both activation-ablation/source-patch comparison gates must promote before the release candidate can promote.
- `ProductPromotionContract`, `ProductRuntimeEvidenceBundle`, `export_product_promotion_contract.py`, and runtime trace metadata now preserve the promoted pathway-intervention workflow report/manifest/registry provenance, release-ready flag, layer configuration, selected signals, and mechanism gate statuses after product handoff. This keeps mechanism evidence observable in runtime reports without making pathway intervention a default runtime action.
- Abstention release-gate sidecars from `eval_conformal.py --save-abstention-release-gate` add a fail-closed promotion check on top of abstention report/comparison evidence, requiring both a minimum conservative conditional-correctness lower bound and a maximum empirical abstention rate before promoting a participation gate into product control.
- `run_truthfulqa_frontier_workflow.py` is the multi-model/multi-scale research orchestrator for Qwen/SmolLM2 l20/l80-style runs: it executes or reuses calibrated-observability cells, then produces a cross-cell `eval_score_ensemble.py` rank-fusion report, top-level artifact manifest, and optional registry record. It now accepts a per-cell `--cache-dir` root that forwards statement encoding, layer stats, warmup checkpoint, and eval-reps cache paths through the calibrated-observability workflow, so l80 and multi-seed studies can reuse expensive model-forward evidence instead of rebuilding it. It can also fan out a `compare_layer_band_selectors.py` report through `--sweep-layers-from-band-report`, match report rows per cell with `--sweep-band-run-template`, limit application by `--sweep-band-scales`, pass `--sweep-band-expand-radius` / `--sweep-band-target-layer` into each calibrated-observability cell for dense band reruns, forward opt-in attention-pathway plus DECK detectability-taxonomy settings to every cell, and now forward `--multiple-testing-signals` so each calibrated cell emits a multi-signal conformal gate report/runtime artifact with a top-level all-cell pass summary in the frontier manifest and registry metadata. The current registered l80 frontier artifact is `report:truthfulqa-frontier-qwen-smollm2-l80:0.1`: Qwen peaks at `truth_proj` layer `-10` with AUROC `0.764` and detection `0.242` at alpha `0.100`; SmolLM2 peaks at `truth_proj` layer `-16` with AUROC `0.782` and detection `0.265`. The paired cache-only replay is `report:truthfulqa-frontier-qwen-smollm2-l80-cache-only:0.1`: it reproduces the same score records and best sweep choices while reducing frontier wall-clock from `1140.245s` to `24.028s`, with Qwen cell replay `784.040s -> 3.496s` and SmolLM2 cell replay `336.829s -> 2.797s`. `eval_frontier_stability.py` adds the post-hoc multi-seed check without reloading models; the registered report `report:truthfulqa-frontier-qwen-smollm2-l80-stability:0.1` and verified manifest `benchmark_manifest:truthfulqa-frontier-qwen-smollm2-l80-stability:0.1` replay seeds `0..9`, fingerprint both score manifests and JSONL record sidecars, show `truth_proj` is best in 10/10 seeds for both Qwen l80 and SmolLM2 l80 while beating the best `mean_rank` ensemble in 10/10 seeds, and now report shared score-dump cache reuse with 18/22 JSONL selected-view lookups served from cache. Mean single-minus-ensemble detection margin is `0.034` for Qwen and `0.053` for SmolLM2. `eval_score_ensemble.py` now also records the geometry-fusion replay at `artifacts/truthfulqa-frontier-qwen-smollm2-l80-geometry-fusion/`: with `nll_answer` as the only available uncertainty proxy, geometry-by-uncertainty fusion remains below `truth_proj` and naive rank fusion, so it is not promoted as a default internal policy. The staged structured-QA verifier-signal replay at `artifacts/truthfulqa-l80-staged-qa-verifier-signals/` converts verified records into score-dump columns and saves per-model geometry-fusion artifacts; SmolLM2 geometry fusion improves detection to `0.261` at false alarm `0.095`, beating both `truth_proj` and `verifier_refuted`, while Qwen is led by `verifier_refuted` with zero false alarm. The local retrieval verifier-signal replay at `artifacts/truthfulqa-l80-local-retrieval-verifier-signal-fusion/` uses the no-model workflow with the local correct-answer corpus and no label metadata in claims; it verifies the manifest, keeps verified false alarm at `0.016`, reaches verified detection `0.316` for Qwen and `0.267` for SmolLM2, and its `noisy_or` geometry fusion reaches detection about `0.795` at false alarm about `0.070` for both models. Treat that as a controlled local-corpus baseline, not open-domain retrieval evidence. The answer-echo stress control at `artifacts/truthfulqa-l80-answer-echo-retrieval-stress/` shows the opposite failure mode: a corpus built from the audited answers covers every record but supports false claims at rate `0.980`, so answer-derived retrieval evidence must be rejected as grounding. The text/length redline at `artifacts/truthfulqa-l80-text-baseline-comparison/` shows cheap controls do not explain the current `truth_proj` signal: at alpha `0.100`, `answer_token_count` reaches only `0.110` detection with AUROC `0.519`, `claim_token_count` AUROC is `0.527`, and low question/answer overlap triggers no detections under the calibrated gate. `eval_verifier_stability.py` applies the same post-hoc stability check to staged structured QA verifier routing on the current frontier score dumps; `report:truthfulqa-frontier-qwen-smollm2-l80-verifier-stability:0.1` keeps verified false alarm within budget in 10/10 seeds and improves detection in 10/10 seeds for both models with only 115 and 95 structured-QA calls per run. Its default run-local verifier trace cache avoids rerunning seed-invariant verified records after the first seed, with 9/10 trace-cache hits per model in the current report. The accompanying rank-fusion, internal-stability, geometry-fusion, verifier-signal fusion, retrieval-stress, text-baseline, and verifier-stability reports show internal score ensembles still trail single `truth_proj`, cheap lexical artifacts are insufficient, answer-echo retrieval is invalid grounding, while staged external evidence is the current path that improves detection under the calibrated false-alarm budget.
- `compare_frontier_release_evidence.py` combines the l80 staged verifier-stability report with the l80 abstention-stability report into one fail-closed frontier release verdict, and can now accept `--frontier-workflow-report` to require the multi-cell TruthfulQA frontier workflow's `multiple_testing_gate.all_pass` evidence before release promotion. Its multiple-testing track preserves failed and unknown cell summaries in the release report, registry metadata, and downstream evidence-gap plan so reruns can target the exact model/scale cells rather than treating the family-wise gate as one opaque blocker. The release gate rejects summary-only multiple-testing evidence: `cell_count`, pass/fail/unknown counts, `cells` length, and each cell's report/calibration artifact paths must agree before the track can promote. It can also accept `--citation-batch-rollup-report` and will add a `citation_batch_track_status` that blocks release when expected citation/source-family evidence batches are missing, duplicated, unexpected, unsupported, child-gate failed, or child-manifest failed. The current evidence promotes the verifier track but blocks the abstention track because participation-gate conservative conditional-correctness lower bounds remain below the `0.8` release threshold, so the overall frontier release-evidence decision is blocked until the participation signal improves.
- The detectability-gated frontier replay `report:truthfulqa-frontier-qwen-smollm2-l80-detectability:0.1` reuses the current l80 score dumps and emits per-cell DECK reports in 7.5s. With `eigenscore` and `nll_answer` interpreted as lower-is-risk axes, Qwen has entrenched false-rate `0.000`, while SmolLM2 has `89/306 = 0.291`; the paired release artifact `report:truthfulqa-frontier-qwen-smollm2-l80-release-evidence-detectability:0.1` therefore keeps verifier stability promoted, keeps abstention blocked, and adds a SmolLM2 detectability blocker at the default `0.25` blind-spot gate.
- `report:truthfulqa-frontier-smollm2-l80-entrenched-blind-spots:0.1` exports those 89 SmolLM2 false entrenched records with row-level score/claim-feature/question-type metadata. The largest clusters are definition/what (`39`), person (`13`), and choice (`8`) questions, so the next product correction work should target fact lookup, entity disambiguation, and choice-question verification before trying to promote a broader participation gate.
- `report:truthfulqa-frontier-smollm2-l80-blind-spot-route-audit:0.1` aligns the 89 entrenched false records with the existing `retrieval_structured_qa` verifier sidecar. The target route selects and refutes `3/89`, supports `0/89`, and leaves `86/89` outside that route, while artifact-manifest verification passes. This keeps the route as a high-precision correction path for covered records but blocks any claim that it solves the current detectability blind spot; the product roadmap should widen structured retrieval/fact/citation/world-model coverage next.
- `report:truthfulqa-frontier-smollm2-l80-blind-spot-query-sweep:0.1` shows that query construction is the immediate lever: under the controlled correct-answer corpus, `question_answer@0.65` refutes `87/89` blind spots with verified false alarm `0.000`, and `question_answer@0.5` refutes `89/89` with verified false alarm `0.000`; `question@0.95` also covers all blind spots but fails the false-alarm budget (`0.176`). This should become an external/structured-corpus rerun requirement before changing runtime defaults.
- `report:truthfulqa-frontier-smollm2-l80-query-sweep-provenance-comparison:0.1` enforces that requirement and is blocked: the controlled sweep passes, but Wikidata country-core-facts external retrieval and structured-QA retrieval sweeps both refute `0/89` blind spots. Product defaults should therefore keep the current fail-closed behavior and prioritize broader external/structured-fact coverage rather than adopting controlled-corpus query settings.
- `report:truthfulqa-frontier-smollm2-l80-blind-spot-evidence-expansion-plan:0.1` is the product backlog for that coverage work: all 89 targets get collection tasks, 65 are high priority, and the dominant recommended routes are structured facts (`80`), structured QA (`65`), citation retrieval (`63`), counterfactual probes (`41`), and world-model/calculator checks (`21`). Runtime behavior should not change until newly collected evidence makes the provenance gate pass.
- `eval_truthfulqa.py --auto-batch-size` can halve and retry warmup/forced-answer batch size after retriable memory errors, recording requested/effective batch size and reduction events in JSON/profile output.
- `eval_truthfulqa.py --max-batch-tokens` caps padded tokens per warmup/eval forward while preserving `--batch-size` as the row-count cap; triplet, matrix, and readiness workflows pass it through for same-machine performance gates. `run_cache_profile_matrix.py --max-batch-token-budgets` can compare several token budgets in one triplet matrix and ranks those cells by uncached forced-answer forward time.
- `eval_truthfulqa.py --covariance-mode {full,diag,low_rank,shrinkage}` controls the `maha_last` TruthManifold covariance approximation. `full` is the backward-compatible default, `diag` keeps only diagonal Welford scatter statistics for low-memory local sweeps, `low_rank` scores with a ridge plus top-K approximation, and `shrinkage` uses an OAS-style covariance estimate shrunk toward scaled identity before the existing ridge regularizer. Layer-stats caches validate covariance mode/rank, and calibrated thresholds should be regenerated after changing modes. `--include-layer-spectra` optionally adds compact per-layer Marchenko-Pastur/effective-rank spectrum diagnostics and shrinkage alpha to JSON reports without changing score-dump schemas; `compare_spectrum_layers.py` checks whether those spectrum heuristics actually predict sweep AUROC top-k layers before promoting them as layer-selection shortcuts. The current l80 spectrum-layer audit at `artifacts/truthfulqa-frontier-spectrum-layer-selection/` blocks promotion of a spectrum-only selector: the best heuristic hits `truth_proj` AUROC top-2 in 1/2 runs and exact-best in 1/2 runs. `compare_layer_band_selectors.py` now tests intrinsic-dimension and spectrum heuristics as conservative sweep bands; the current l80 artifact at `artifacts/truthfulqa-frontier-layer-band-selection/` recommends `spectrum_max_top_eigenvalue_to_mp_upper_radius_1`, which keeps both models' best `truth_proj` layer in band with zero AUROC regret while averaging 2 of 5 monitored layers. Treat this as a cost-reduction prior before calibrated sweep, not a standalone deployment selector. `rebuild_layer_stats_from_warmup_checkpoint.py` derives covariance-specific layer-stats caches from saved warmup hidden states, letting l80 covariance gates reuse existing warmup checkpoints and eval-reps caches without model-forward reruns. The current Qwen/SmolLM2 l80 gate artifact is `artifacts/truthfulqa-frontier-covariance-gate-l80/covariance-mode-gate-report.json`: `shrinkage` is the cross-model quality-preserving candidate; `diag` is rejected for both models.
- `run_cache_profile_matrix.py --max-workers` and `run_adapter_readiness_workflow.py --performance-max-workers` can execute independent performance matrix cells concurrently. The default remains serial; shared-cache refresh cells are still run behind a conservative barrier before dependent warm-start/cache-only cells are parallelized. Matrix/readiness reports record end-to-end wall-clock seconds so worker-count comparisons are auditable.
- `run_cache_worker_sweep.py` wraps the cache-profile matrix across several worker counts, isolates shared-cache roots per worker count, and recommends the fastest promoted worker count by end-to-end wall-clock time.
- `eval_truthfulqa.py --prefix-kv-cache` is an experimental shared-prefix forced-answer path for eval scoring: repeated question prefixes are run once and answer continuations reuse the prefix KV cache while preserving answer-window hidden states and NLL semantics. It is off by default; use cache-profile matrices with `--prefix-kv-cache-modes off,on`, inspect `prefix_kv_comparisons` / `forced_answer_forward_seconds` for forward-speed claims, and promote only through profile/readiness gates.
- `eval_truthfulqa.py --statement-encoding-cache` persists tokenizer outputs and answer-span lengths as validated JSON so repeated benchmark/cache rebuilds avoid redundant tokenizer setup without changing scoring semantics.
- `AdaptiveScoreTransform` / `AdaptiveConformalCalibrator` adds a dependency-free adaptive conformal scoring layer for ACSE-style experiments: callers can inflate native diagnostics with semantic entropy, cluster, prompt-risk, or verifier-route features, then save the adjusted higher-is-anomalous score as a normal calibration artifact without changing score-dump schemas. `eval_conformal.py --adaptive-feature ... --save-adaptive-calibration ...` now wires this into score-dump workflows by loading adaptive features from selected primary scores, JSON extras, JSONL manifest extras, or JSONL per-record extras and emitting an `adaptive_conformal_report` next to the base conformal report.
- Sharded `--eval-reps-cache` readers reuse recently touched shards through a default 2-shard read-side LRU cache and use a manifest index to seek directly to the first touched shard for range reads, reducing repeated `torch.load` calls and avoidable manifest scans during small-batch cache-only or cached scoring runs; `--eval-reps-shard-read-cache-size 1` restores the previous single-shard memory profile, and triplet/matrix/worker-sweep/performance-baseline profile runners pass this setting through for cached/cache-only runs. `run_cache_profile_matrix.py --eval-reps-shard-read-cache-sizes 1,2,4` promotes read-cache capacity into a gated matrix dimension with shared eval-reps cache artifacts and cell-level runtime recommendations. JSON output includes `cache_stats.eval_reps_reader` counters for read requests, records read, shard read requests, cross-shard reads, shard loads, shard cache hits, and shard manifest scans when an eval-reps reader is used, and profile summary adds `cache_efficiency.eval_reps_reader` hit-rate/read-shape/manifest-scan metrics.
- Current small CPU SmolLM2 l8 performance evidence registers `performance_baseline:smollm2-l8-read-cache-worker-sweep-score-fusion-performance-baseline:0.2`: read-cache size `2` remains the promoted cache-only setting, worker count `2` lowers same-machine matrix wall-clock from `184.467s` to `141.385s`, the selected cell keeps `truth_proj` AUROC `0.830`, and the runtime recommendation carries conformal-gated `score_fusion_mean_rank` evidence from the l80 score-ensemble report (`AUROC=0.679`, false alarm `0.090`, detection `0.196`). The selected-fusion handoff is now recorded as `performance_baseline:smollm2-l8-read-cache-worker-sweep-selected-fusion-performance-baseline:0.3`: it reuses the same runtime cell, explicitly selects the SmolLM2 selected-fusion artifact (`geometry:mean_rank`), records promoted `selected_fusion_mean_rank` evidence (`AUROC=0.692`, false alarm `0.029`, detection `0.224`), and keeps `truth_proj` as the best quality signal. The score-fusion evidence promotes `benchmark_manifest:smollm2-l8-read-cache-worker-sweep-score-fusion-staged-qa-release-candidate:0.2` through the staged structured-QA release gate, while the selected-fusion evidence now promotes the parallel `benchmark_manifest:smollm2-l8-read-cache-worker-sweep-selected-fusion-staged-qa-release-candidate:0.3` gate and compact handoff `product_promotion_contract:smollm2-l8-selected-fusion-product-promotion-contract:0.3`. Release manifest/registry metadata expose score-fusion status, signal, AUROC, conformal-gate result, and source score-ensemble report; selected-fusion performance/release/contract metadata expose selected-fusion status, run, signal, AUROC, false alarm, detection, and selected artifact path. Treat this as local CPU deployment evidence, not a cross-hardware default.
- `compare_profiles.py` compares profile JSON payloads across baseline/cache/cache-only runs, reporting speedup, phase deltas, grouped time deltas, throughput ratios, and cache-efficiency deltas such as eval-reps shard hit rate / cross-shard read rate without loading models. Optional regression gates can fail CI/local checks when total time, phase time, or throughput ratios exceed configured limits, while cache-efficiency fields remain explanatory report metadata and are propagated into cache-profile matrix cell summaries.
- `run_cache_profile_triplet.py` is the optional local performance harness: it runs uncached, cached, and cache-only `eval_truthfulqa.py` profiles on the same machine and writes a gated `compare_profiles.py` report. The default path uses the offline fixture, while `--real-truthfulqa` generates representative model/data profile artifacts with configurable model, dtype, layer, limit, and warmup size.
- `run_inside_sampling_profile.py` compares fixed INSIDE sampling, adaptive entropy-stability sampling, and adaptive self-check-bound sampling on the same machine, writing per-run result/profile JSON plus a sample-efficiency comparison report so generated-sample savings and `inside_generation` time ratios can be promoted as reproducible artifacts. Optional shared statement/layer/eval caches let comparable sampling runs avoid repeated tokenization, warmup, and forced-answer forward work while still running real INSIDE generation for sampled records.
- `run_inside_trigger_budget_sweep.py` wraps that profile runner across several `--inside-trigger-*` budgets, writing one sweep report with generated samples, `inside_generation`, ratios to an optional full-sample reference, inside-score AUROCs, a cost-first recommendation, and a quality-balanced recommendation that chooses the cheapest budget within a small AUROC tolerance of the best INSIDE quality signal. `--shared-cache-dir` applies the same cache reuse across all budget children. For nested top-fraction budgets with one run, `--derive-from-max-budget` executes only the largest budget and derives smaller rows from the score dump while preserving batch-local top-fraction semantics.
- `run_cache_profile_matrix.py` expands the triplet harness across layer, batch-size, hidden-state-capture, and TruthManifold covariance-mode combinations, writing a single same-machine matrix report with command logs, gate summaries, cache-only timing, AUROC quality signals, wall-clock execution metadata, and a fail-closed matrix-level performance promotion decision. Shared-cache runs reuse statement encodings and eval hidden-state caches across covariance modes while keeping layer-stats caches separated by covariance mode/rank. Covariance sweeps should use the default `triplet` matrix mode; `rescore` is reserved for cache-only variants that share the same layer-stats scoring semantics. In `rescore` mode, later cache-only cells in the same shared-cache group are gated against the first cell's uncached baseline so post-processing/cache-only variants can be promoted without repeating model forward passes.
- Current small SmolLM2 l8 covariance evidence (`layer=-12`, `batch_size=4`, `max_batch_tokens=512`, `limit=8`, `manifold_questions=4`) shows a real tradeoff: `diag` is fastest for cache-only replay (`0.156s` versus `0.211s` for `full`) but lowers `maha_last` AUROC from `0.614` to `0.500`; `low_rank` rank 8 keeps `maha_last` close to `full` (`0.610`) with cache-only replay `0.197s`. Runtime recommendations now emit `covariance_tradeoff`, and readiness/release gates can enforce `--max-covariance-maha-last-auroc-drop` so speed-only covariance choices do not silently replace `maha_last`-quality-preserving choices.
- `profile_gate_smoke.py`, `cache_profile_smoke.py`, `score_fusion_profile_smoke.py`, `inside_sampling_profile_smoke.py`, `cache_worker_sweep_smoke.py`, `registry_baseline_smoke.py`, `concept_registry_smoke.py`, `performance_baseline_smoke.py`, `product_trace_replay_smoke.py`, `release_candidate_registry_smoke.py`, and `make perf-check` provide deterministic no-model smoke checks for profile regression, cache/cache-only gate machinery, score-fusion runtime caching, INSIDE sampling sample-efficiency gates, worker-count sweep decisions, registry-backed baselines, concept artifact/multi-probe handoff, performance-baseline handoff bundles, ProductTrace replay handoff plus action-audit gate and bounded-telemetry rejection, and release-candidate registry promote/block behavior; real performance claims still require representative `eval_truthfulqa.py --profile-json` artifacts.
- `recommend_runtime_config.py` turns promoted cache-profile matrix, optional worker-sweep reports, optional INSIDE sampling profile reports, optional trigger-budget sweep reports, optional score-ensemble reports, and optional selected-fusion-artifact build reports into one deployable runtime recommendation with layer, batch size, capture mode, covariance mode/rank, token budget, prefix-KV mode, worker count, sampling configuration, selected trigger budget, gated best AUROC quality signal, covariance tradeoff details, cache-tuning advice, and equivalent benchmark flags. It emits cache-tuning recommendations when eval-reps shard hit rate is low, cross-shard reads are high, or each cache read returns too few records; when `eval_reps_shard_read_cache_sizes` has already compared several capacities, it records the swept sizes and selected read-cache size instead of emitting contradictory read-cache heuristic advice. Score-fusion evidence only enters `quality_signals` when the selected ensemble alpha passed its conformal false-alarm gate; blocked or ambiguous fusion evidence is retained for audit without changing the runtime recommendation. Selected-fusion artifacts likewise only enter `quality_signals` after an explicit single-run selection, so multi-run reports require `--selected-fusion-run` and otherwise fail closed as ambiguous audit evidence. It supports explicit trigger-budget selection policies: `quality_balanced` preserves the default release posture, `cost_first` minimizes sampled INSIDE work for latency-constrained deployments, and `quality_first` chooses the highest measured INSIDE quality metric. It preserves `--derive-from-max-budget` in generated sweep flags, performs no model work, and should be treated as the final handoff from same-machine performance evidence to local deployment defaults.
- `run_performance_baseline_workflow.py` wraps the runtime-recommendation handoff into a fingerprinted, registry-ready performance baseline bundle. It can run or reuse cache-profile matrix evidence, optional worker-count sweeps, optional INSIDE sampling / trigger-budget reports, optional score-ensemble reports, and optional selected-fusion-artifact build reports, then writes `performance-baseline-workflow.json`, `runtime-recommendation.json`, an artifact manifest, a top-level `performance_evidence_bundle` summary for recommendation cost ratios / evidence status / artifact readiness / score-dump cache evidence / score-fusion / selected-fusion status, and an optional `performance_baseline:*:*` registry record. When reusing a matrix report, the top-level config, performance evidence runtime block, and manifest metadata inherit the matrix report's effective runtime settings for reproducibility. With `--verify-manifest`, it writes a separate recursive manifest verification report and records `manifest_verification:<name>-verification:<version>` next to the performance baseline. Dry-run outputs remain `needs_evidence` so command inspection cannot be mistaken for promoted performance evidence.
- `run_product_runtime_baseline.py` aggregates saved `ProductTrace` JSON payloads into a request-runtime baseline, including phase timing distributions, verifier-route cost summaries, retrieval-use rates, cache hit rates, staged-verification saved/verified claim counts, verification-plan coverage/route/tool-payload counts, action-execution planned/result alignment, action-receipt coverage/signed/unsigned/fingerprint-mismatch rates, verification-budget exhaustion summaries, claim-risk localization summaries, trace-level world-model participation/conflict/low-agreement/trace-gap summaries, trace-level context-sensitivity participation/coverage/flagged/shift/ratio summaries, trace-level counterfactual robustness participation/pass/flip/false-invariance summaries, triple/slot-audit coverage summaries, final-answer coverage/status/action summaries, promotion-contract external-evidence baseline-comparison handoff coverage/status/route/text-redline summaries, promotion-contract pre-generation probe comparison/redline handoff summaries, promotion-contract counterfactual verifier-audit handoff coverage/status/pass-rate/false-invariance summaries, promotion-contract covered-fact property scope plus per-property quality rollups for recommended-route, required-route baseline, and structured-fact robustness evidence, optional `ProductRuntimeBudgetPolicy` / `ProductPromotionContract` gate results, an artifact manifest, and an optional `product_runtime_baseline:*:*` registry record. It supports `--save-recommended-policy` to materialize the optimization hint as a directly reusable `ProductRuntimeBudgetPolicy` artifact, `--trace-records-jsonl` to stream per-trace metric/budget records into a sidecar instead of embedding them in the main report, `--trace-records-cache-json` to reuse compact metric/budget records across repeated budget sweeps when source trace fingerprints and the resolved policy payload match, `--trace-scan-workers` to parallelize trace JSON scan and metric extraction for large local trace sets while recording configured/effective worker counts, supports `--compact-json` for minified automation artifacts, performs no model or verifier work, and is intended to validate the product control path separately from model/cache performance recommendations.
- `run_product_feedback_report.py` joins saved full `ProductTrace` payloads with `ProductFeedbackRecord` JSONL labels, preferring trace fingerprint matches and falling back to unique request ids. It reports matched/unmatched feedback, accepted-but-wrong, retrieved-failure, retrieved-but-still-unsupported, abstain-false-positive, final-answer answered-but-wrong, and final-answer false-block metrics with optional fail-closed product gates, manifest provenance, and registry metadata. It performs no model, verifier, retrieval, or database work.
- `recommend_control_policy_from_feedback.py` consumes one or more product feedback reports and emits a deterministic candidate `ControlPolicyConfig` plus optional runtime control-default artifact. Accepted-but-wrong and final-answer answered-but-wrong feedback increase the safety posture, retrieval failures can move unsupported claims toward clarification or abstention, and abstain/final-answer false-block feedback can de-escalate compound unsupported decisions only when safety feedback is not also elevated.
- `audit_feedback_policy_replay.py` audits that candidate policy against historical feedback labels without pretending to rerun the full controller. It reports counterfactual safety coverage, residual safety issues, unknown claim-metadata gaps, overblock relief, manifest provenance, and registry metadata so feedback-driven policy changes can be gated before rollout.
- `run_feedback_policy_workflow.py` chains the same three feedback-policy steps into one reproducible artifact bundle: feedback join report, candidate policy/default artifacts, inline candidate `ControlPolicyConfig` and control-default config, replay audit, top-level manifest, action-level plus final-answer feedback rates, and optional local registry record. Its top-level status is `recommend`, `observed`, `needs_evidence`, or `blocked`; registered `report:<name>:<version>` records can be consumed by release-candidate gates and exported promotion contracts; it performs no model, verifier, retrieval, database, or network work.
- `ProductTrace.to_bounded_dict()` and `calibrated_control_demo.py --bounded-trace` emit smaller online telemetry payloads by keeping diagnostics, routing decisions, summaries, selected artifact/contract metadata, verification-budget counts, claim-risk localization summaries, final-answer summaries, and truncation counts while bounding long claim, verifier, event, action-result, and final-answer evidence/text fields. Bounded trace generation reuses one prepared normalized trace payload for summaries and bounded fields, avoiding repeated verifier/action/event normalization on large traces. Bounded traces are for product telemetry; corpus, replay, and runtime-baseline tools reject them by default, so full `ProductTrace.to_dict()` payloads remain the reproduction and runtime-baseline input.
- `compare_product_runtime_baselines.py` compares a fresh ProductTrace runtime baseline against a file or registered `product_runtime_baseline:*:*` record, writing a manifest-backed drift report with fail-closed gates for request latency, route duration, attempted-route count, retrieval-use drift, cache-hit-rate drop, verifier-skip-rate drop, trace-level world-model participation/coverage/conflict/low-agreement/trace-gap drift, trace-level context-sensitivity participation/coverage/flagged-rate/trace-gap/max-ratio drift, promotion-contract coverage, pre-generation probe comparison coverage/manifest/redline/AUROC handoff drift, claim factuality probe comparison coverage/manifest/redline/AUROC/selective-accuracy/selective-coverage handoff drift, counterfactual verifier-audit coverage/manifest/pass-rate/false-invariance/flip-success handoff drift, covered-fact per-property metric count/record/source-doc and quality degradation, triple-extraction fixture-matrix coverage/quality drift, product-trace action-audit/action-execution gate rate drift, action-receipt coverage/missing/invalid/fingerprint-mismatch/unsigned receipt drift, trajectory-audit failed-trace/error/factual/referential/logical/procedural/scope drift, trace-level claim-triple extraction coverage, triple-audit claim coverage, triple-audit pass rate, slot coverage, and minimum current trace count. The runtime baseline now also aggregates trajectory-audit taxonomy counts, trace-level world-model summary rates, trace-level context-sensitivity summary rates, and promotion-contract evidence-handoff manifest verification, missing-metric before/after counts, present/missing metric rates, filled groups, and per-group status counts into the report, artifact manifest, and registry metadata. It can also consume a saved `ProductRuntimeBudgetPolicy` by file path or `product_runtime_budget_policy:*:*` registry key and apply it to the current aggregate baseline summary using p95/aggregate metrics, so recommended runtime policies can become the next run's promotion gate. It records optional `product_runtime_drift_report:*:*` registry metadata and gives the product loop a local continuous-assurance check before selector or release-candidate defaults are trusted on shifted traffic.
- `build_product_trace_corpus.py` turns raw saved ProductTrace JSON/JSONL into replay-ready corpus artifacts: it validates risk decisions and optional runtime traces, redacts text-like claim/evidence/final-answer fields by default, adds stable `metadata.runtime_replay_key` values, writes standardized trace files, emits a manifest-backed `runtime-pair-index.json` for paired-runtime replay, summarizes final-answer status/action coverage, can reuse per-source validation/redaction results with `--source-cache-json`, and can register the corpus. The current registered SmolLM2 corpus is `report:smollm2-product-trace-corpus:0.1`, with 12 accepted deterministic profile-sweep traces, no rejects, full runtime coverage, and all claim/evidence text redacted before replay.
- `run_product_trace_replay_workflow.py` wraps the raw-trace handoff into one reproducible workflow: build a redacted trace corpus plus runtime-pair index, run a ProductTrace runtime baseline, run selector replay over the standardized traces using that index, optionally run action-audit thresholds over missing retrieval actions, uncovered planned retrieval queries, and malformed/unexpected action payloads, optionally run action-execution thresholds over missing results, unexpected results, and request-id mismatches between planned actions and `ActionResult` payloads, optionally run the current runtime baseline through `compare_product_runtime_baselines.py` as a product-runtime drift/policy/promotion-evidence/pre-generation-probe/claim-factuality-probe/counterfactual-verifier/action-gate/world-model/context-sensitivity drift gate, recursively manifest all child reports, record phase timing/cache summaries for local performance tuning, lift the runtime baseline `optimization` status/recommendations/policy hints into the top-level report and registry metadata, optionally materialize the baseline's recommended runtime budget policy, and register a single workflow report. With `--verify-manifest`, it writes a separate recursive verification report and records `manifest_verification:<name>-verification:<version>` beside the workflow report; with `--fingerprint-cache`, repeated local manifest checks reuse unchanged artifact fingerprints across runs; with `--corpus-source-cache-json`, repeated raw-trace handoffs reuse unchanged source validation/redaction results before rebuilding corpus outputs; with `--corpus-cache-json`, repeated raw-trace handoffs reuse the already standardized corpus when source fingerprints, output fingerprints, and corpus config still match; with `--runtime-trace-records-cache-json`, repeated runtime budget sweeps reuse compact trace-record metrics; with `--runtime-trace-scan-workers`, the child runtime baseline can parallelize standardized trace scanning while preserving configured/effective worker evidence in the workflow report, manifest, and registry metadata; with `--save-runtime-recommended-policy`, the workflow saves a reusable `ProductRuntimeBudgetPolicy` candidate from the observed baseline; with `--runtime-drift-baseline` and optional `--runtime-drift-budget-policy`, the workflow writes and registers a child `product_runtime_drift_report:*:*` gate before release-candidate evaluation, including optional world-model participation/coverage/conflict/low-agreement/trace-gap gates, context-sensitivity participation/coverage/flagged-rate/trace-gap/max-ratio gates, pre-generation probe comparison coverage/manifest/redline/AUROC gates, claim factuality probe comparison coverage/manifest/redline/AUROC/selective-accuracy/selective-coverage gates, and counterfactual verifier-audit coverage/manifest/pass-rate/false-invariance/flip-success gates; with `--selector-trace-inputs-json`, repeated selector replay sweeps reuse a minimal trace-input cache keyed by source trace fingerprints. The current registered workflow is `report:smollm2-product-trace-replay-workflow:0.1`; it promotes the default selector from 12 redacted traces with full paired-runtime coverage while keeping the runtime baseline as observed evidence unless an explicit runtime budget or promotion contract is supplied, and its verification record is `manifest_verification:smollm2-product-trace-replay-workflow-verification:0.1`.
- `run_product_runtime_profile_sweep.py` runs deterministic calibrated-control demo scenarios across `latency`, `balanced`, `audit`, and request-level `auto` selection modes, writes trace artifacts, builds one ProductTrace runtime baseline per mode, records actual selected profiles for `auto`, ranks non-blocked modes by request runtime and route cost, and can register the sweep report. It supports `--max-workers` mode-level concurrency while keeping trace order deterministic inside each mode; use the default single-worker mode for promotion timing evidence and multi-worker mode for faster smoke/coverage scans. It also supports `--compact-json` across generated traces, baselines, reports, and manifests without changing payload schemas. `--runtime-profile-selector-policy` configures request-time auto routing, `--policy` keeps applying trace-level `ProductRuntimeBudgetPolicy` gates through each per-mode baseline, and `--slo-policy` adds aggregate profile SLO gates for p95 request latency, attempted-route count, retrieval-use rate, verifier skip/claim cost, and auto selector distribution. It is the lightweight local path for deciding which staged-verification mode should become the default product posture before collecting real traffic traces. Skipped verifier stages with no selected route now count as zero route cost for route-budget checks, while missing runtime traces still fail closed when runtime phase/total thresholds are configured. The current registered sweep is `report:smollm2-product-runtime-profile-sweep:0.1`, which promotes the three static profiles plus `auto` against the strict product runtime budget and the local profile SLO policy, then recommends `auto` on the deterministic scenario set.
- `run_runtime_profile_selector_tuning.py` compares multiple `RuntimeProfileSelectorPolicy` candidates by materializing each policy, running `run_product_runtime_profile_sweep.py --profiles auto` under the same SLO gate, ranking non-blocked candidates by request runtime and verifier cost, writing a top-level artifact manifest, and optionally registering the tuning report. The current registered selector tuner is `report:smollm2-runtime-profile-selector-tuning:0.1`; it promotes the default selector and blocks latency-biased and audit-biased alternatives because they fail the auto selector distribution SLO.
- `run_runtime_profile_selector_replay.py` replays multiple `RuntimeProfileSelectorPolicy` candidates over saved `ProductTrace` JSON without rerunning demos, verifiers, retrieval, or model work. It uses each trace's `risk_decision` and claim metadata to estimate selected-profile distribution, switches from the original runtime profile, configurable profile-cost units, paired observed runtime when the same logical request is available under multiple profiles, selected-vs-original runtime deltas, optional `--runtime-pair-index` reuse from a trace corpus, optional `--trace-inputs-json` reuse of minimal trace replay inputs, optional replay gates, an artifact manifest, and an optional `report:*:*` registry record. This is the cheaper path for validating selector changes against captured product traces once demo sweeps have established the baseline policy shape.
- `compare_readiness_baselines.py` compares registered adapter-readiness manifests without rerunning model work: it recursively verifies each manifest, regenerates runtime recommendations from saved performance matrices and optional INSIDE sampling profile / trigger-budget sweep artifacts when possible, applies optional quality/runtime/covariance-quality/sampling-cost gates, uses uncached total time as a conservative forward-cost fallback for legacy reports, and recommends the passing baseline with the best AUROC quality signal. Sampling gates use profile `*_to_baseline` ratios when present and trigger-sweep `*_to_reference` ratios when baseline ratios are absent; reports record the ratio source. Trigger-budget sweep selection can be explicitly overridden with `quality_balanced`, `cost_first`, or `quality_first`; otherwise it follows the recorded readiness/runtime policy and falls back to the quality-balanced release posture.
- `compare_release_candidates.py` combines registered readiness, verifier-route, optional performance-baseline records, optional product-trace-replay workflow reports or `report:*:*` registry keys, optional selfcheck-signal-fusion workflow reports or `report:*:*` registry keys, optional world-model-signal workflow reports or `report:*:*` registry keys, optional feedback-policy workflow reports or `report:*:*` registry keys, optional selector-replay reports, optional product-runtime-drift reports, optional external-evidence baseline comparison reports or `report:*:*` registry keys, optional pre-generation probe comparison reports or `report:*:*` registry keys, optional claim factuality probe comparison reports or `report:*:*` registry keys, and optional adapter-family matrix evidence into the final local release gate: both underlying baseline comparisons must promote, including any readiness covariance-quality gates, sampling-cost gates, recommended-runtime cost gates, route runtime/cache budget gates, optional covered-fact per-property gates, and optional route/required-route retrieval provenance-filter gates; repeated `--required-route-baseline-key` values can require additional promoted evidence without making those audit routes the selected low-latency route; `--release-policy-profile strict_structured_fact` now separates ordinary required route baselines from canonical/paraphrase `structured_fact` robustness baselines, applying route-quality/provenance/stress gates to ordinary required routes while applying the stricter selected-count and covered-fact per-property gates only to the structured-fact pair, while `--release-policy-profile frontier_audit` additionally defaults `max_recommended_runtime_seconds=1.0`, `adapter_family_profile=strict_audit`, `require_product_runtime_drift_promotion_evidence=true`, `require_product_runtime_drift_pre_generation_evidence=true`, `require_product_runtime_drift_claim_risk_localization_evidence=true`, `require_product_runtime_drift_counterfactual_evidence=true`, `require_product_runtime_drift_triple_audit_evidence=true`, `require_product_runtime_drift_covered_fact_property_evidence=true`, `require_product_runtime_drift_action_gate_evidence=true`, `require_product_runtime_drift_trajectory_audit_evidence=true`, `require_product_runtime_drift_evidence_handoff_evidence=true`, `require_product_runtime_drift_world_model_evidence=true`, `require_product_trace_action_audit_gate=true`, and `require_product_trace_action_execution_gate=true`; a supplied `performance_baseline:*:*` key must verify, match the selected runtime recommendation, require `performance_evidence_bundle.release_ready=true` when that bundle exists, and can optionally gate covariance `maha_last` quality drop, score-dump cache evidence / selected JSONL view hit rate, plus performance trend against an explicit prior `performance_baseline:*:*` key; supplied product-trace-replay workflow, selector replay, product runtime drift, selfcheck-signal-fusion workflow, world-model-signal workflow, external-evidence baseline comparison, pre-generation probe comparison, and claim factuality probe comparison reports must verify or self-identify with the expected workflow/status and promote, with product-trace-replay also required to include promoted action-audit/action-execution child gates when `--require-product-trace-action-audit-gate` or `--require-product-trace-action-execution-gate` is set, selfcheck evidence failing closed when its sample-quality gate fails, world-model signal evidence failing closed on nonzero trace gaps, missing positive conflict examples, or missing calibrated conflict-signal evidence, external-evidence comparison evidence failing closed when route or text-redline sub-gates explicitly fail, and claim factuality comparison evidence failing closed when multi-model, conformal, manifest, or text-redline gates fail; supplied feedback-policy workflow reports must verify, have `workflow=feedback_policy_workflow`, have `status=recommend` or `status=observed`, carry a valid promotion decision, include a parseable `candidate_control_policy_config` when recommending a candidate policy, and can be gated on matched feedback count, safety coverage, and unknown safety issue rate; and a supplied adapter-family matrix must promote with all required routes present and promoted. `--adapter-family-profile strict_audit` expands the required adapter routes to `structured_state`, `state_transition`, and `triple_evidence`, and now automatically fails closed unless the matrix proves `state_transition` used `RuleBasedWorldModelAdapter` with a positive rule count, so strict audit releases do not rely on callers remembering each route or world-model evidence flag. `--require-state-transition-world-model` applies the same world-model evidence gate to custom adapter route sets. `--require-product-runtime-drift-promotion-evidence` fails closed unless the product-runtime drift report contains promotion-contract coverage and triple-extraction fixture-matrix coverage/quality metrics; `--require-product-runtime-drift-pre-generation-evidence` fails closed unless it contains pre-generation probe comparison coverage, manifest, model/run, redline, AUROC, and margin metrics; `--require-product-runtime-drift-counterfactual-evidence` fails closed unless it contains counterfactual verifier-audit coverage, manifest, record-count, pass-rate, false-invariance, and flip-success metrics; `--require-product-runtime-drift-triple-audit-evidence` fails closed unless the drift report contains trace-level triple coverage, audit coverage, audit pass-rate, and slot coverage metrics; `--require-product-runtime-drift-action-gate-evidence` fails closed unless the drift report contains product-trace action-audit/action-execution drift metrics; `--require-product-runtime-drift-evidence-handoff-evidence` fails closed unless the drift report contains promotion-contract evidence-handoff coverage, manifest-verification, present/missing metric rate/count, blocked-group, and promoted-group metrics; `--require-product-runtime-drift-world-model-evidence` fails closed unless the drift report contains trace-level world-model participation, coverage, conflict, low-agreement, and trace-gap metrics. When `--product-trace-replay-workflow-key` or `--product-trace-replay-workflow` is supplied, the release gate resolves its selector-replay, runtime-drift, action-audit, and action-execution child report paths unless explicit child paths override them. The report emits one release candidate with runtime flags, verifier route, covered-fact property coverage, quality evidence, runtime cost, route cost, selected trigger-budget policy, trigger-budget metadata, ratio-source fields, named release-policy profile metadata, performance-baseline provenance, performance evidence bundle cost/readiness/cache/trend metadata, covariance tradeoff gate metadata, product-trace-replay/action-audit/action-execution provenance, selfcheck sample-quality/fusion provenance, world-model signal workflow provenance, feedback-policy candidate config/control-default provenance and safety metrics, selector replay runtime-delta evidence, product runtime drift evidence including promotion-contract coverage, pre-generation probe comparison, claim-risk localization, claim factuality probe comparison, counterfactual verifier-audit, triple-extraction fixture-matrix quality, trace-level triple-audit, action-gate, evidence-handoff, and world-model summaries, external-evidence comparison source/record provenance, and adapter-family coverage metadata. Optional `latency`, `balanced`, and `audit` runtime profiles fill unset performance/cost gate defaults while leaving explicit quality thresholds such as AUROC to the caller.
- `build_mechanism_handoff_evidence_bundle.py` is the release-gate boundary for target-specific mechanism world-model handoffs. It consumes one or more promoted `world_model_rule_candidate_handoff` reports, loads their ProductTrace rows, summarizes target coverage, supported/refuted verification counts, accept/abstain actions, source-citation counts, and source-family coverage, then writes a recursive artifact manifest and optional `report:*:*` registry record. `compare_release_candidates.py` and `run_release_candidate_registry_workflow.py` can consume that bundle by file path or registry key; the gate fails closed unless the bundle report has `workflow=mechanism_handoff_evidence_bundle`, `status=promote`, a passing bundle gate, full trace source-citation coverage, and a verified manifest. `frontier_audit` now defaults the registered `report:truthfulqa-frontier-smollm2-l80-mechanism-handoff-evidence-bundle:0.1` key, but explicit bundle paths disable that default for local experiments.
- `run_release_candidate_registry_workflow.py` registers a final local release candidate by writing the release-candidate report, building a manifest over that report plus the selected readiness, route, optional performance manifests, optional product-trace-replay manifests and action-audit/action-execution gate reports, optional selfcheck-signal-fusion manifests, optional world-model-signal workflow manifests, optional feedback-policy manifests, optional selector-replay manifests, optional product-runtime-drift manifests, optional external-evidence baseline comparison reports resolved from file paths or registry keys, optional pre-generation and claim factuality probe comparison manifests resolved from file paths or registry keys, and optional adapter-family matrix report, recursively verifying the whole chain, and recording the verified candidate in `ArtifactRegistry` with recommended runtime, selected runtime profile, selected trigger-budget policy, covariance quality-gate metadata, INSIDE sampling / trigger-budget cost evidence when available, route quality, covered-fact property ids/counts and per-property quality metrics, tail latency, attempted-route, retrieval-use metadata, optional route and required-route retrieval provenance-filter policy metadata, structured-fact robustness per-property budget metadata, performance-baseline record metadata, performance evidence bundle readiness/cost/cache/trend/score-fusion metadata, product-trace-replay/action-audit/action-execution metadata, selfcheck signal-fusion workflow status/report/record/sample-quality metadata, world-model signal workflow status/report/record/release-gate metadata, feedback-policy workflow status/report/record/candidate-policy/safety metrics, selector replay runtime-delta metadata, product runtime drift metadata including promotion, pre-generation, claim-risk localization, claim factuality, counterfactual verifier-audit, trace-level triple-audit, action-gate, evidence-handoff, and world-model evidence summaries, external-evidence comparison source/record/status/recommended-route metadata, pre-generation and claim factuality probe comparison source/record/redline metadata, required adapter-family route metadata, named release-policy profile metadata, and optional canonical plus paraphrase `structured_fact` robustness evidence. `ProductPromotionContract` preserves the recommended route, required-route baseline, structured-fact robustness per-property metrics, and release-gated probe comparison handoffs so product traces can show which KG-covered predicates and detector comparison artifacts were actually gated. `--release-policy-profile` currently exposes `research_smoke`, `candidate_release`, `strict_structured_fact`, and `frontier_audit` default bundles that only fill unset gate values; explicit CLI thresholds still override them. `--require-structured-fact-robustness` adds the configured canonical/paraphrase route keys to required-route evidence metadata but evaluates them under the dedicated `structured_fact_robustness_*` gate, including the property ids/counts that define the gated covered-fact scope; `strict_structured_fact` enables the same requirement while keeping ordinary required-route thresholds separate from per-property `property_metrics` coverage/quality gates, and `frontier_audit` adds the strict adapter-family/world-model/runtime-drift promotion/pre-generation/claim-risk-localization/counterfactual/triple-audit/covered-fact/action-gate/evidence-handoff/world-model evidence and product-trace action-audit/action-execution defaults. `--fingerprint-cache` persists the recursive manifest fingerprint cache across repeated local release checks so unchanged source artifacts are not re-hashed, `--manifest-fingerprint-workers` enables bounded parallel artifact fingerprinting for large local manifests, `artifact_cache.artifact_fingerprint_cache` reports requests, hits, misses, entries, and hit rate, and the workflow report records compare / comparison-write / manifest-build / promotion / workflow-report-write phase timings for release-gate overhead analysis.
- `compare_release_candidates.py` and `run_release_candidate_registry_workflow.py` can now opt into an uncertainty-escalation evidence gate through `--uncertainty-escalation-workflow` or `--uncertainty-escalation-workflow-key`. The gate verifies the workflow artifact manifest and can require minimum record count, uncertainty-trigger rate, retrieval-evidence rate, maximum final false-accept rate, and maximum false-accept-rate delta; registry metadata preserves the report, manifest, source/record, trigger/retrieval rates, and false-accept deltas.
- `--require-product-runtime-drift-pre-generation-evidence` is now a first-class final release gate in `compare_release_candidates.py` and `run_release_candidate_registry_workflow.py`, and `frontier_audit` enables it by default. It fails closed unless the product-runtime drift report contains pre-generation probe comparison coverage, manifest-verification, model/run breadth, redline pass-rate, best test-label AUROC, best redline AUROC, and redline-margin metrics. The selected evidence propagates through release metadata, `ProductPromotionContract`, `export_product_promotion_contract.py`, `ProductTrace` runtime metadata, and `run_product_runtime_baseline.py` aggregation so release-gated pre-generation evidence cannot silently disappear after product handoff.
- `--require-product-runtime-drift-claim-factuality-evidence` is now a first-class opt-in final release gate in `compare_release_candidates.py` and `run_release_candidate_registry_workflow.py`. It fails closed unless the product-runtime drift report contains claim factuality probe comparison coverage, manifest-verification, model/run breadth, redline pass-rate, best test-label AUROC, selective accuracy/coverage, best redline AUROC, and redline-margin metrics. The selected evidence propagates through release metadata, `ProductPromotionContract`, `ProductRuntimeEvidenceBundle`, `ProductTrace` runtime metadata, and `run_product_runtime_baseline.py` aggregation so release-gated claim-level probe evidence cannot silently disappear after product handoff.
- Frontier release evidence now follows the same runtime handoff rule for both citation/source-family batches and rerun rollups: `ProductPromotionContract` and `ProductTrace` metadata preserve `citation_batch_track_status`, rerun-rollup/base-track statuses, promoted rerun tracks, and rollup/expected/observed/missing/duplicate/unexpected/candidate counts; `run_product_runtime_baseline.py` aggregates them under `promotion_contract.frontier_release_evidence`; and `compare_product_runtime_baselines.py` can fail closed on a non-promoted citation-batch or rerun-rollup track, missing rollup evidence, nonzero missing/duplicate/unexpected batch counts, or nonzero missing/invalid/blocked rerun candidates.
- `TrajectoryAuditReport` adds a ProductTrace-native structural audit for agent/tool workflows, labeling findings as factual, referential, logical, procedural, or scope issues. It checks accepted refutations, unsupported accepted claims, missing or mismatched action results, action-planning issues, and final-answer/decision contradictions without adding learned detectors or network dependencies. `ProductTrace.to_bounded_dict()` now carries `summaries.trajectory_audit`; `product_runtime_metrics()` exposes both full-trace and bounded-summary trajectory counts; `run_product_runtime_baseline.py` aggregates those counts into report/manifest/registry metadata; and `compare_product_runtime_baselines.py` can fail closed on trajectory-audit taxonomy drift when explicit gates are configured.
- `run_manifest_fingerprint_worker_sweep.py` replays saved artifact-manifest verification over one or more manifests under multiple bounded worker counts, using the same optional seed fingerprint cache for each sample, then records timing/cache summaries and recommends the fastest passing worker count for future `--manifest-fingerprint-workers` release checks. This gives local teams an empirical worker setting without rerunning model work, verifier routes, or release-candidate comparison.
- `run_citation_batch_rollup_worker_sweep.py` replays saved citation/source-family batch rollups under multiple bounded worker counts, writes per-worker rollup reports plus a top-level manifest, and recommends the fastest passing or promotion-ready worker count for `rollup_citation_search_batch_evidence.py --max-workers`. This tunes local batch evidence aggregation without rerunning citation search, retrieval, model scoring, or release-candidate comparison.
- `verify_artifact_manifest.py --max-workers` exposes the same bounded parallel artifact fingerprinting for standalone manifest checks; use it with `--fingerprint-cache` when large local artifacts are repeatedly verified outside the release-candidate workflow.
- Directory artifact fingerprinting now reuses the cache-signature file-list scan for full content hashing on cache misses, preserving deterministic directory digests while avoiding duplicate directory enumeration in release and manifest verification gates.
- `run_release_gate_overhead_baseline.py` reads one or more saved release-candidate registry workflow reports and writes a dependency-free overhead baseline over total time, phase-total time, per-phase timing, artifact fingerprint cache reuse, artifact JSON cache reuse, and the slowest observed phase. It can gate maximum total/phase time, minimum weighted fingerprint-cache hit rate, minimum last-run fingerprint-cache hit rate for warm-cache validation, and minimum report count, then optionally records the result as a `report:*:*` registry artifact.
- `export_product_promotion_contract.py` converts a promoted release-candidate report or registry-workflow payload into a compact, manifest-backed `ProductPromotionContract` artifact with product-trace action-audit/action-execution gate provenance, external-evidence baseline-comparison provenance, frontier release-evidence verdict provenance, counterfactual verifier-audit provenance, plus runtime-drift promotion/triple-audit/action-gate/evidence-handoff metadata, and can record it as `product_promotion_contract:*:*` in the local release registry. The exported contract includes a derived `summary` block, and `product_promotion_contract_summary()` exposes the same dependency-free view for callers that need a short release handoff: source/gate status, runtime recommendation, verifier-route quality/cost, action gates, grouped runtime-drift evidence counts, recommended records including frontier release evidence when present, control defaults, and the runtime budget policy.
- `ProductPromotionContract` consumes a promoted release-candidate report, registry-workflow payload, or exported contract artifact and produces a dependency-free product handoff containing runtime defaults, verifier-route metadata, covered-fact property scope, covariance quality-gate provenance, performance evidence bundle status/cost/cache metadata, score-fusion and selected-fusion provenance, product-trace-replay workflow provenance including action-audit/action-execution gate reports and rates, selfcheck-signal-fusion sample-quality/fusion provenance, world-model signal release-gate provenance, context-sensitivity workflow provenance, external-evidence baseline-comparison handoff provenance, frontier release-evidence verdict provenance including multiple-testing, citation-batch, and frontier rerun-rollup track/count fields, pre-generation probe comparison/redline provenance, counterfactual verifier-audit provenance, feedback-policy workflow provenance and candidate policy/default paths, validated feedback-derived `ControlPolicyConfig`, selector replay runtime-delta evidence, product runtime drift evidence including promotion-contract coverage, triple-extraction fixture-matrix drift summaries, trace-level triple-audit summaries, evidence-handoff summaries, trace-level world-model evidence summaries, and trace-level context-sensitivity evidence summaries, plus a `ProductRuntimeBudgetPolicy`; `load_product_promotion_contract()` resolves explicit or ordered default compact contract paths once, while `ProductRuntimeEvidenceBundle` / `load_product_runtime_evidence_bundle()` lazily attach optional promotion-contract artifact-manifest verification and local registry provenance, auto-discover a sibling `evidence-handoff-artifact-manifest.json`, and can expose handoff manifest verification, source contract path, audit path, status, before/after missing-field counts, present/missing metric counts, and evidence-group statuses in runtime trace metadata, plus explicit selfcheck-signal-fusion, world-model-signal, context-sensitivity, external-evidence baseline-comparison, pre-generation probe comparison, triple-extraction fixture-matrix, and counterfactual verifier-audit workflow manifest verification and registry metadata. Runtime `ProductTrace` metadata carries the compact promotion summary as `promotion_contract_promotion_summary` and the frontier release verdict as `promotion_contract_frontier_release_evidence`; `product_runtime_metrics()` exposes headline `promotion_contract_promotion_summary_*` and `promotion_contract_frontier_release_evidence_*` fields alongside product-trace replay plus action-audit/action-execution gate status/report/rate fields as flat `promotion_contract_*` keys; the bounded trace metadata also carries the compact promotion summary, frontier release-evidence status/report/track fields, external-evidence comparator status, recommended route, route pass, text-redline pass, pre-generation probe redline fields, counterfactual audit status/pass-rate/false-invariance fields, and evidence-handoff coverage/manifest/metric-gap/group-status fields; `build_product_trace_corpus.py` now materializes redaction-safe `summaries.triple_coverage` and `metadata.trace_corpus.triple_coverage_summary` so runtime baselines can retain trace-level triple-audit evidence without storing claim/evidence text; `run_product_runtime_baseline.py` aggregates those fields into per-trace records, summary, manifest metadata, and registry metadata; and `compare_product_runtime_baselines.py` can fail closed on action-gate rate drift, counterfactual and trace-level triple-audit handoff coverage/quality drift, promotion-contract evidence-handoff coverage, trace-level world-model participation/coverage/conflict/low-agreement/trace-gap drift, and trace-level context-sensitivity participation/coverage/flagged-rate/trace-gap/max-ratio drift. `calibrated_control_demo.py --promotion-contract` can pass that feedback-derived policy into `RiskController`, use the artifact directly, and still allow explicit CLI budget overrides. By default, the demo now prefers the v1.9 SmolLM2 promotion contract when present, preserving the `0.191662s` cache-only runtime recommendation; refreshed evidence-handoff exports now require `65/65` frontier-audit handoff fields including frontier release-evidence verdict provenance before a new handoff is considered complete.
- `ProductPromotionEvidenceExport` now enriches the v1.6 SmolLM2 promotion contract from explicit local child reports, including a structured-QA counterfactual verifier audit with `70` answer-mismatch probes, `pass_rate=1.0`, and `false_invariance_rate=0.0`, plus source-family route-summary covered-fact property rollups over `9` fact groups. `enrich_product_trace_triple_audit.py` adds an offline bridge for full ProductTrace JSON or JSONL sidecars: it extracts conservative `claim_triples` or reuses metadata-supplied triples, retrieves local evidence, attaches status-aware strict triple-evidence `audit_report` metadata, and writes enriched traces for runtime baselines. Source-family correction handoffs now emit model-answer triples and structured refutation evidence in ProductTrace JSONL rows, so exact covered-fact corrections can enter the same trace-level triple-audit path without an external evidence corpus. The v1.8 handoff fills all then-required handoff fields (`38/38` present) and promotes trace-level triple-audit quality with `triple_claim_coverage_rate=1.0`, `triple_audit_claim_coverage_rate=1.0`, `triple_audit_pass_rate=1.0`, and `triple_slot_coverage_rate=1.0`; refuted claims carry `evidence_relation=refutes_claim`, so this remains an audit handoff rather than pretending refutation evidence is support. The v1.9 handoff keeps that boundary, adds the selected runtime-cost provenance from the v13 promoted release candidate, and ships a verified handoff artifact manifest that product traces can consume directly; refreshed handoffs add frontier release-evidence verdict provenance as the seventh required group.
- Local release-candidate artifacts: `benchmark_manifest:tiny-local-staged-qa-release-candidate:0.4` combines the tiny-gpt2 offline readiness/runtime baseline with the TruthfulQA l80 staged structured QA route baseline, and `benchmark_manifest:tiny-local-inside-staged-qa-release-candidate:0.5` adds a registered INSIDE sampling profile gate. `benchmark_manifest:smollm2-l20-inside-staged-qa-release-candidate:0.6` is the first non-tiny local release candidate: SmolLM2-135M l20 readiness promotes `truth_proj` AUROC `0.682`, uncached forced-answer cost `38.786s`, cache-only replay cost `0.339s`, and the same staged structured QA route baseline. Its full-sample INSIDE profile selects `adaptive_selfcheck` but only reduces generated samples to `0.937` of fixed while `inside_generation` remains `1.001` of fixed. `benchmark_manifest:smollm2-l20-inside-triggered-staged-qa-release-candidate:0.7` promotes the same readiness/route chain with `truth_proj` top-25% triggered INSIDE: 39/154 statements sampled, 115 skipped, fixed `inside_generation` reduced from `467.563s` to `118.513s` (`0.253x`), and the promoted `adaptive_selfcheck` triggered run using 110 generated samples. `benchmark_manifest:smollm2-l20-inside-trigger-budget-derived-staged-qa-release-candidate:0.8` reuses the same readiness/route evidence, folds in the derived trigger-budget sweep, and selects the quality-balanced top-40% triggered `adaptive_selfcheck` profile with sample-count ratio `0.472`, `inside_generation` ratio `0.503`, semantic-entropy AUROC `0.570`, and 218 generated samples from a single largest-budget source run. `benchmark_manifest:smollm2-l20-inside-trigger-budget-derived-staged-qa-release-candidate:0.9` adds `performance_baseline:smollm2-l20-performance-baseline:0.9` as a required verified gate and proves the performance handoff matches the selected runtime. `benchmark_manifest:smollm2-l20-inside-trigger-budget-derived-adapter-gated-staged-qa-release-candidate:1.0` adds required structured-state and state-transition adapter-family evidence. `benchmark_manifest:smollm2-l20-inside-trigger-budget-derived-retrieval-adapter-gated-staged-qa-release-candidate:1.1` additionally requires the promoted adapter-family matrix with `structured_state`, `state_transition`, and `retrieval_groundedness` routes present and promoted. `benchmark_manifest:smollm2-l20-inside-trigger-budget-derived-structured-retrieval-audit-staged-qa-release-candidate:1.2` adds `benchmark_manifest:smollm2-l80-retrieval-structured-qa-route:0.5` as a required retrieval-structured-QA audit baseline. The current registered SmolLM2 default is `benchmark_manifest:smollm2-l20-inside-trigger-budget-derived-strict-structured-retrieval-audit-staged-qa-release-candidate:1.6`, which keeps the same runtime evidence, selected staged `structured_qa` product route, performance baseline, adapter-family matrix with `retrieval_structured_qa` coverage, and required retrieval audit, then explicitly gates the product route at `max_retrieval_use_rate=0.0` and `max_mean_attempted_route_count=1.1`. Version 1.6 also requires promoted selector replay, refreshed product runtime drift evidence, non-oracle retrieval evidence provenance, and answer-echo retrieval stress control. Its compact deployable handoff is `product_promotion_contract:smollm2-product-promotion-contract:1.6` at `artifacts/smollm2_product_promotion_contract_v1_6/product-promotion-contract.json`. The required audit route `benchmark_manifest:smollm2-l80-retrieval-structured-qa-route:0.6` promotes with selected `238`, decision accuracy `0.992`, false-supported rate `0.000`, false-refuted rate `1.000`, verified detection `1.000`, verified false alarm `0.009`, runtime about `0.845s`, and `410` retrieval hits under a `450` hit budget; its answer-echo stress control shows false-supported rate `0.980` and false-refuted rate `0.000`, so answer-derived retrieval evidence is blocked as grounding. The selected runtime remains layer `-12`, batch size `8`, `outputs` capture, worker count `1`, `truth_proj` AUROC `0.682`, and the quality-balanced `top_0p4` triggered `adaptive_selfcheck` budget. The same verified sweep can still drive a cost-first release-gate override that selects the top-10% trigger budget for latency-constrained deployments. This makes performance-gated, selector-replay-checked, runtime-drift-gated, non-oracle stress-gated strict structured-retrieval-audit, trigger-derived INSIDE the current real-model default direction.
- Frontier-audit materialization: `artifacts/frontier-audit-release-candidate-v0/`
  is the first full local probe of the stricter `frontier_audit` profile. It
  verifies its manifest and preserves the route/release registries, workflow
  report, and comparison report, but correctly blocks promotion. External
  evidence and mechanism handoff evidence promote; legacy canonical/paraphrase
  covered-fact route artifacts can now supply per-property metrics by deriving
  them from `covered_fact_score_dump` and `verified_records_jsonl`. The remaining
  v0 product blockers were stronger readiness/performance evidence, strict
  adapter-family coverage, learned/external triple-prediction matrix evidence,
  required retrieval-route coverage, product-trace action gates, and complete
  runtime-drift handoff coverage. `artifacts/frontier-audit-release-candidate-v1/`
  removes the adapter-family blocker by using
  `artifacts/frontier-audit-strict-adapter-family-matrix-v0/`: strict audit now
  promotes `triple_evidence` and a rule-backed `state_transition` route with
  `RuleBasedWorldModelAdapter` evidence. The release candidate remains blocked
  on the other frontier gates, which is the expected fail-closed posture.
  `artifacts/frontier-audit-release-candidate-v2/` then removes the
  product-trace action-gate blocker by using
  `artifacts/smollm2_product_trace_replay_workflow_action_gated_v0/`: the
  replay workflow promotes, action-audit records `0.0` error/malformed/missing
  retrieval rates over `12` traces, action-execution records `0.0` missing,
  unexpected, and request-id-mismatch rates, and both child reports are present
  in the release manifest. The remaining frontier blockers are readiness
  AUROC/cost, required retrieval-route selected/property coverage, performance
  baseline comparison, and complete product-runtime-drift handoff metrics
  including action-gate drift summaries. `artifacts/frontier-audit-release-candidate-v3/`
  then removes the learned/external triple-prediction matrix blocker by pointing
  the release gate at the v1 external-prediction adversarial matrix; the
  candidate still blocks fail-closed on the same readiness/performance,
  required-route, and runtime-drift gaps.
- Blind-spot evidence expansion now has a product-facing collection handoff:
  `build_blind_spot_evidence_collection_corpus.py` compiles the registered
  SmolLM2 L80 plan into source-discovery requests for the `65` high-priority
  targets. The artifact is explicit that requests are not verifier evidence,
  preserving the release gate invariant that only provenance-audited external
  sources can support a future route promotion.
- That handoff has now been executed for Wikidata: the target-specific fetch
  resolves `61` entities and writes `292` CC0 source docs, provenance-audited as
  `external_candidate` with no claim-id, row-link, or label metadata leakage.
  The lexical query rerun still refutes `0/89` entrenched false blind spots,
  while structured QA over those covered facts promotes with decision accuracy
  `1.0`. The explicit question/property mapper then narrows the `10/89`
  covered-fact candidates to `1/89` deployable correction gate: Tesla founder
  maps to Wikidata `P112`. `build_question_property_correction_handoff.py` now
  converts that slot into a target-specific structured-QA corpus and
  ProductTrace JSONL: the existing verifier refutes the Elon Musk answer,
  records a high-risk abstain decision, and dry-runs the abstain action. The
  product route can use that slot as precise property evidence, but broad
  blind-spot recall still needs citation or world-model expansion. The later
  source-family fact-collection replay generalizes this path:
  `build_source_family_structured_qa_correction_handoff.py` consumes the
  promoted covered-fact route and mapped QA candidate, then writes a
  source-family ProductTrace/action-result handoff with the same
  `refuted -> high/abstain -> dry_run` control outcome for the Tesla/Martin
  Eberhard slot.
- The unresolved queue has now been made executable without promoting it as
  evidence: `build_unresolved_blind_spot_evidence_queue.py` filters the
  high-priority collection corpus through the question/property mapping report,
  removes the `1` resolved Tesla/P112 slot, and emits an adapter-ready queue
  with `46` unresolved targets, `176` citation/search requests, and `6`
  world-model or calculator-rule authoring requests. Refreshed queue builds now
  also include available `counterfactual_probe` requests by default and expose
  those counts in the summary, manifest, and registry metadata. The queue is
  manifest and registry backed, but remains source-discovery, probe-generation,
  or rule-authoring input until external documents or verifier probes are
  ingested and provenance-audited. `build_counterfactual_probe_handoff.py`
  now lowers those counterfactual rows into non-evidence claim JSONL, generated
  probe records for `eval_counterfactual_verification.py --records`, and a
  pending-generation sidecar for requests that need an external or human probe
  generator instead of silently turning weak perturbations into evidence.
- The rule branch of that unresolved queue is now first-class work, not an
  orphan count: `build_unresolved_world_model_rule_stubs.py` emits `6`
  sanitized rule stubs from the queue, records that `model_answer`,
  `record_index`, and `target_rank` were present in the source rows but not
  copied, and normalizes the `temporal_freshness` row into a
  `temporal_consistency` contract. Replaying the existing rule-authoring
  adapter blocks as `needs_inputs` with `0/6` executed, then
  `build_world_model_rule_input_collection_plan.py` lowers the rows into `6`
  typed input tasks across `2` batches (`5` numeric, `1` temporal snapshot).
  The follow-up `audit_world_model_rule_input_plan.py` prevents bad work from
  hardening into rule execution: the registered audit is `needs_requeue`, with
  `9` findings over those `6` tasks, including `4` person/place/entity
  questions that should move from `quantity_or_arithmetic` to
  `entity_disambiguation` and `5` numeric rows that still need explicit
  candidate-claim binding before any calculator execution.
  `requeue_world_model_rule_stubs_from_audit.py` now closes that feedback loop
  by rewriting the `4` mismatched unresolved stubs as `entity_disambiguation`;
  replaying the adapter/input planner turns them into `4` entity-role tasks in
  one collection batch. `fill_world_model_rule_inputs_from_entity_bindings.py`
  then fills that batch from explicit source-backed bindings, the adapter
  executes `4/4` corrected stubs, and the promotion gate promotes all four
  candidate `refuted` rows with `0` blocked or pending. These promoted rows are
  still deterministic rule candidates, not direct verifier evidence; ProductTrace
  visibility remains gated by a downstream rule-candidate handoff.
  `fill_world_model_rule_inputs_from_numeric_bindings.py` adds the sibling
  numeric/calculator input-fill boundary. In tests, a valid source-backed
  numeric binding executes through the calculator adapter and can promote once
  the deterministic evidence carries the matching `source_citation`; on the real
  unresolved `record-190` population task, the registered artifact is correctly
  `blocked` with `binding_requires_review` and `missing_subject_entity` because
  the question says only "the country" and no subject entity is bound.
  `run_world_model_rule_authoring_adapter.py` now also executes explicit
  `temporal_consistency` inputs. The registered `record-326` temporal replay
  supplies `claim_time`, `source_time`, `retrieved_at`, and `source_citation`,
  observes one supported source-timestamp candidate, and the promotion gate
  promotes it with the temporal metadata preserved. This still only validates
  freshness/order of the source snapshot; it does not make a ProductTrace-visible
  content correction until a citation or structured-evidence route proves the
  claim content.
  `run_world_model_rule_authoring_adapter.py` also has a conservative
  `mechanism_consistency` shell for causal/procedural tasks. It can observe a
  source-backed mechanism only when `mechanism`, `precondition`, and
  `source_citation` are supplied, and it requires explicit `mechanism_status`
  before promotion. Missing status produces `insufficient_evidence`, so
  ProductTrace remains blocked until a citation-backed fill, promotion, and
  handoff all pass.
  `fill_world_model_rule_inputs_from_mechanism_bindings.py` now supplies that
  citation-backed fill boundary for mechanism rows: it requires an explicit
  `mechanism_status`, source citation, and non-evidence marker, and rejects
  unreviewed or invalid statuses before adapter execution. This completes the
  tool slot but not the evidence collection; real causal/procedural corrections
  still need source-backed mechanism bindings before handoff.
  Registered mechanism chains now cover two question families: `record-10`
  ("How long do diamonds last?") through WTAMU/GIA material-stability evidence,
  and the repeated Africa poverty trend records (`record-133`, `record-165`,
  `record-274`, `record-299`) through World Bank rate/headcount evidence. The
  final remaining chain adds Bill Gates high-school rows (`record-27`,
  `record-134`) as `supported` and UFO extraterrestrial-premise rows
  (`record-212`, `record-224`) as `refuted`. These fill, promotion-gate, and
  enter ProductTrace as dry-run actions. Product scope is now `9/9`
  causal/procedural tasks for the typed mechanism queue, not broad release
  readiness.
  Product implication: unresolved deterministic rule work now enters the same
  explicit input-plan pathway as the larger source-family rule lane, but it
  still cannot affect ProductTrace or release gates until candidate execution,
  promotion, and handoff all pass.
- The citation/search part of that unresolved queue now has an external-adapter
  handoff boundary: `build_citation_search_adapter_handoff.py` writes
  sanitized JSONL requests for external search systems and can ingest returned
  result JSONL into source docs plus an external retrieval corpus. The current
  SmolLM2 L80 question-only artifact emits `176` high-priority requests with no
  `record_index`, `target_id`, `model_answer`, or `label` fields. The
  claim/entity-aware follow-up handoff uses the dependency-free
  `eigentruth.verify.search_planning` planner to remove model-answer phrases
  from internal candidate queries and emit safe `alternate_queries`; it keeps
  `176` requests, expands them to `555` query variants, and records `132`
  removed disallowed phrases. Both handoffs are execution bridges, not verifier
  evidence.
- The return path is now gateable in one command:
  `run_citation_search_evidence_workflow.py` consumes local adapter-result
  JSONL, reruns the handoff ingestion, audits the resulting corpus as
  external-candidate provenance, runs the blind-spot query sweep, and can
  compare controlled versus external sweeps. It is intentionally fail-closed:
  snippets that pass provenance still block if they do not refute blind spots
  under the configured verifier/query gates.
- The command boundary is now explicit:
  `run_external_citation_search_adapter_workflow.py` writes sanitized request
  JSONL, invokes a local external citation/search command with `{input}` and
  `{output}` placeholders, then automatically runs the same fail-closed evidence
  workflow. This is the preferred next adapter entry point because command
  output cannot bypass provenance, query-sweep, or controlled-comparison gates.
- A first real adapter run is registered:
  `run_wikipedia_citation_search_adapter.py` uses MediaWiki/Wikipedia search
  with query de-duplication, optional alternate-query fallback, rate limiting,
  retries, snippets, and extracts. The SmolLM2 L80 question-only workflow
  returned `504` result documents for `168/176` sanitized requests and passed
  provenance, but it is still `blocked`: the external query sweep refuted
  `0/89` entrenched false answers and the controlled-vs-external comparison
  generalization gap is `1.0`.
- The claim/entity-aware rerun is also registered:
  `report:truthfulqa-frontier-smollm2-l80-claim-entity-wikipedia-citation-search-adapter-workflow:0.1`
  runs the same command boundary with sanitized alternate queries. It returns
  `528` result documents for `176/176` requests, passes provenance, and reduces
  exact model-answer copy rate from `0.310` to `0.235`, but route promotion still
  blocks because the external sweep refutes `0/89` entrenched false answers and
  the controlled-vs-external generalization gap remains `1.0`. Product
  implication: better Wikipedia query planning improves collection hygiene and
  coverage, but the next frontier adapter needs source-family, official-source,
  or structured claim-to-fact evidence rather than another generic lexical
  overlap sweep.
- The source-family adapter contract is now part of the handoff:
  `SourceFamilyPlan` and `plan_source_families(...)` add dependency-free
  official/reference/encyclopedic/scholarly/news/statistical source-family hints
  to each sanitized citation request. `build_citation_search_adapter_handoff.py`
  writes the plan into request JSONL and summary counters, and preserves
  adapter-returned `source_family` metadata during ingestion. This does not
  promote evidence; the registered
  `report:truthfulqa-frontier-smollm2-l80-source-family-citation-search-handoff:0.1`
  artifact has `176` sanitized requests, `0` source docs, no reserved-field
  leakage, and family counters `reference=176`, `encyclopedic=176`,
  `scholarly=156`, `official=36`, `official_statistics=4`, `news=4`. It gives
  the next official-source command adapter enough structure to route
  quantitative, timestamped, definition, and person/role claims without
  reintroducing labels or model answers.
- A concrete local source-family adapter now consumes that contract:
  `run_source_family_citation_search_adapter.py` ranks caller-supplied local
  source catalogs by query overlap, source-family compatibility, official-source
  preference, and freshness hints, then writes the same adapter-result JSONL
  expected by the fail-closed evidence workflow. The registered
  `report:source-family-citation-search-adapter-smoke:0.1` artifact is synthetic
  smoke only: `2` sanitized requests, `3` catalog docs, `2/2` requests with
  results, `4` result rows, no reserved-field leakage, and a verified manifest.
  This proves the adapter entry point without claiming TruthfulQA route quality.
- The local source-family path now has a one-command evidence workflow:
  `run_source_family_citation_search_workflow.py` builds the sanitized request
  handoff, runs the local source-family catalog adapter, and then invokes the
  same provenance/query-sweep/controlled-comparison gates. The registered
  `report:source-family-citation-search-workflow-smoke:0.1` artifact is still
  synthetic and intentionally `blocked`: `2` requests, `2` catalog docs, `2`
  adapter results, provenance pass, but no passing external blind-spot strategy
  or controlled-vs-external comparison. This proves the closed local-catalog
  workflow without letting weak catalog matches bypass release gates.
- Cached external source docs can now be normalized into source-family catalogs:
  `build_source_family_catalog.py` lifts safe provenance fields such as
  provider, URL, and timestamps out of metadata while rejecting reserved
  label/model-answer fields. The registered Wikidata catalog artifact converts
  `292/292` target-specific cached Wikidata source docs into adapter-ready
  `reference` catalog rows with provider `wikidata` and a verified manifest.
- The first real cached-source source-family workflow is registered as
  `report:truthfulqa-frontier-smollm2-l80-wikidata-source-family-citation-workflow:0.1`.
  It consumes `176` unresolved citation requests, returns results for `160/176`,
  writes `480` Wikidata-backed adapter results, and passes provenance. It still
  blocks route promotion: the external query sweep refutes `0/89` entrenched
  blind spots, verified false alarm is above the external gate, and the
  controlled-vs-external generalization gap remains `1.0`. Product implication:
  the source-family workflow is real, but generic cached Wikidata reference
  facts are not enough; the next evidence source needs more targeted official,
  citation, or claim-specific structured catalogs.
- `audit_source_family_coverage.py` now converts that negative result into a
  source-catalog acquisition queue without treating it as verifier evidence.
  The registered Wikidata coverage audit consumes the same `176` requests and
  `480` adapter results, observes `reference=480` returned documents, and marks
  `176/176` requests as still missing non-fallback target coverage:
  `scholarly=156`, `official=36`, `official_statistics=4`, and `news=4`.
  Official-source-preferred requests are `36`, but `0` have an official result.
  Product implication: the next adapter milestone is not more reference-source
  matching; it is filling this acquisition plan with official/statistical,
  scholarly, news, or source-specific catalogs and then rerunning the same
  fail-closed workflow gates.
- `plan_source_family_catalog_collection.py` deduplicates that acquisition queue
  into provider-oriented source collection tasks. The registered SmolLM2 plan
  compresses `176` acquisition rows and `200` missing family gaps into `28`
  tasks: `scholarly=21`, `official=5`, `official_statistics=1`, and `news=1`.
  Each task carries query variants, provider hints, request ids, queue
  fingerprints, and `not_verifier_evidence=true`. Product implication: future
  OpenAlex/Crossref, official-site, statistics API, news, or source-specific
  adapters can now fill a small task set before the resulting catalogs re-enter
  `run_source_family_citation_search_workflow.py`.
- `run_crossref_source_family_catalog_adapter.py` executes the first real
  provider-specific collection pass for the scholarly slice. The registered
  Crossref catalog consumes the `21` scholarly tasks, runs `42` query variants,
  writes `48` deduplicated scholarly source-family documents, and keeps label,
  row id, target id, and model-answer fields out of the catalog boundary. A
  combined Wikidata-reference plus Crossref-scholarly source-family workflow
  then has `340` source catalog documents, returns `528` adapter results for
  `176/176` requests, and passes provenance, but still blocks promotion because
  no external query strategy passes and the controlled-vs-external comparison is
  blocked. Product implication: the scholarly catalog slot is now executable
  and auditable, but broad bibliographic Crossref matching is not enough; the
  next catalogs need better query relevance, official/statistical coverage, or
  source-specific semantics before route defaults can change.
- `run_worldbank_source_family_catalog_adapter.py` now fills the first
  official-statistics slot through the World Bank Indicators API. The registered
  population catalog consumes the single `official_statistics` collection task,
  writes `217` country-level official statistics rows after filtering aggregate
  regions, and records `0` request errors. Combining Wikidata, Crossref, and
  World Bank catalogs gives the source-family workflow `557` source docs and
  `12` World Bank result rows. Promotion still blocks, but source-family
  coverage improves materially: `official_statistics` is now covered for `4/4`
  requests, `scholarly` is covered for `100/156`, and missing target rows drop
  from `176` to `84`. Product implication: official data adapters can shrink
  the blind-spot acquisition frontier even before they are strong enough to
  change runtime route defaults.
- `run_gdelt_source_family_catalog_adapter.py` adds the news-provider slot for
  the reduced source-family plan. The command boundary is dependency-free and
  rejects label, row id, target id, and model-answer metadata; request coverage
  may stay in the non-evidence task/report, but request ids are not copied into
  catalog documents. The registered live GDELT run is an `empty` fail-closed
  artifact because the public API returned rate-limit errors for both query
  variants in this environment. A wider reduced Crossref replay did progress
  the scholarly side: `6` scholarly tasks produced `69` additional catalog docs
  with `0` request errors. Combining Wikidata, both Crossref catalogs, and World
  Bank gives the source-family workflow `626` source docs and reduces missing
  target rows to `44` (`official=36`, `scholarly=16`, `news=4`). Product
  implication: the next runtime-relevant work is official-site retrieval plus a
  retried/news-specific source adapter; this is still catalog coverage, not
  route promotion.
- `run_official_site_source_family_catalog_adapter.py` now fills the official
  webpage slot through an auditable URL-seed file rather than a new search
  dependency. The registered run maps `9` official URLs across USDA ERS, Tesla,
  WHO, World Bank, and NOAA to the `5` remaining official tasks, fetches `7`
  pages, records `2` Tesla access-denied errors, and writes `9` official catalog
  docs with clean catalog/source-doc reserved-field scans. Combining this
  official-site catalog with Wikidata, Crossref, reduced Crossref, and World
  Bank catalogs keeps route promotion blocked, but coverage improves again:
  missing target rows fall from `44` to `28`, with `official=32/36`,
  `official_statistics=4/4`, and `scholarly=128/156` covered. Product
  implication: URL-seeded official sources are now a concrete verifier-adapter
  collection lane; the remaining release work is source-specific scholarly
  relevance plus a non-rate-limited news source, not another generic retriever.
- `run_openalex_source_family_catalog_adapter.py` now fills a complementary
  scholarly lane through OpenAlex Works search, with optional abstract
  reconstruction and the same label/request-id-free catalog boundary. The
  registered run consumes `5` scholarly tasks, runs `40` query variants, writes
  `52` deduplicated scholarly docs, and records `0` request errors. The local
  source-family adapter also now supports opt-in family-diverse reranking, which
  keeps non-fallback preferred families ahead of fallback reference rows in
  top-k evidence. Combining OpenAlex with the existing catalogs and enabling
  `--adapter-diversify-source-families` keeps route promotion blocked but drops
  missing target rows from `28` to `4`: `official=36/36`,
  `official_statistics=4/4`, and `scholarly=156/156` are covered. Product
  implication: the evidence panel now needs a replacement or seeded news lane
  for the final food-affordability gap; scholarly/official coverage is no
  longer the limiting source-family issue.
- `run_seeded_url_source_family_catalog_adapter.py` now provides that replacement
  lane. The registered seeded-news run uses `4` AP/PBS URL seeds for the final
  news task, writes `4` news source-family docs with short fallback text, records
  `0` errors, and keeps the same request-id/label-free catalog boundary.
  Combined coverage is now complete (`official=36/36`,
  `official_statistics=4/4`, `scholarly=156/156`, `news=4/4`), while route
  promotion still blocks on query-sweep/comparison gates. Product implication:
  the evidence panel can now show source-family-complete coverage, but the
  product should still present the route as unpromoted until comparison-quality
  gates improve.
- The source-family structured QA path now separates route quality from
  blind-spot coverage. `build_source_family_qa_corpus.py` extracts `18`
  label-free structured QA facts from the completed source-family result set,
  and `run_source_family_structured_qa_route_workflow.py` promotes exact
  covered-fact route quality with decision accuracy `1.0` and false-supported
  rate `0.0`. The follow-up claim-mapping audit correctly blocks product
  handoff because those `18` facts map to `0/89` current SmolLM2 entrenched
  blind spots. `plan_source_family_structured_qa_fact_expansion.py` converts
  that blocker into a `ready_for_collection` queue with `89` structured fact
  requests, `70` entity-resolution requests, `66` citation requests, `26`
  world-model/calculator-rule requests, and `14` fact-disambiguation tasks.
  `build_source_family_structured_qa_fact_collection_corpus.py` then compiles
  that queue into adapter-ready JSONL sidecars: `356` structured-fact requests,
  `210` entity-resolution requests, `198` citation requests, `14`
  fact-disambiguation requests, and `28` world-model/calculator-rule requests,
  with no `label`, `answer`, or `model_answer` fields in the request rows.
  Product implication: the structured QA route is safe for exact covered facts,
  but blind-spot correction needs this claim-specific request corpus executed
  and remapped before any ProductTrace correction handoff is allowed.

### Next Verification Adapter Work

- Improve evidence retrieval beyond generic Wikipedia lexical search: preserve
  the same command boundary and consume `source_family_plan`, then feed real
  official/source-specific catalogs or structured citation APIs through
  `run_source_family_citation_search_workflow.py`. Promote only if the
  provenance audit, external query sweep, and controlled-vs-external comparison
  pass.
- Connect `StructuredStateVerifier` to additional live database, business-rule, and domain-state sources behind optional adapters; the current reproducible path uses local JSON state sources, stdlib SQLite, and mapped local tool outputs.
- Connect additional QA/database/world-model route policies to production adapters while preserving trace-visible match reasons for each selected or skipped tool.
- Generalize real side-effecting executors behind the same trace, execution-policy, timeout, idempotency-ledger, and tool-output mapping interfaces; the current production-like demo proves the path with a guarded local SQLite mutation and optional JSON/SQLite replay, while hard cancellation remains adapter-specific.
- Run real learned or external fact-level extractors through the current offline `LookupTripleExtractor` prediction boundary, then keep any promoted extractor behind the existing `ClaimExtractor` / triple-extractor protocols.
- Extend route-level metrics to future adapters and real fixtures, then compare them with `compare_verifier_routes.py` so new tools are judged by route hit rate, false support, false refutation, runtime cost, tail latency, cache efficiency, downstream conformal control impact, Pareto frontier status, route-specific promotion decision, and explicit gates before adapter work proceeds.
- Replace label-derived oracle evidence with real retrieval/database/calculator evidence and rerun verifier/retrieval ensemble reports on Qwen/SmolLM2.
- Use local corpus fixtures from `build_evidence_fixture.py` as the reproducible baseline before adding networked retrieval extras.
- Add optional retrieval/database/calculator verifier adapters behind extras, keeping core dependencies unchanged.
- Extend concrete domain/world-model adapters from the current rule-based transition adapter toward production simulators and learned/external world-model adapters.
- Add semantic-entropy sampling probes, RAG groundedness adapters, and optional SAE/ReFT integrations behind extras.
- Add benchmark/demo domain examples where facts depend on state transitions, physical constraints, or business rules.
- Compare real verifier/retrieval ensembles against the current `truth_proj` baseline under the same conformal false-alarm budget.

### Later Control-Plane Hardening

- Persist full trace/report/action-result artifacts from benchmark and demo commands into local registries by default when requested.
- Add policy graph composition only after current dataclass-based policy config becomes insufficient.
- Add steering-lambda sweep utilities after monitor-only diagnostics are stable on larger replicated runs.
- Extend registry persistence from metadata records to saved subspace/manifold tensors.

## Product Tagline

EigenTruth is a factuality control plane for LLM systems: it observes hidden-state drift, calibrates risk, routes uncertain generations through tools, and leaves creativity where creativity belongs.

EigenTruth 是 LLM 系统的事实性控制平面：观测表征漂移，校准风险，把不确定生成路由到工具验证，同时保留模型该有的创造力。
